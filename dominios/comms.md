# Dominio: COMUNICACIONES (agente @comms)

Doc de dominio + bitácora. El agente lo lee al arrancar y lo actualiza al cerrar.

## Estado del dominio (nacimiento, 2026-07-07)
- galgas-supabase: HTTPS a Supabase OK (cert GTS Root R4 + HTTPClient simple — lección cerrada). Falta: gateway HTTP local del RX (PLAN v5) y prueba de robustez WiFi en planta.
- RuView: LoRa 433 RV1 mesh+ACK OK en banco. Falta: alcance real, salto repetidor, RSSI↔distancia, SSID GIMAP case-sensitive. Regla dura: jamás mDNS; beacon UDP 50505.
- FrioSeguro: SIM800 código completo, JAMÁS probado con SIM real. APN/credenciales producción pendientes.
- Cosechador: NRF24 spec cerrada (payload 8B, auto-ACK), sin armar.

## ⚖️ DOCTRINA: ¿LoRa o WiFi? (pedida por Matías 2026-07-07 — regla de oro del dominio)

**El árbol de decisión (en orden):**
1. **¿El dato entra en decenas de bytes por mensaje?** (agregados, eventos, telemetría) → LoRa es POSIBLE. Si necesitás forma de onda cruda, FFT completa u OTA → **LoRa NO alcanza**: el crudo va a SD local y por el aire viaja solo el resumen (patrón datalogger), o directamente WiFi.
2. **¿Hay AP/router confiable a <30-50m indoor (o <100m con línea vista)?** → **WiFi nomás.** No agregues LoRa donde el WiFi ya llega: es hardware, firmware y una capa de fallas extra. Ej: FrioSeguro en comercios (router del local), ESP-RX de galgas (220V + WiFi de planta).
3. **¿Distancia larga (100m–km), campo abierto, sin infraestructura, o zonas que el WiFi no cubre?** → **LoRa**, con gateway que puentea a WiFi/internet (patrón RuView: nodos LoRa → ESP32-S3 gateway → cloud). Con obstáculos/estructura metálica: mesh con repetidores (frame RV1 ya lo soporta).
4. **¿No hay NADA (ni WiFi ni posibilidad de gateway cerca)?** → GSM/SIM800 (patrón FrioSeguro minero) o registrar offline en SD y descargar después.

**El mito de energía (importante):** "LoRa siempre gasta menos" es FALSO en nuestros sistemas. Un ESP32 que despierta, manda una ráfaga corta por WiFi y vuelve a deep sleep (patrón galgas-supabase validado) compite con LoRa e incluso gana si el AP está cerca — el costo real de WiFi es la CONEXIÓN (DHCP/TLS), no el TX; se amortiza espaciando reportes. LoRa gana sin discusión cuando: la distancia obligaría a repetidores WiFi, el nodo reporta muy seguido (conexión WiFi cara por ciclo), o el receptor debe escuchar continuo (RX LoRa duty-cycled << WiFi asociado).

**Resumen por sistema (estado actual):**
| Sistema | Enlace correcto | Por qué |
|---|---|---|
| Galgas Dreyfus (emisores) | WiFi en ráfaga + deep sleep → RX local/Supabase | planta con infra, payload chico, ya validado |
| Galgas RX/gateway | WiFi (220V) | enchufado, es el puente |
| Datalogger RuView (nodos) | **LoRa** → gateway ESP32-S3 | distancia/zonas sin WiFi, telemetría de agregados; crudo queda en SD |
| FrioSeguro comercios | WiFi del local (fallback GSM si el local no tiene) | AP a metros |
| FrioSeguro minero | WiFi campamento + fallback SIM800 | infra irregular |
| Cosechador | NRF24 punto a punto | 2-10m, 8 bytes, ya especificado |

**Regla final:** ante la duda, prototipá con WiFi (ya lo dominás, debug fácil) y migrá a LoRa SOLO si una prueba de alcance real lo exige. La prueba de alcance manda, no la intuición.

## Métricas de enlaces (tabla viva)
| Enlace | Escenario | RSSI | Loss | Fecha |
|---|---|---|---|---|
| (vacío — completar con pruebas reales) | | | | |

## Bitácora
- 2026-07-07 — Agente creado por Claude Fable. Próximo paso sugerido: plan de prueba de robustez WiFi de galgas-supabase (cortes inducidos + reconexión) y prueba de alcance LoRa de RuView.

- 2026-08-06 — **LOGO! en casa: red medida + guía de acceso remoto gratis.** Entregable `dominios\LOGO_RED_GUIA.html` (verificado: 0 errores de tags, renderizado Edge headless 14729 px). Hallazgo principal: `192.168.0.1` **NO es el TP-LINK sino el cablemodem DOCSIS de Fibertel** (302 a `Docsis_system.php`, lighttpd, PHP/5.3.2) → el TP-LINK es el segundo router y ahí nace el doble NAT. IP pública `181.231.144.34`; traceroute con saltos 2 y 3 mudos → CGNAT no confirmado ni descartado (test definitivo: IP WAN del modem vs. pública). Barrido /24: vivos solo `.1 .46 .232 .254`; **ningún LOGO en línea, puerto 102 cerrado en todos**. Decisión de transporte: **port forwarding DESCARTADO** (CGNAT probable + exponer un PLC), **Tailscale free con subnet router `--advertise-routes=192.168.0.0/24` en PC siempre prendida** = la respuesta (atraviesa CGNAT/doble NAT sin abrir puertos); ZeroTier plan B; Chrome Remote Desktop complemento. Doble NAT se mata poniendo el TP-LINK en modo AP (IP `.5`, DHCP OFF, cable LAN-LAN). DoD del enlace definido en la guía: prueba de aceptación (celular en datos móviles) + prueba de recuperación (reboot de la PC dedicada) + `enlace.log` del watchdog. Detalle completo en `dominios\logo_acceso_remoto.md`.

- 2026-07-08 [BRIEFING GIMAP] — leer ../BRIEFING_EQUIPO_GIMAP.md y los 4 docs (PARTE_GIMAP, PRESUPUESTO_ENERGIA, PROTOCOLO_CALIBRACION, INGENIERIA_NODO_1ANO). Para vos: LoRa emisores, WiFi solo receptor (220V, internet LDC). 0.33Hz entra sobrado por LoRa. MACs en galgas/docs/MAIL_DREYFUS_IT.md.

- 2026-09-03 — **GALGAS DREYFUS rev D: contrato de enlace LoRa del nodo ↔ receptor.** Entregable: `C:\Proyectos\galgas\hardware\COMMS_REV_D.md` (620 líneas, secciones C0-C7). Diseño, no implementación: nada se tocó de firmware.
  - **6 hallazgos leyendo el código, 3 de ellos defectos del enlace de hoy:** (1) **la trama NO lleva identificador de nodo** — con 2 nodos idénticos y toda la detección basada en comparar cadena A vs B, el receptor no puede saber de quién es lo que recibe; es el defecto que rompe el producto y nadie lo había marcado; (2) `sx1278Init()` **nunca escribe `RegSyncWord` ni el largo de preámbulo** → los dos extremos corren en el default de reset (sync `0x12`, el mismo que RuView, que también es 433); (3) **`sx1278Init(433000000UL)` está FUERA de la banda ISM** (433,05–434,79) → se pasa a **433,500 MHz**, que además queda 420 kHz de 433,92 (telemandos comerciales). Más: 2 nodos de 300 s sin coordinación (jitter de sueño, costo cero); `EST_VENTANA_RX` tira lo que recibe (4,5 % del presupuesto de energía a la basura); y `RegSymbTimeout` (10 bits) pone un **techo duro de 1047 ms** a la ventana a SF7.
  - **Contrato de trama cerrado**: cabecera común 4 B (`tipo|id|seq`) + `0xA3` medición **22 B** (con `boots` que pidió @firmware, y desvío/mín/máx que destraban la etapa 5 de calibración), `0xA3` corta 9 B, `0xA4` cédula **18 B**, `0xA5` volcado de las 60 conversiones crudas 3×50 B (sólo bajo comando y en modo banco), `0xC0` downlink **8 B** con 8 comandos (SHUNT_CAL y MODO_BANCO son los dos pedidos de §5.2). ACK = campo `ack_eco` del uplink siguiente. Sin OTA ni comandos destructivos por aire.
  - **Airtime medido en papel (SF7/BW125/CR4:5)**: uplink 22 B = **63,7 ms = 5,5 mAs** a +17 dBm, contra los **150 ms / 13,05 mAs** que C10 tenía presupuestados → **la trama creció de 13 a 22 bytes gratis y sobran 7,5 mAs/ciclo**. Entran hasta 63 B en el presupuesto actual. **El cuello de botella no son los bytes: son la ventana de RX y las colisiones.**
  - **Ventana de RX**: 200 ms = 2,2 mAs = **4,5 % del presupuesto**. Tabla de precios del agrandamiento firmada para @energia: 350 ms −26 días, 500 ms −52 días, 1000 ms −127 días sobre 2,43 años.
  - **Hallazgo propio sobre el ensayo 2.d**: el receptor que contesta **demasiado rápido** falla igual que el que contesta tarde (si transmite antes de que el nodo entre a `RX_SINGLE`, no hay preámbulo que detectar) → **guarda de 10 ms** en el contrato, y el ensayo debe loguear el Δt `RxDone→TX`, no sólo "entró/no entró".
  - **Si 2.d falla**: escalera 0(diagnosticar Δt) → 1(preámbulo 12 símbolos) → 2(medir `RegFei`, y si hay corrimiento **BW250, que baja el airtime**) → **4: downlink best-effort con reintento por ventana hasta ver el `ack_eco`**. Con 50 % de éxito por ventana el comando llega en <25 min con 96,9 %. Agrandar la ventana (escalón 3) es el que menos me gusta y es explícitamente el anteúltimo recurso: nada de la operación depende del downlink — la detección A vs B es 100 % uplink.
  - **Receptor: validé la tarea FreeRTOS con dos correcciones.** No alcanza "otra tarea": tiene que ser **prioridad 3 en el MISMO core que `loopTask`** (para preemptirlo; el core 0 lo tiene WiFi/lwIP) y **la respuesta pre-armada** (`pendingCmd[2]` bajo `portMUX`, nada de red/`String`/`malloc`/`Wire` adentro). Agujero declarado: durante un OTA la caché se deshabilita y la radio queda sorda — aceptable, se loguea. Plan B con disparador: si el soak da <98 % de entrega, radio a micro dedicado por UART. **Estimación: 6 días** (rango 5-7) + ~2 días de cambios en el nodo, aparte.
  - **Pendiente de firma**: @energia (ventana, jitter, reintento de evento, y el regalo de los 7,5 mAs), @muestreador (¿alcanza media+σ+mín+máx o hace falta el volcado en cada calibración?), @backend (columnas `nodo_id/seq/boots/mcusr/flags/n/ganancia/rssi/snr`).
  - **Métricas: siguen vacías.** No hay una sola medición de este enlace todavía — ni RSSI, ni loss, ni la ventana. La etapa 2 del bring-up es la que las produce (2.c, 2.d, 2.e) y hasta entonces todo lo de arriba es cálculo, no dato.

- 2026-09-03 (b) — **COMMS_REV_D rev 2: me comí un error de 41 kHz y lo encontró @firmware implementando.** Publiqué `RegFrf = 0x6C5F5C` como constante en la tabla C1. Eso es **433,459 MHz, 41 kHz corridos**. La cuenta correcta es `433,5e6 × 2^19 / 32e6 = 7 102 464 = 0x6C6000` = 433,500 MHz **exactos** (433,5 MHz cae justo en un múltiplo de `FSTEP` = 61,03515625 Hz: el error de redondeo es **cero**, tampoco eran los "26 Hz" que escribí).
  - **Lo que más me importa del error no es el número: es el modo de falla que habilitaba.** El nodo **calcula** el Frf; el receptor **habría copiado la constante de mi documento** → dos extremos 41 kHz desalineados sobre BW125, enlace intermitente y asimétrico, y el sospechoso hubiera sido el cristal de un módulo. **Es exactamente el escenario del escalón 2 de mi propio C4.** Un dato duplicado a mano se desincroniza y ningún chequeo lo ve (la misma lección que ya está en memoria como "una sola fuente de verdad en EDA" — vale igual para un doc de protocolo).
  - **Arreglo estructural, no puntual**: saqué la constante del documento. Ahora va **la fórmula** (`((uint64_t)freq_hz << 19) / 32000000ULL`), la misma que ya tiene `sx1278.cpp`, para que los dos extremos la deriven igual. Y agregué la **evidencia 2.g al bring-up**: se leen de vuelta los 3 bytes de `RegFrf` **de los dos chips** y tienen que ser idénticos (`6C 60 00`). Regla que anoto para el dominio: **un `RegFei` no significa nada hasta que 2.g pase** — el 90 % de los "corrimientos de frecuencia" van a ser un `Frf` distinto en los dos extremos, no un cuarzo.
  - **NACK (pedido de @firmware): resuelto sin v2 del protocolo y sin agrandar la trama.** El byte 21 pasa de `ack_eco` a **`ack`**: bit7 = NACK, bits 6-4 = motivo, bits 3-0 = cmd. Motivos: `NO_IMPLEMENTADO` (el caso de `VOLCADO_CRUDO` hoy), `DESCONOCIDO`, `ARG_INVALIDO`, `PRECONDICION`, `ENERGIA`, `EEPROM`. **Lo que @firmware ya escribió cumple el formato nuevo sin tocar una línea** (`ack = cmd` con cmd ≤ 0x07 ya es bit7=0, motivo=0). El problema real que cierra: con la v1, *"no ejecuté"* y *"no me llegó"* valen los dos `0x00` → el receptor reintentaría 25 minutos un comando que el nodo recibió perfecto y va a rechazar siempre, y lo registraría como fallo de enlace. Tres reglas: el RX deja de reintentar con **cualquier** respuesta (un NACK es final, no una entrega fallida); contadores `cmd_ok`/`cmd_nack[motivo]`/`cmd_sin_respuesta` **separados** (sólo el tercero es métrica de enlace; mezclarlos arruina la estadística de loss); y el nodo contesta `DESCONOCIDO` en vez de callarse.
  - **Reintento de evento (C2.7): lo parto y digo lo incómodo.** El despertar por PCINT2 en power-down es **@energia firma / @firmware implementa** (toca la corriente de reposo, el parámetro del que cuelga toda la autonomía y que sigue sin una sola medición); la trama es mía y está hecha; qué se mide en un evento es de @muestreador (un evento duró 4,4 s y otro **2,8 s** — los 3 s de medición **no cubren el corto entero**). **Y mientras no exista el despertar, mi C2.7 es código muerto**: `g_reed` sólo se consume al final del ciclo, así que un evento no dispara nada hasta 300 s después. Si eso no entra antes de octubre, la detección queda con resolución de 300 s — **decisión de alcance para el Director, no pendiente para descubrir en la parada de planta.**
  - **Aviso de @firmware sobre offsets → C5.2.1 nueva.** La trama va por offsets: agregar un campo sin mover el largo declarado no rompe compilación ni transmisión, y del otro lado todo corre un byte y **la medición sigue teniendo pinta de número**. Del lado del receptor es peor que del lado del nodo: el nodo al menos revienta el buffer, el receptor **nunca falla** y entrega un valor plausible y falso a Supabase, al LCD y a la comparación A vs B. Cinco barreras, tres de ellas de compilación/test: tabla única de offsets con `static_assert(último_off+largo == 22)`; **rechazo por largo EXACTO** (nada de `if (n > max) n = max`, que es justo el patrón de `sx1278VentanaRx()`); **trama de oro** (vector hexa fijo + valores esperados como test, el análogo del chequeo con mutantes de @firmware); plausibilidad en runtime (`ganancia` tiene 8 valores válidos de 256 — un corrimiento de un byte casi siempre la rompe); y la regla de fondo: **el `tipo` identifica el LAYOUT, no el contenido — si un campo se mueve o la trama cambia de largo, cambia el `tipo`** (`0xA3`→`0xA6`). Cuesta cero bytes y convierte el peor modo de falla (basura verosímil) en el mejor (descarte ruidoso con contador visible en `/health`).
  - **Corrección de mi estimación, sin excusa**: di ~2 días para los cambios del nodo, @firmware los hizo en **~3 horas**. Estimé mirando el contrato (cuántos campos toco) en vez del terreno (cuánta infraestructura ya existe: máquina de estados explícita, config versionada con CRC, chequeador con mutantes). **Error de método.** Los 6 días del receptor siguen en pie porque ahí esa infraestructura no existe — pero aviso apenas empiece si el número baja, con el mismo criterio con que acepto que este se me fue para arriba.

- 2026-09-04 (nocturno local) — **El nodo GIMAP no reconectaba el WiFi: un parpadeo del enlace lo dejaba mudo para siempre.** Branch `nocturno/local-2026-09-04-el-nodo-que-se-queda-mudo` (`f57483f`, pusheado; **sale de `nodo-gimap/wifi-y-flasheo-2026-08-24`, no de `main`**). Repo `datalogger`, firmware del nodo 1.0.5 → 1.0.6.
  - **El defecto**: `red.conectar()` se llamaba **una sola vez, en el arranque**; después el loop no volvía a mirar el enlace nunca más, y `_broadcast()` hacía `except Exception: pass`. Tres hechos que juegan juntos y ninguno alcanza solo: el **CYW43 del Pico W no se reasocia solo** (perdido el AP queda desasociado hasta que alguien llame a `connect()`), **UDP broadcast no acusa recibo** (y sobre una interfaz caída `sendto()` puede ni tirar), y el **LED seguía en `OK`** = "conectado y midiendo" según la tabla del propio README. Un router que se reinicia dejaba al nodo midiendo para adentro, para siempre, avisando que estaba todo bien. **El que mentía era el nodo** — el reverso de "un visor sin datos no miente".
  - **Ata un cabo del PORTFOLIO**: *"el nodo se murió a los 276 s, causa sin determinar"*. Desde la PC **no hay diferencia entre un nodo muerto y uno mudo**. Ahora hay tres hipótesis distinguibles con lo que el nodo publica: `recon > 0` = fue el WiFi · `uptime_s` reseteado = se reinició · nada = muerto.
  - **Arreglo**: `enlace_ok()` = `active() AND isconnected()` (la segunda mitad importa: `conectar()` termina con `active(False)`), vigilancia colgada del `if` de estado que ya existía (2 s), y `_reenganchar()` con espera creciente 0/5/15/30/60 s para siempre. **NO reinicia el nodo**: `boot.py` cuenta arranques y a los 3 restaura los `.bak` del último OTA — un router que se reinicia no tiene por qué deshacer una actualización. Socket nuevo, `ip` actualizada, buffer a medio llenar descartado (si no, el paquete cruza el hueco declarando `fs=200` con un pozo de minutos adentro, y la fs efectiva medida es justo el número que el README pide anotar el día del encendido) y deadline reiniciado.
  - **Regla de dominio que dejo escrita: el estado de un enlace sin acuse se pregunta a la capa de abajo, nunca se infiere del resultado del envío.** Vale igual para el LoRa de galgas: un `send()` que retorna no prueba entrega, y ahí el equivalente de `enlace_ok()` es el `ack` del uplink siguiente (C2, rev 2).
  - **Visibilidad**: `recon`/`mudo_s`/`tx_err` en el JSON de estado → chip "cortes de WiFi" en el visor + línea en `rx_gimap.py`. Son **acumulativos** porque mientras el enlace está caído ese JSON no llega; `seq` no sirve (sólo avanza con lo que se manda, un corte no deja salto).
  - **Verificación sin hardware**: `tools/test_reenganche_gimap.py` corre el **`main()` real** con dobles y **reloj virtual** (90 s de nodo en 2 s, cero esperas), modelando que el CYW43 no se reasocia solo y que `sendto()` no tira → **12 chequeos verdes, 6/6 mutantes muertos** (el 1.º reproduce el bug original). Sin regresión en protocolo/red/ota. Chip del visor **mirado en el navegador**: `docs/evidencia/visor-cortes-wifi.png`.
  - **Falta banco (5 min, en la misma sesión del primer encendido)**: apagar el router un minuto y ver volver los paquetes con `recon=1`; y mirar el LED durante el reenganche — en la Pico W va **por el chip de WiFi** y `conectar()` hace `wlan.active(False)`.
  - **Hallazgo abierto que NO toqué**: sin MPU6050, el nodo avisa 3 s y después transmite `ax..gz = 0` para siempre, indistinguible de un acelerómetro quieto, sin ningún campo en el estado que lo diga. Mismo patrón, otro sensor; es decisión de diseño, no parche de paso.
