# Dominio: verificador (agente @verificador)

Bitacora del evaluador independiente. Generator != evaluator: aca van los veredictos con evidencia observable. Definicion: ~/.claude/agents/verificador.md (backup en ../agentes/verificador.md).

## Bitacora

- 2026-07-13 [AUDITORIA 3 branches nocturnos FrioSeguro pre-merge a main] — repo C:\Proyectos\frioseguro. Entorno: Python 3.14.3, Node v24.14.0, deno AUSENTE, supabase CLI 2.90.0 (no toque nube). Volvi a main limpio, no mergee, no toque origin.

  ### Branch 1 `nocturno/local-2026-07-10-b-secret-scan` → VEREDICTO: MERGE-OK
  Evidencia:
  - `py_compile` scan_secrets.py + test → OK.
  - `python tools/test_scan_secrets.py` → **"OK: 13 checks pasaron, 0 fallaron"**, exit 0.
  - Corrida sobre el repo REAL: el nocturno declaro "34 coincidencias, 14 al nivel de fallo (>= high)". **Reproduce EXACTO**: 11 critical + 3 high + 20 info = 34 total, 14 failing, exit 1.
  - Cold-start OK: solo stdlib + `git ls-files`, corre `python tools/scan_secrets.py .` sin setup.
  - El scanner FUNCIONA de verdad: distingue anon(info) de service_role(critical), enmascara, respeta `scan-secrets: allow` e ignora `.example.*`.
  - Deudas menores (NO bloquean el merge de la herramienta): (a) FN latente — token de Telegram inline en URL `.../bot<token>` no se detecta por el `\b\d` (word boundary); el repo hoy usa concatenacion `"bot"+TOKEN` asi que no esta expuesto, pero es fragil. (b) FN latente — bot IDs de Telegram de 11+ digitos no matchean (`\d{8,10}`).
  - **HALLAZGO CRITICO DE DOMINIO (no del branch, del repo)**: el scanner CONFIRMA credenciales reales commiteadas y en la historia de git: `sbp_` management token (llave maestra de la cuenta) en 10 archivos `tests/*.js`, JWT service_role en `tests/recreate_clean.js:4`, token de bot Telegram en `firmware_modular/config.h:94` + `config_SANTA_CRUZ.h:44` + `DISPOSITIVOS.md:150`. SECURITY_AUDIT.md lo documenta honesto. **ROTAR antes del primer cliente pago** (ver RUNBOOK backend paso 5.x). Escala a Director.

  ### Branch 3 `nocturno/local-2026-07-12-lint-device-config` → VEREDICTO: MERGE-OK (1 deuda menor)
  Evidencia:
  - `py_compile` lint_device_config.py + test → OK.
  - `python tools/test_lint_device_config.py` → **"OK: 35 checks"**, exit 0 (coincide con lo declarado por el nocturno).
  - Corrida sobre `firmware_modular/config.h` REAL → detecta correcto OTA_OPEN (ERROR, exit 1), TELEGRAM_HARDCODED (warn), AP_OPEN + SIM_APN (info). `config_SANTA_CRUZ.h` → 0 errores (config bien formada: temps -40/-18/-10 presentes, sin OTA/PLACA definidos → checks de provisioning correctamente omitidos).
  - Cold-start OK: stdlib, `--from-header` / `--json-in` / stdin.
  - Deuda menor: `_coerce()` no parsea literales C comunes `-18.0f` (sufijo f) ni `(-18.0)` (parentesis) → los toma como string → dispara TEMP_PRESENT error. **Falla SEGURA** (bloquea el flasheo, no lo deja pasar), pero daria falso-error si algun config.h usa esos formatos. Los config.h actuales usan `-18.0` sin sufijo → OK hoy. Fix sugerido: strip trailing f/F y parentesis en `_coerce`.

  ### Branch 2 `nocturno/local-2026-07-11-b-resumen-mensual` → VEREDICTO: MERGE-CON-FIXES
  Evidencia (deno ausente, pero Node 24 corre el .test.ts via type-stripping — offline, no toca nube):
  - `node --check` de monthly_summary.ts + index.ts → parse OK.
  - `node --test` → **13 pass / 0 fail** (el doc declara "13 checks / pass 13" → reproduce EXACTO).
  - Logica revisada: `monthWindow` correcto (medianoche AR = 03:00Z; cruce de anio dic→ene ok). index.ts filtra por [from,to) en SQL (la funcion pura confia en el caller, correcto). Columnas/tablas referenciadas EXISTEN en el schema real: `readings.temp_over_critical` (SETUP_COMPLETO.sql:142), `power_events` (195), `door_events` (210), `devices.subscription_status` (95).
  - Migracion `migration_monthly_summaries.sql`: **IDEMPOTENTE** (CREATE TABLE/INDEX IF NOT EXISTS, UNIQUE inline no se duplica en re-run). SETUP_CRON: idempotente (guard `unschedule` antes de re-`schedule`) y coherente (`0 8 1 * *` = 05:00 AR, calcula mes anterior correcto).
  - **FIX 1 (bloqueante para prod) — RLS FALTANTE**: `monthly_summaries` NO tiene `ENABLE ROW LEVEL SECURITY` ni policy. TODAS las demas tablas del schema lo tienen (SETUP_COMPLETO.sql:969-983). Sin RLS + anon key embebida en la app Android → cualquiera con la anon key lee/escribe/borra los resumenes de TODOS los clientes (fuga cross-tenant). Corrobora el hallazgo ya anotado por @backend (backend.md:14, "tarea pendiente"). Fix: append a la migracion `ALTER TABLE monthly_summaries ENABLE ROW LEVEL SECURITY;` + policy por owner consistente con readings/alerts. El cron escribe con service_role (bypassa RLS) → no se rompe.
  - **FIX 2 (riesgo de confianza) — falso "protegida todo el mes" con 0% cobertura**: device sin datos → `verdict.sinPerdidas=true` → texto "0 perdidas: la mercaderia estuvo protegida todo el mes" aunque `coveragePct=0`. El test 187-198 lo consagra como intencional. Enganoso justo en el artefacto de retencion. Fix: si coveragePct < umbral, emitir "datos insuficientes este mes" en vez de afirmar proteccion (monthly_summary.ts:217 + 282-286).
  - DEUDA 3: `temp_over_critical` (ReadingRow, seleccionada en index.ts:85) y `temp_critical` (DeviceInfo) declaradas/traidas pero NUNCA usadas en `buildMonthlySummary`. El veredicto depende 100% de la tabla alerts e ignora el flag que el firmware computa. Data pull muerto + robustez perdida. Considerar usar temp_over_critical como respaldo del veredicto.
  - DEUDA 4: FN latente en veredicto — `severity === "critical"` no captura `"emergency"` (schema_v2.sql:114 lista info/warning/critical/emergency). El firmware HOY solo emite critical/warning (alerts.h:64) → no activo. Si algun dia emite emergency, el veredicto lo pierde. Considerar `["critical","emergency"].includes(sev)`.

  Resumen: **B1 MERGE-OK, B3 MERGE-OK**, ambos con deuda menor no bloqueante. **B2 MERGE-CON-FIXES** (RLS + mensaje de 0% cobertura antes de exponer a un cliente pago). El codigo de B2 compila y sus 13 tests pasan; los fixes son de hardening SQL y de mensaje, no de arquitectura.

- 2026-09-02 [GALGAS / DREYFUS — P6: revision cruzada del esquematico `nodo_galga_v3.kicad_sch` rev B antes del layout] → **VEREDICTO: RECHAZADO PARA RUTEO. P6 NO se cierra; se reabre como P6b (revision de la rev C).** Informe con evidencia: `C:\Proyectos\galgas\hardware\VERIFICACION_P6_2026-09-02.md` (sin commit). Resumen: **el circuito pasa, el archivo no** — la topologia es correcta y 20 de 22 cuentas cierran con mi aritmetica, pero hay 9 hallazgos que obligan a tocar el esquematico antes de poner una pista.
  - Evidencia propia corrida hoy (KiCad 10.0.0): `sch erc --severity-all` → **0 violaciones** (reproduce lo declarado); `sch export netlist` regenerado → **identico byte a byte al `.net` entregado**; 45 componentes / 47 redes contadas a mano; pinouts verificados contra `datasheets\ADS1220_TI.pdf` (pag. 4 y 8) y `MCP1700_Microchip.pdf` (pag. 3).
  - **PASA con evidencia:** `E-/REFN` **NO** esta unido a GND (red `Net-(D3-A2)` de 5 nodos, ninguno en la red `GND` de 34) → el riesgo #1 de la guia de perfboard no existe en el diseno. Pinout ADS1220 **16/16** y ATmega328P-AU TQFP-32 **32/32** correctos. Las 16 redes de un solo nodo son todas `+no_connect`: ningun pin flotante sin marcar. Q2 (proteccion contra pila invertida) bien orientado, re-derivado. Topologia rev B del supercap (RSC en serie, CSC sobre V_SC, U4 desde V_SC) confirmada en el netlist. Shunt-cal en paralelo con el brazo activo, como pide la formula de C3.
  - **BLOQUEAN EL RUTEO (9):**
    - **G1** — 13 componentes sin anotar (`RCAL, CD, CA, CB, CBLK, RR, RP, RL, RG, RSC, CSC, CI, CO`): `kicad-cli sch export netlist` avisa "errores de anotacion" y nadie lo registro; `Update PCB from Schematic` puede renumerarlos en silencio. El ERC 0/0 no cubre anotacion.
    - **G2** — U3 (RA-02) **sin footprint en el esquematico** (`FP_RA02 = ""`, netlist `fp=None`); el PCB se lo inventa con `FOOTPRINT_FORZADO` hardcodeado en `generar_pcb.py:115` = asignacion de huella fuera de la fuente de verdad.
    - **G3** — simbolo del RA-02 mal numerado: **CONFIRMADA la denuncia de @pcb**, con la huella oficial `RF_Module:Ai-Thinker-Ra-01-LoRa` el pin "3V3" cae en el pad 2 = GND del modulo real. La huella propia `galgas.pretty/RA-02.kicad_mod` **si compensa geometricamente** (verifique los 13 pads uno por uno), pero tiene el **pad "14" duplicado y ningun pad "16"**, y solo funciona mientras el simbolo siga estando mal.
    - **G4** — J1 con footprint `MX126-5.0` (paso 5,00) contra la bornera 5,08 que exige el BOM de la guia: el conector comprado no entra en los pads dibujados.
    - **G5** — falta RGD, el pull-down de la compuerta de Q1: la red `CAL_EN` tiene 2 nodos y ninguno va a masa. **La desviacion que @pcb declaro en la guia es un defecto real del esquematico** (durante el POR del ATmega, PD5 es hi-Z y el shunt-cal puede quedar a medio encender: el sintoma es un cero corrido, no una falla). Recomiendo 1 Mohm.
    - **G6** — sin pull-up en `CS` del ADS1220 ni en `NSS` del RA-02: durante reset/arranque/ISP flotan y el esclavo puede manejar MISO **contra el programador ISP**.
    - **G7** — `DIO0` del RA-02 esta en **PD1 (TXD)**: sin serial de diagnostico en un nodo que va a planta. PB0 esta libre y marcado NC. Decidir antes de rutear.
    - **G8** — `ANT` (pin 15) marcado No-Conexion pero la huella tiene el pad: IPEX vs pad+linea de 50 ohm sin decidir, y de eso depende el borde de la placa.
    - **G9** — las dos redes analogicas criticas no tienen nombre (`Net-(D1-A1)` = nodo de la galga, `Net-(D3-A2)` = E-/REFN) y el `.kicad_pro` no tiene netclases: rutear microvoltios a Default y a ciegas.
  - **NO BLOQUEAN el ruteo (16, G10-G25), los de mas peso:** **G10** C12 tiene un **error de factor 10** (10 nA sobre 175 ohm = **1,17 ue**, no 0,12 ue) → endurece P2, la fuga admisible ya vale ~1 ue. **G13** C6 lee el dropout del MCP1700 como "maximo 178 mV" cuando el datasheet (pag. 3) dice **178 tipico / 350 maximo**, y C7 nunca analiza la pila a fin de vida → brownout probable en la ultima TX. **G17** el LDO comprado es **HT7130A (30 mA, TO-92)** y el del esquematico es MCP1700-3002 (250 mA, SOT-23): con el HT7130A el nodo **no transmite** (TX pide 87-120 mA) → bloquea la Placa B. **G14** con Q3 abierto el nodo de ADC7 queda sobre VCC+0,5 (clamp, ~0,17 uA permanentes) y su impedancia de fuente es 248 kohm contra los <=10 kohm que pide el ATmega. **G11** NOTAS par. 8 declara "15 redes con nombre y 32 automaticas" cuando son **5 y 42**: la "revision red por red" no tiene respaldo. **G16** la guia manda 2N7000/BS170 (Vgs(th) hasta 3 V) donde el esquematico usa BSS138: con 3,0 V de compuerta el shunt-cal deja de ser patron. **G19** **`hardware/` esta UNTRACKED en git** — todo galgas (esquematico, netlist, NOTAS, PCB, guia) fuera del repo: cold-start cero, un `git clean -fd` lo borra. **G20** el layout arranco (placement 45/45, 2026-08-21) antes de que P6 cerrara.
  - **Gate de la rev C para liberar el ruteo:** G1 + G2 + G3 + G4 + G5 + G6 + G7 + G8 + G9 resueltos, netlist regenerado, ERC re-corrido y este informe re-corrido punto por punto. **P3 sigue abierto**: el pinout del RA-02 que use sale del datasheet, no del modulo fisico de Matias — hay que medirlo con tester antes de fabricar.
  - No corregi nada (evaluador, no generador). Dueños: @esquematico (G1, G2, G3, G5, G6, G7, G9, G10, G11, G12, G21, G22, G23, G25), @pcb (G3, G8, G15, G16, G18), @hardware (G4, G17), @energia (G13, G14, G24), Matias/@cronista (G19).

- 2026-09-02 [GALGAS / DREYFUS — **P6b**: re-auditoria de la **rev C** del esquematico `nodo_galga_v3.kicad_sch`] → **VEREDICTO: APROBADO CON OBSERVACIONES — SE HABILITA EL RUTEO FINAL. P6b CERRADO.** Informe con evidencia: `C:\Proyectos\galgas\hardware\VERIFICACION_P6b_revC.md` (sin commit). Verificacion **corta y dirigida** (solo lo que la rev C toco), apoyada en mi propio informe de P6. Yo no escribi nada de lo auditado y **no corregi nada**.

  - **Los 9 bloqueantes G1-G9 estan CERRADOS**, uno por uno, con evidencia que corri yo (KiCad 10.0.0, copia aislada):
    - **Cold-start**: regenere el esquematico en un temporal → el `.kicad_sch` y `galgas.kicad_sym` entregados son **identicos** a los que salen del generador (las 1702 lineas de diff son **todas** UUID aleatorios). El `.net` entregado = el regenerado, 5793 lineas, sin diferencias. **Nadie edito el `.kicad_sch` a mano.**
    - **G1 anotacion**: 103/103 referencias con digito; el export de netlist ya no emite la advertencia. **G25 ERC**: el `.kicad_pro` tiene **43 chequeos declarados y 0 en `ignore`**, y el reporte del propio KiCad dice `Ignored checks: - None` (en la rev B listaba 4). 0 errores / 0 advertencias. **El "0/0" ahora significa algo.**
    - **G3 RA-02 — el que mas me preocupaba (dos errores que se compensaban)**: arreglaron los dos. El simbolo tiene la numeracion fisica 1-16 con los **tres** GND (2/9/16), y la huella propia `galgas.pretty/RA-02` es ahora **identica pad por pad, en posicion y tamano, a la oficial `RF_Module:Ai-Thinker-Ra-01-LoRa`**. Sin pad "14" duplicado ni "16" faltante. **Y ningun pad cambio de coordenada fisica respecto de la rev B** → el cobre alrededor del RA-02 y el keepout de antena no se mueven.
    - **G4** J1 pasa a `MKDS-1,5-4-5.08 P5.08mm` y la guia `.md` dice lo mismo. **G5** RGD1 = 1 MOhm (rehice las 3 cuentas: IGSS 100 nA x 1 M = 0,10 V contra Vgs(th) 0,8 V, 8x de margen; tau 27 us; 3 uA de costo). **G6** los 3 pull-ups de 100 k en CS, NSS y RST_LORA, verificados en el netlist. **G7** DIO0 a PB0 (`Net-(U2-PB0)`) y PD0/PD1 a J3 1x3. **G8** decision IPEX escrita en 3 lugares (hoja del esquematico, generador y `LAYOUT.md` par. 4). **G9** 7 etiquetas **locales** sobre el cable + netclases verificadas **en runtime** (el `.net` trae la clase por red: ANALOGICA en `/NODO_A`, `/S_MAS`, `/S_MENOS`; EXCITACION en `/E_REFN`).
  - **LA AFIRMACION FUERTE ES VERDADERA: ninguna red analogica cambio de nodos.** Comparacion de conjuntos de nodos normalizada por el renombre de anotacion: `/NODO_A`, `/S_MAS`, `/S_MENOS` y `/E_REFN` tienen **exactamente** los mismos componentes que en la rev B. Y recorri las 51 redes: **`/E_REFN` sigue sin tocar `GND`**. → **el ruteo analogico que @pcb ya cerro SIGUE VALIDO, no se rehace.** Lo que si cambia para @pcb: la franja de alimentacion (CSC1+CSC2+RB1+RB2 donde habia un solo CSC), DIO0 del pin 31 al 12 del micro, y J3 nuevo.
  - **Busqueda sistematica de "simbolo de N patas sobre huella de M pads" (lo pedido): 0 desajustes en los 55 componentes de la rev C.** Y encontre que en la **rev B eran 4** diodos, no 3: D1, D2, D3 **y D4**, todos sobre SOT-23. JP1 confirmado como `Jumper_2_Bridged` en la rev B. **0 cables de longitud cero** en los 188 wires. Conteo G11 verificado contra el archivo: **51 = 11 + 23 + 17**.
  - **HALLAZGO PRINCIPAL — el brownout NO esta cerrado: @hardware y @esquematico se CONTRADICEN dentro del mismo documento** (`NOTAS_CALCULO.md`, par. 9-bis C23b contra par. 10 C7b, 180 lineas de distancia, **mismo destinatario: @firmware**):
    1. **Fusible BOD:** @hardware manda `BODLEVEL=110` = **1,8 V** (textual: *"es el arreglo principal"*, *"NO 101 (2,7 V)"*); @esquematico escribe *"el BOD tiene que quedar en **2,7 V**"*. **C7b reintroduce el bug que par. 9-bis arreglo** (con 2,7 V el peor ejemplar dispara a 2,9 V y resetea justo en la ultima TX, que es G13). Y `par. 7 P12` le manda a @firmware el back-off **sin mencionar el BODLEVEL** → queda el default del grabador. Un fusible mal grabado no deja rastro.
    2. **Back-off:** @hardware dice +20 a **+17 dBm**; @esquematico dice **+14 dBm** a 45 mA, numero que @hardware declara explicitamente **inexistente** (*"Semtech no publica el consumo de PA_BOOST por debajo de +17 dBm; hay que MEDIRLO"*). Viola la regla dura del proyecto (ningun numero sin la pagina del datasheet al lado).
    3. **Escenarios distintos:** C7b usa ESR 0,8 Ohm (peor caso) pero **omite** los 67 mV de la medicion y **omite R3** (TX 30 s despues de medir). Sumandole la medicion, el margen de +14 dBm no es 90 mV sino **20 mV**.
    → **No bloquea el ruteo** (no cambia ni un componente ni una red). **Bloquea declarar G13 cerrado y bloquea grabar fusibles.** Mi lectura: la sintesis correcta es la de @hardware (R1+R2+R3) y C7b hay que retirarla o subordinarla. **No me toca elegir: arbitra el Director.** → **ESCALA A PORTFOLIO.**
  - **De los 6 abiertos a proposito (P3, G15, G17, G18, G21, G23): estoy de acuerdo con dejarlos abiertos. Solo P3 bloquea FABRICAR** — y ahora **mas que antes**: en la rev B dos errores se compensaban y el modulo hubiera andado igual; en la rev C simbolo y huella siguen **los dos** al datasheet, asi que si el modulo real no coincide **la placa es chatarra**. P3 tiene que dejar de ser "riesgo vivo" y pasar a **item del checklist de fabricacion** (10 min de tester: continuidad entre pads 2/9/16 y que el 3 sea la alimentacion). **G18 (Kelvin de REFP1) no bloquea fabricar pero es GATE DE RUTEO**: si @pcb no lo garantiza por layout y se va al net-tie, **eso cambia el netlist y vuelve a P6** → decidirlo ANTES de rutear.
  - **Bloqueantes de FABRICACION que nadie tenia en la lista (los encontre yo): tres huellas elegidas ANTES que las partes.** **CSC1/CSC2 Eaton HV0810** (C16: *"importar"*, *"en AR no hay"*, **precio PENDIENTE**, *"preguntar a GIMAP"*) es la pieza mas grande de la placa y no esta cotizada; **P2 D1/D2/D3** tienen `D_SOD-323` sin numero de parte (y C18 dice que el ESD5Z5.0T1G **no sirve** ahi); **P10** 100 nF C0G en 0805 (CD1, **en la cadena analogica**) sin stock confirmado. Cualquiera de los tres puede obligar a **rehacer gerbers**. → @hardware, cerrarlos antes de exportar.
  - **Deuda menor con dueno (no bloquea nada):** C25 declara `/V_SC_MID` en ALIMENTACION y **no esta** (queda en Default, igual que `/V_BAT_SENSE`) — @esquematico/@pcb · **`GUIA_ARMADO_PERFBOARD.html` quedo del 21-ago y sigue diciendo "RGD 100 kOhm, no esta en el esquematico"** mientras el `.md` esta al dia: **el HTML es el que se abre para soldar** — @pcb · **IDs C21/C22/C23 duplicados** en NOTAS (par. 9-bis de @hardware contra par. 10 de @esquematico, contenido distinto; C23c ya cita un "C21" ambiguo) — @esquematico · el **par. 8 "Verificacion hecha" quedo con "47 redes" y cita `kicad/erc.rpt`, archivo que ya no existe** — @esquematico · **`FOOTPRINT_FORZADO` no se borro** de `generar_pcb.py` (quedo con D4, redundante, y con un comentario que ya es falso): el segundo camino que G2 pedia eliminar sigue abierto — @esquematico · el **"1 nA" de los pull-ups (C22)** depende de un duty de 10 ms que nadie escribio (si el firmware deja CS bajo los 3,05 s de conversion son 0,305 uA) — @firmware · **C16 y C10c no dan el mismo reposo** (28,4 contra 29,0 uA; vale C10c) — @hardware · **G19 arreglado pero SIN COMMIT**: `hardware/` ya figura como `A` en el indice, pero un `git reset --hard` hoy se lleva la rev C entera — Matias/@cronista.
  - **Nota de proceso, a favor de @esquematico:** `kicad/verificar_sch.py` (anotacion + huellas vacias + ERC 0/0 sin chequeos apagados + advertencia del netlist, con exit code) y `kicad/diff_redes.py` son **harness engineering hecho bien**: no parchean el error, hacen imposible repetirlo en silencio. **Candidatos claros para `C:\Proyectos\biblioteca` via @bibliotecario.** (Salvedad: `diff_redes.py` no normaliza renombres de anotacion y por eso infla las listas de "eliminadas/nuevas"; lo verifique con parseo propio.)
  - **Resumen: habilitado el ruteo final. NO habilitada la fabricacion** (7 items en el par. 10 del informe).

- 2026-09-02 [GALGAS / DREYFUS — **rev D**: auditoria cruzada del esquematico `nodo_galga_v3.kicad_sch` (placa rehecha ENTERA en pasante + modulos + alimentacion nueva), pedido `D13` de C40] → **VEREDICTO: APROBADO CON OBSERVACIONES.** Informe con evidencia: `C:\Proyectos\galgas\hardware\VERIFICACION_REV_D.md` (sin commit). 16 hallazgos numerados **H-1 a H-16**: **2 bloquean LAYOUT**, **5 bloquean FABRICAR/CAMPO**, 9 no bloquean. Yo no escribi nada de lo auditado y no corregi nada. Tercera auditoria de este esquematico (P6 rev B RECHAZADO → P6b rev C aprobado → rev D).
  - **Cold-start: PASA.** Copia aislada, regenerado con `generar_nodo_galga.py`: el `.kicad_sch` es **identico modulo UUID (0 lineas de diff)**, `galgas.kicad_sym` y **las dos huellas propias byte a byte identicas**, mi `.net` regenerado da **50 redes, las 50 IGUALES** al entregado, y mi `diff_redes.py revC→revD` es **identico linea por linea** al `DIFF_REDES_revC_revD.txt` entregado. **Nadie edito a mano.** `verificar_sch.py` corrido por mi: 108 refs anotadas, 57 componentes con huella, **ERC 0/0 con `Ignored checks: None` en el reporte que genere yo**, netlist sin advertencias, exit 0. `chequear_solapes_sch.py` → 0 solapes.
  - **PASA con evidencia propia:** `/S_MAS` y `/S_MENOS` **identicas nodo por nodo** a la rev C (aparecen en IGUALES de mi propio diff) · **`/E_REFN` NO toca `GND`** — conjuntos disjuntos (6 nodos contra 38), **tercera verificacion, sobre un circuito que cambio entero** · ratiometria de C5 intacta sobre el CJMCU-1220 (sin regulador a bordo) · **tabla de pines del DIP-28: 28/28 correctos** contra netlist y `PINOUT.md` §8, incluido `VBAT_SENSE` en **ADC3/PC3/pin 26** y el strap `PC2` por `RSTRAP1` 1 k · **0 desajustes simbolo↔huella** en los 57 componentes (CJMCU 16 pines/16 pads, RA-02 16/16, supercap 2/2 con pad 1 = "+" y polaridad correcta, MCP1700 en el simbolo **TO-92** con `1 GND / 2 VI / 3 VO`) · el `descr` "PENDIENTE DE MEDIR (M2)" del CJMCU esta bien declarado.
  - **Los 2 defectos que @esquematico dice haber encontrado con el diff: CONFIRMADOS CORREGIDOS.** `/V_SC` ahora tiene 4 nodos (`CI1.1 CSC1.1 RSC1.2 U4.2[VI]`) y la red `Net-(U4-VI)` desaparecio; `RPU1` esta en `Net-(U1-~{CS})` y ya no en MOSI. **Busqueda sistematica de mas redes huerfanas: 3 pruebas independientes (9/9 etiquetas locales resuelven, las 20 redes `Net-(...)` son todas locales legitimas, las 12 redes de un nodo son las 12 `unconnected-*` intencionales) → NO HAY MAS.**
  - **ARBITRAJE (me pidieron las dos cuentas donde @esquematico difiere de @hardware): LE DOY LA RAZON EN LAS DOS, con mi aritmetica.** `RGD1` = **100 k** y no 1 M: el criterio de G5 era de MOSFET y no se traslada al BJT; `1 uA x 1 M = 1,00 V > Vbe` → **habria reintroducido el shunt-cal encendido en el POR, que es el modo de falla que G5 cerro**. `RB_Q1` = **22 k** y no 100 k: rehice Ebers-Moll y reproduce exacto (100 k → `Vce(sat)` 22,29 mV, `Rce` 1300 Ω, **-7,4 µε**; 22 k → 5,35 mV, 312 Ω, **-1,8 µε**). El *"Vce(sat) << 10 mV"* de @hardware es falso: el offset `Vt·ln(1+1/betaR) = 7,44 mV` no tiende a cero, y lo que entra al error es `Vce/Ic`.
  - **HALLAZGO CRITICO — H-3: CONFIRMO QUE EL FUSIBLE ES HOY EL PUNTO UNICO DE FALLA DEL PRODUCTO.** Re-derive C37: `VOUT(min) = 2,811 - 0,120x1,40 = 2,643 V` (cierra al milivolt) → **+643 mV con `BODLEVEL=110`, -257 mV con `BODLEVEL=101`**; con los fusibles bien transmite hasta **OCV 2,56 V**, con los fusibles mal se muere a **OCV 3,46 V, la meseta entera**. Y lo agravo con tres cosas que C37 no tiene: (1) **`EFUSE 0xFD` = `BODLEVEL=101` es exactamente lo que escribe un "burn bootloader" de Arduino**, y el chip virgen viene con `0xFF` = BOD apagado → **de los tres estados alcanzables sin querer, ninguno es el correcto**; (2) la falla es **invisible en el banco** (con pila nueva a 25 °C anda perfecto) y aparece en campo a fin de vida; (3) **`FUSE_LOW/HIGH/EXT_ESPERADO` estan definidos en `config_nodo.h` y NO se leen desde ningun lado — 3 definiciones, 0 usos** (grep sobre todo el repo): el patron del *header huerfano* otra vez, y encima el mismo firmware **si** implementa un chequeo en ejecucion (`mcuStrapCoincide()`) para un riesgo mucho mas barato. → **@firmware: el ATmega puede leer sus propios fusibles desde la seccion de aplicacion (`boot_lock_fuse_bits_get`); comparar contra `FUSE_EXT_ESPERADO` en el arranque y entrar en un estado visible. `D8` ("confirmarlo en el procedimiento de grabado") NO alcanza: un procedimiento no es una barrera. ESCALA A PORTFOLIO.**
  - **H-4 (correccion tecnica al analisis de @esquematico): la fuga del Schottky en la celda invertida va en el sentido contrario al que dice C30.4.** Rehice la malla: la corriente **entra por el terminal negativo** de la celda invertida → **es descarga, no recarga**. O sea que **no es un problema de seguridad de litio**: es que **22 µA contra 21,2 µA de reposo duplican el consumo del nodo** (193 mAh/año) y la autonomia cae a ~1,2 años. **H-5: el caso que SI es recarga —dos celdas bien puestas y desapareadas— nadie lo habia calculado**; mi cuenta da **0,0985 µA a 25 °C y 1,11 µA a 60 °C, 9x debajo del limite de 10 µA → PASA**, pero por modelo y no por hoja, asi que el criterio de P9/C17 obliga a **medir el `IR` del lote de BAT85 a 0,15/0,4 V y a 25/60 °C** (extender `D6`, @hardware). **El bloqueo pila↔supercapacitor y la malla de 367 mA: verificados CERRADOS por topologia.**
  - **BLOQUEAN LAYOUT (2, chicos y especificos):** **H-1** — `SW1` (reed de **vidrio de 14 mm**, dicho en negrita en la propia hoja) lleva huella `R_Axial_DIN0414` de **paso 15,24 mm y cuerpo 11,9 mm** → obliga a doblar los alambres **a 0,62 mm del sello de vidrio**, que es la forma canonica de romperlo y contradice el aviso de fatiga escrito arriba; y **su pendiente de medicion se perdio** (no esta en C40 ni tiene variable, a diferencia de M1/M2). **H-2** — falta la restriccion de **punto-estrella de excitacion** en C39: **esto corrige mi propio G18**, que apuntaba al nodo equivocado — el Kelvin de `REFP1` es **despreciable (58 ppm = 0,06 µε)**, pero que `R1` y `R2` tomen `+3V0` en puntos distintos es **cero corrido diferencial: 0,35 mΩ = 1 µε, 20 mΩ = 57 µε, y 0,22 µε/°C de deriva**, del mismo orden que la EMF termica de L1 que si esta escrita.
  - **BLOQUEAN FABRICAR (ademas de H-3/H-4/H-5):** **H-6** — celda invertida / ausente / agotada dan **cero sintoma**: `V_BAT_SENSE` mide `max(OCV) - Vf` y `J4` no tiene sensado por celda; el tester en la instalacion es la unica barrera y no cubre los dos años siguientes. **H-7** — el checklist *"esperar 60 s y verificar VBAT > 3,0 V"* **da FALSO PASA**: `VBAT` mide `+BATT`, que esta **antes de `RSC1`**, y a t=0 vale 3,19 V con el supercap **vacio** (`0,160 x 15 = 2,4 V` de caida). En la rev C era inocuo; los 15 Ω de la rev D lo rompieron.
  - **No bloquean (9):** H-8 al reposo de C36 le faltan **tres electroliticos de 10 µF** (~1-9 µA = hasta +42 % del presupuesto), `D4` y `RP1` con el reed cerrado · H-9 *"la cadena de medicion no se toco"* es mas ancha que su evidencia (@pcb **no** puede reusar el analogico de la rev C: clamps nuevos, `/SENSE` nueva, Q1 de MOSFET a BJT, seis resistencias de precision a axial) · H-10 el BJT **inyecta ~100 µA de base en `/E_REFN`**, que el MOSFET no hacia (lo cuantifique: 0,19 µε, despreciable, pero sin escribir) · H-11 **el chequeador aviso "texto fuera del area util" y el aviso se ignoro**: el `Value` de `J1` cruza el marco y termina en el borde del papel (lo mire en el PDF renderizado); el aviso no devuelve codigo de error · H-12 los 3 `PWR_FLAG` dejan apagado de hecho el chequeo de riel no alimentado → **`D13` tiene que ser gate permanente, no un pendiente que se cierra hoy** · H-13 la tabla de C30.3 mezcla corriente total con corriente por diodo (139 mV deberian ser 162) · H-14 la re-referenciacion de C34 usa 200 mV donde el documento dice 224, arrastra ese error en la cuenta de pila fresca, y **no declara que `Vf` deriva -50/-70 mV entre 25 y 60 °C (±30 % de la correccion)** · **H-15 cold-start engañoso: `nodo_galga_v3.kicad_pcb`, `gerbers/`, `drc.rpt` y `LAYOUT.md` son rev C SIN MARCAR** al lado de las fuentes rev D — el board que abre KiCad tiene `Q2`/`CSC2`/`D1`, que ya no existen · H-16 `PINOUT.md` §8 sigue diciendo que produccion es TQFP-32 y que `PC2` va directo a masa.
  - **Verificado y adoptado:** los **224 mV** del diodo (re-derive `n·Vt = 34,744 mV` e `Is = 0,0999 µA` desde la Tabla 7 del BAT85; `Vf(par)` a 0,177 mA = 235,8 mV) y la **autonomia 2,43 años** (`4200/0,177 = 2,709 a` brutos, reproduce exacto; la diferencia con los 2,52 de @energia es solo el % de meseta supuesto, y @esquematico es el mas conservador). El hundimiento de TX lo domina la **ESR** (120 de 165 mV) → **`D10` es efectivamente la medicion de mayor palanca del proyecto**.
  - **Gate para liberar LAYOUT:** H-1 + H-2 + H-15. **Gate para liberar FABRICACION:** H-3 (el mas importante) + H-4 + H-5 + H-6 + H-7, ademas de M1/M3 que ya estaban. **No hace falta rehacer** el netlist, el ERC, la anotacion, la ratiometria, las dos huellas propias, el pinout del DIP-28 ni los valores de `RGD1`/`RGD2`/`RB_Q1`/`RSC1`.
  - Dueños: **@firmware (H-3, H-14, H-16)**, @esquematico (H-1, H-2, H-4, H-6, H-9 a H-13, H-15), @hardware (H-1, H-5), @energia (H-7, H-8, H-14), @pcb (H-15). **Nota a favor de @esquematico:** es la primera revision de este proyecto donde el harness encontro defectos electricos que el ERC no ve **y los encontro el autor, no el auditor**; y la deteccion de que el diodo pasa la corriente **media** y no la de reposo es justamente lo que hizo visible H-3.

- **2026-09-04 — DREYFUS/galgas — auditoría rev E.1 del esquemático. ⚠ APROBADO CON OBSERVACIONES, 17 hallazgos.**
  Informe: `C:\Proyectos\galgas\hardware\VERIFICACION_REV_E1.md` (transcrito por el Director: la sesión del
  verificador tenía `Write` deshabilitado y devolvió el informe en su respuesta).
  **Netlist verificado:** cold-start reproduce el `.kicad_sch` byte a byte (0 líneas de diff normalizando UUIDs),
  netlist regenerado idéntico al entregado (59 comp/64 redes, 0 redes distintas), ERC `--severity-all` **0/0 con
  `Ignored checks: None`** corrido por él, `/E_REFN` disjunto de `GND` (4.ª vez), 59/59 símbolo↔huella coherentes
  (el defecto de N patas/M pads **no** reaparece), los 15 `JBxx` con 2 pines en 2 redes y ninguna red de medición
  partida por error.
  **Bloquea fabricar: H-1** — `JP2` no tiene pin de sentido y el firmware no puede saber el modo; galga doble +
  `JP2` en ¼ = lee 50 % **en silencio**; hay 5 pines libres, se decide antes del ácido.
  **Bloquean calibrar/firmware:** H-2 el shunt-cal **no da 1000 µε en ½ puente (da 500)**, contra la propia
  fórmula de la hoja; H-3 `PINOUT.md` se contradice sobre la polaridad de `PD7`; H-4 `DE-7` sin cerrar;
  H-5 `PINOUT.md` no menciona `JP2`.
  **Bloquean el dossier:** H-6 `C41`/`C44`/`C45` citados y **inexistentes** (deuda de @esquematico confirmada);
  H-7 la hoja documenta `D4`/`Q1`/`Q3`/`RB_Q1`/`RGD1`/6 clamps que no existen; H-8 `C33`/`C36`/`L3` obsoletos por
  `CO2`+`CBLK1` borrados en la rev E — y **cortar `JB43` le saca al riel su única reserva (`CR2`)**;
  H-9 `L10` cita `J1.3` donde va `J1.4`.
  **Observaciones:** H-10 `JB15` lleva 8,5 mA, no 0; H-11 `JB11` lleva 8,5 y no 4,25 mA, pero **la conclusión
  "no toca L10" es correcta y la suscribe**; H-12 `JP2` mete 28 µε de cero contra tolerancia de 0,70 mΩ y por
  fretting **salta, no deriva**; H-13 sin clamps ni `D4` se excede el absoluto del ADS1220 a **3,7 V en `J1.4`** y
  en ½ puente el cable llega al riel — **`CLAMPS=True` no cubre `/E_MAS_DOBLE`** y `M16` no puede detectar el modo
  de falla; H-14 `E_REFN` quedó pegado a la malla en la bornera (lo creó la rev E); H-15/H-16/H-17.
  **Proceso: la rev E se ruteó sin auditar; 5 de los 17 hallazgos son de ella.** Regla propuesta y adoptada por el
  Director: **una rev no se congela como baseline de diff hasta que pasó el gate.**

- **2026-09-04 — DREYFUS/galgas — auditoría de la cifra "~50 puentes es el piso" (`LAYOUT.md` §0.4, @pcb). ❌ NO PASA: la cota MST no es una cota inferior y el número no está sostenido.**
  Informe completo devuelto en la respuesta (esta sesión no persiste `.md` de informe; el Director lo transcribe a
  `C:\Proyectos\galgas\hardware\VERIFICACION_PUENTES.md` si lo quiere en repo). No toqué `hardware\kicad\`; todos mis
  scripts corrieron en scratchpad contra el board en modo lectura.
  - **La cota MST NO es válida — refutada empíricamente, no por argumento.** Comparé, par de redes por par de redes,
    lo que `cruces_minimos.py` cuenta como cruce contra lo que el cobre ruteado de la propia placa hace:
    **60 de los 112 pares (54 %) que el MST declara "cruce inevitable" están ruteados HOY sin cruzarse.** Una cota
    inferior que una solución factible viola 60 veces no es una cota. Contraejemplo mínimo citable:
    `Net-(U2-PD4)` × `Net-(U2-PD6)` — la cuerda recta `U2.6(116.19,18.81)→SW1.2(143.71,24.50)` cruza a
    `RL1.2(129.04,24.50)→U2.12(131.43,18.81)` en (130.2,21.6); en el cobre real PD4 baja a la banda sur (y≈27.0) y
    PD6 sube a y≤24.5: **no se cruzan, y costó 3 mm de cobre y cero puentes**. Segundo: `JB13-B` × `JB15-B`, con
    terminales genuinamente entrelazados, resueltos usando dos corredores distintos (x≈222.8 y x≈215.8).
  - **Además la cota no es una cota ni siquiera en su propio sentido:** hay **35 pares que cruzan en el cobre real y
    que el MST NO predijo**. No acota por arriba ni por abajo; no está correlacionada con el resultado.
  - **Tres defectos de método, en el código:** (1) MST ≠ topología de ruteo — `+3V0` (15 pads) tiene un MST serpenteante
    de 256,6 mm que solo genera **45 de los 145 cruces**; el riel real es un Steiner de dos carriles por los bordes
    (y≈1,3 y y≈28,8) y participa en **13 cruces reales**. (2) Las cuerdas rectas atraviesan cuerpos de componentes y
    los keepouts L5, por donde ninguna pista puede ir. (3) **Cruces ≠ puentes**: un alambre que salta un bus de 4 pistas
    es 1 puente y 4 cruces. `cortar_cruces.py` levanta un tramo por cruce sin agrupar — de ahí salen los "48/53".
  - **"Abrir a 40 mm no baja un cruce" es una tautología del instrumento, demostrada:** `cruces_minimos.py` calcula
    `n_cruces` **antes** de leer el contorno (`caja = b.GetBoardEdgesBoundingBox()`, línea 67, se usa **solo en el
    `print`**). Experimento: copié el board a scratchpad, estiré **solo** el contorno de 30 a 40 mm sin mover un pad,
    y el tool devolvió salida **idéntica carácter por carácter** (`277 x 40 mm ... 145 | micro 55 | radio 46 |
    ADC y galga 40 | alimentacion 4`). El tool es ciego al ancho por construcción; no midió nada.
    **Un carril extra a lo largo de la tira vale ~4-8 cruces reales (2-4 puentes)**: en el muro del RA-02 la banda
    norte da ~2 pistas y hacen falta 4 (SCK/MOSI/MISO + el riel `+3V0`, que ocupa el único carril de borde con 1,5 mm).
  - **Reproducibilidad: los números de §0.4 no salen del repo.** Corrí el tool yo: dio **155 a las 21:24 y 145 quince
    minutos después** (@pcb edita en vivo; `nodo_galga_v3.kicad_pcb` mtime 21:31). El board de hoy tiene
    **0 segmentos en F.Cu** — o sea **cero puentes**, no 53 — y **127 cruces geométricos** (DRC: 89 `tracks_crossing`
    + 48 `shorting_items`, 276 violaciones totales).
  - **HALLAZGO CRÍTICO — el ruteo con el que se midió "138 cruces / está en el óptimo" es INFACTIBLE: viola la propia
    regla L5 del proyecto en 59 tramos.** Corrí la geometría exacta de `verificar.py`: **`U1` = 38 tramos de B.Cu bajo
    el cuerpo (L5 admite ≤1)** y **`U3` = 21 (L5 admite 0)**. Ese ruteo "óptimo" pasa por debajo del CJMCU y del RA-02,
    que es justo donde no hay cobre permitido. Un número medido sobre una solución que no cumple las reglas no es ni
    piso ni techo. **Dueño: @pcb.**
  - **Mi estimación a mano, con la topología real: 10-20 puentes (punto medio ~14)**, no ~50. Fundamento: **el bus de
    la placa tiene 6 redes de ancho**, verificado — las únicas que deben atravesar el RA-02 son `+3V0`, `J2-Pin_3`
    (SCK), `J2-Pin_4` (MOSI), `J2-Pin_1` (MISO), `JB22-B` (CS) y `JB23-B` (DRDY); y **atravesando el CJMCU entero hay
    UNA sola red (`+3V0`)**. Reparto: radio 2 · CJMCU+columna `JB1x` 2 · bloque analógico 3-5 · micro (los 3 que cruzan
    el DIP: `JB42-A`, `/VBAT_RET`, RESET) 2-3 · fanout ISP/`JB3x` 2-3 · alimentación 1. Si L5 se aplica a rajatabla y
    los módulos quedan como muros macizos, sube a ~22-28 — **pero entonces el driver es L5 y el ancho de la tira, no el
    pinout**.
  - **Reasignar pines del ATmega: NO conviene ahora, es el tercer término.** El SPI ya está óptimo (PB3/4/5 = pines
    17/18/19, contiguos, fila norte, extremo este mirando al radio). Lo único mejorable es `CS` en PC0/pin 23 y `DRDY`
    en PD2/INT0/pin 4, ambos en el extremo **oeste** y con ~80 mm de viaje: pasarlos a PB2/pin 16 y PB1/pin 15 ahorra
    ~20 mm cada uno, **1-2 puentes**, y cuesta perder INT0 (DRDY pasaría a PCINT, y toca el camino crítico del driver
    del ADS1220). Palancas mucho más baratas, en orden: **(1)** reordenar la columna `JB1x` — hoy está invertida
    (`JB15`@y6,6 viene de `U1.6` que está en la fila **sur**, y `JB13`@y23,4 viene de `U1.10` que está en la fila
    **norte**), lo que fuerza que dos haces se crucen en una franja de 4 mm: **vale ~2 puentes y es gratis**;
    **(2)** sacar el riel `+3V0` de 1,5 mm del único carril del borde norte; **(3)** placement del bloque analógico
    (31 de los 127 cruces reales están en x 220-260); **(4)** el ancho de la tira, si L5 manda.
  - **Deuda de documentación (dueño @pcb):** `pcb/LEEME.md` encabeza **"rev E.1 (placa alargada sobre eje, 218 × 27)"**
    y describe una grilla de 218 × 27, cuando la placa es **rev F, 277 × 30**. Es el patrón de *docs que mienten* otra vez.
  - **Lo que NO cuestiono:** el placement por bloques, la decisión de simple faz, el criterio de `cortar_cruces.py` de no
    levantar jamás las redes de medición ni las de potencia, y que el bloque analógico es el que de verdad tiene cruces
    forzados. **Lo que se cae es el "piso de ~50" y el "no hay margen por ruteo".**
