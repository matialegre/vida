# Dominio: FIRMWARE (agente @firmware)

Doc de dominio + bitácora. El agente lo lee al arrancar y lo actualiza al cerrar.

## Estado del dominio (nacimiento, 2026-07-07)
- galgas-supabase: `ota_wm_pp` v3.6.x, OTA cloud validada (A 0.1.2→0.1.3). PENDIENTE: RX completo (Realtime+LCD+buzzer+gateway HTTP), re-flash B, OTA distinguir A/B.
- RuView: gateway .ino 62KB monolítico (candidato modularizar); nodos MicroPython; benchmark MicroPython vs C/PIO pendiente (decisión datalogger).
- FrioSeguro: v4.0.0 modular (referencia de arquitectura); flasheo de módulos nuevos + SIM800/OTA/NTP en hardware pendiente.
- Lección fundacional: headers huérfanos del legacy — verificar SIEMPRE que el código esté incluido y ejecutándose.

## Versiones flasheadas (tabla viva)
| Placa | Proyecto | Versión | Fecha | Nota |
|---|---|---|---|---|
| ESP-A | galgas-supabase | 0.1.3 (OTA) | pre-2026-07 | cliente nuevo |
| ESP-B | galgas-supabase | vieja | — | RE-FLASHEAR |
| ESP-RX | galgas-supabase | heartbeat-only | — | completar Task 08 |
| ESP32 reefer Cerro Moro (SCZ) | FrioSeguro original | desconocida (pre-revival) | — | 2026-08-05: v2.1.0 revival COMPILADA, flasheo esta noche por USB remoto — anotar acá el resultado |
| ESP32-S3 DevKit "Stitch" | stitch-ainho | 1.0.0 (COMPILADA, no flasheada) | 2026-08-21 | primer flasheo tiene que ser POR CABLE (partición default con OTA); recién ahí sirve el OTA |
| ESP32 DevKit (banco Termovigía) | frioseguro `v3.1` | 3.1.0-p2 **kit_v1** (COMPILADA 66 % min_spiffs en `4cefce7`, post-verificación, NO flasheada) | 2026-09-04 | los 5 de demo van con **KIT v1** (`HW_VARIANTE=HW_KIT_V1`, `RELAY_ON=LOW`). Primer flasheo POR CABLE; nunca OTA sobre 3.0.x. Guía: `hardware/v1_modulos/FLASHEO.md` |
| ESP32 DevKit (variante PCB) | frioseguro `v3.1` | 3.1.0-pN **placa_v2** (COMPILADA 65 %, NO flasheada; no hay placa todavía) | 2026-09-02 | mismo binario base con `-DHW_VARIANTE=2`; `RELAY_ON=HIGH`. **No flashear este bin en un kit v1** |
| Termovigía **Mini** PCB v1 rev A | frioseguro `v3.1` | 3.1.0-pN **mini** (COMPILADA 66 % en `1afde2d`, NO flasheada; **la placa todavía no se fabrica**) | 2026-09-04 | `-DHW_VARIANTE=3`. `RELAY_ON=LOW` (igual que el kit), 2 relés, 4 puertas, 6 sondas, **2 entradas de defrost**. Es la placa del **módulo doble**. Pinout: `hardware/mini/PINOUT_MINI.md` |
| Termovigía **Mini LITE** PCB v1L rev C | frioseguro `v3.1` | 3.1.0-pN **mini_lite** (COMPILADA 66 % en `af71b83`, NO flasheada; **la placa se fabrica con ácido, todavía no existe**) | 2026-09-05 | `-DHW_VARIANTE=4`. `RELAY_ON=LOW`, **2 relés, 5 puertas, hasta 8 sondas, SIN defrost, SIN buzzer, SIN I2C**. **Los GPIO NO coinciden con ninguna otra variante** (una sola cara): 1-Wire IO32, puertas IO33/25/26/27/14, relés IO15+IO13. Es la placa de las **5 demos de Bahía**. Pinout: `hardware/mini_lite/PINOUT_LITE.md` |

## Bitácora

- 2026-09-05 [TERMOVIGÍA v3.1 — **cuarta variante `HW_MINI_LITE`**, la placa que se fabrica PRIMERO con ácido para las 5 demos de Bahía. Las CUATRO variantes COMPILAN; **nada flasheado**] — worktree `C:\Proyectos\frioseguro-v31`, branch `v3.1`, 6 commits: `d8280b0` (gitignore) → `7214e88` (variante) → `4e2b972` (5 puertas + ventana ciega) → `e55699d` (nube + migración) → `251de53` (checker) → `af71b83` (PLAN). `C:\Proyectos\frioseguro` **no se tocó** (solo lectura: `hardware/mini_lite/PINOUT_LITE.md`).
  - **Por qué NO se pudo reusar `HW_MINI` ni `HW_KIT_V1`, y no es una opinión**: @pcb reasignó **todos** los GPIO para rutear en **una sola cara** (la rev B pedía 10 puentes / 580 mm). **IO26 e IO27, que en las otras dos variantes son los RELÉS K1/K2, acá son las PUERTAS 3 y 4**, y el 1-Wire se mudó de IO4 a **IO32**. Con la variante equivocada el equipo **no falla ruidoso**: "aparecen" puertas al accionar un relé y el bus de sondas queda en un pin al aire. Nadie ve nada roto; simplemente no anda. La tabla se copió del bloque `#define` de `PINOUT_LITE.md` §2 (que sale del netlist): **ningún número se reinterpretó**.
  - **Los tres cuidados de @esquematico, en código y con evidencia**: (1) **IO15 (RELAY1) es strapping** — `relaysSafeState()` ya escribía `RELAY_OFF` (=HIGH) **antes** del `pinMode(OUTPUT)`; se verificó, y ahora hay un check que lo exige (el mutante que invierte las dos líneas **muere**). El pull-up externo R15 lo deja HIGH: boot normal **y** relé abierto; el firmware sólo tiene que no arruinarlo. (2) **IO14 (DOOR5) pulsa al arrancar** — ventana ciega de **250 ms para las cinco puertas** (contagiarla a todas es gratis; razonar cuál se contagia, no) + la primera lectura aceptada **siembra** el estado en vez de generar un evento. **Sin esto el equipo mandaría un Telegram de "puerta abierta" en CADA reinicio**, que es justo lo que más hace un equipo con problemas. (3) **5 puertas de punta a punta**: las tres tablas de `door_sensors.h`, el espejo, el buffer offline y la publicación.
  - **El espejo era la mitad que se olvida**: `sensors.h` lee `PIN_DOOR` por su cuenta para `sensorData.doorOpen`, que es lo que va a `readings.door1_open` y a `/api/status`. Si la ventana ciega tapaba el evento pero no el dato, el panel diría "abierta" y el equipo "cerrada" — peor que el bug original. Respeta la misma ventana.
  - **La nube llegaba hasta `door4_open` y `temp6`.** PostgREST ante una columna que no existe contesta **400 y rechaza el body ENTERO**: publicar `door5_open` sin red de seguridad dejaba al equipo sin publicar **ninguna** lectura y el panel lo vería offline. Migración append-only nueva `supabase/migration_mini_lite.sql` (`door5_open`, `temp7`, `temp8`, nullables, **sin default FALSE**: "no sé" no es "cerrada", y un FALSE inventado se grafica y se audita como si fuera una medición — misma lección que F-10) y **dos grupos independientes** de apagado-al-primer-400 (`g_door5Supported`, `g_temp78Supported`), en la línea de F-03: un 400 por `temp7` no puede apagar la quinta puerta.
  - **El caso que el camino en vivo NO cubre**: un equipo que arranque **sin internet** y sin la migración llena el buffer y después **no puede drenarlo nunca** (cada POST da 400 por `door5_open` y la cola queda intacta para siempre), porque el apagado del camino en vivo todavía no ocurrió. `supabaseFlushOfflineBuffer()` ahora lee el cuerpo del 400 y apaga la columna sin perder la cola.
  - **`MAX_PROBES 8` obligaba a `temp7`/`temp8`**: 8 es el límite **eléctrico** del pull-up del bus (`DISENO_LITE` §K3), no un número elegido. Sin esas columnas las sondas 7 y 8 se leían, promediaban y alertaban localmente y **no llegaban a ninguna cámara**: en el panel se verían como si no existieran — la peor forma de perder un dato, en silencio y con la pantalla en verde. Queda escrito en la migración que el trigger `repartir_lectura_por_sonda()` sólo contempla slots 1..6: extenderlo a 8 es de **@backend**.
  - **Tres pines dejaron de ser globales** (un pin es propiedad de la variante, no una constante — misma lección que la pinza vs. el 2º defrost peleándose GPIO32 en la Mini): `PIN_DEFROST_INPUT` cuelga de `HW_HAS_DEFROST_OPTO` porque **en la LITE GPIO33 es DOOR1**; `PIN_BUZZER` cuelga de `HW_HAS_BUZZER` **y con él los 3 sitios que lo escribían** — en la LITE la sirena es **carga del relé** y un pin de buzzer sería una salida que no mueve nada, o sea un feature fantasma (de paso, el kit v1 dejó de escribir un GPIO libre); `PIN_I2C_SDA/SCL` y `PIN_RELAY_3/4` sólo donde hay hardware. Efecto colateral que había que resolver: el aviso de reset de WiFi eran 5 beeps — ahora también parpadea el LED, si no en una placa sin buzzer el usuario no tenía **ninguna** señal de que se disparó.
  - **Candados de compilación nuevos, para las CUATRO variantes**: `static_assert` de que ninguna puerta cae sobre un relé (**el bug del GPIO27** de la rev B: un `INPUT_PULLUP` ahí dejaba K2 pegado) y de que el 1-Wire no comparte pin, más la tabla exacta de la LITE clavada contra `PINOUT_LITE` §2. **Verificado que muerden**: con `PIN_DOOR_5` puesto en 15 el build falla con *"PIN_DOOR_5 pisa otro pin"* y *"Mini LITE: DOOR5 va en IO14"*. Un candado que no se probó no es un candado.
  - **EVIDENCIA (todo corrido, nada razonado)**: compilan las **cuatro** (arduino-cli 1.4.1, core 3.3.8, `min_spiffs`, `--warnings all`): `kit_v1` **1 315 888 B (66 %)** · `placa_v2` **1 317 344 B (66 %)** · `mini` **1 317 472 B (67 %)** · `mini_lite` **1 317 104 B (66 %)**, RAM 57 800 / 57 800 / 57 832 / 57 936 B (17 %); sólo los warnings preexistentes (`device_clock.h:206`, `wifi_utils.h:68`, y `defrostCheckOne` sin usar donde no hay opto = F-16). Nuevo `tools/check_mini_lite_wiring.py` **61/61** y `--mutantes` **7/7 muertos**. Suites intactas: `check_reefer_wiring` 91/91 (+6/6 mutantes) · `check_pgrst400_wiring` 9/0 · `test_sync_budget` 20/0 (+27/27) · `mutantes_v30_equivalencia` 4/4 · offline_buffer 31/31 · net_witness 16/16 · `test_reefer_model` 97/0 · `test_v30_equivalencia` 34/0 · `test_pgrst_error` 16/0 · `test_sensor_fault` 39/0 · las 6 suites python OK · **`lint_device_config.py` → config APTA** y **`scan_secrets.py` → OK, 20 info**. En el **binario**: el `.bin` de `mini_lite` trae `mini_lite`, "una cara" y `door5_open`, y **cero** ocurrencias de `kit_v1`, `placa_v2` ni "Mini PCB v1 rev A"; los otros tres **no** contienen `mini_lite` ni `door5_open`.
  - **NO hecho (DoD)**: **nada flasheado** — la placa todavía no se fabricó. "Compila" no es DoD. Escenarios de banco nuevos en el PLAN, en el orden que hace aparecer primero el error más caro: **B19** medir con osciloscopio el pulso de IO14 al arrancar y confirmar que 250 ms lo cubren (si no, subir `DOOR_BOOT_IGNORE_MS`); **B20** 10 arranques seguidos con cero eventos de puerta espurios y cero Telegram; **B21** los 5 reeds uno por uno contra su bornera (el orden de J3/J4 es lo que más se equivoca al cablear); **B22** los 2 relés con téster **antes** de colgar la sirena. Sigue pendiente todo lo de la entrada anterior (F-09, F-11, F-13, F-15, F-16, F-17, prueba de resistencia, cuelgue provocado, y **rotar los 3 secretos**).
  - **Deuda anotada**: `PIN_I2C_SDA/SCL` eran **defines huérfanos** en las cuatro variantes (cero código `Wire.` en el árbol); ahora sólo existen en `placa_v2`, pero ahí **siguen sin usarse** — o se cablea el OLED o se borran. Y `MAX_PROBES 8` hace que el `delay(750)` bloqueante de la conversión pese más en esta variante que en ninguna: el `setWaitForConversion(false)` pendiente sube de prioridad.

- 2026-09-04 (tarde) [TERMOVIGÍA v3.1 — **cerrados los 3 bloqueantes de campo del informe de @verificador** + 6 hallazgos más. Las TRES variantes COMPILAN al 66 %; **nada flasheado**] — worktree `C:\Proyectos\frioseguro-v31`, branch `v3.1`, 8 commits: `4414302` (F-08 OTA) → `148b6cb` (F-08 secretos) → `e1ce26b` (F-02) → `0af7009` (F-03) → `c487027` (F-01/F-05/F-06) → `00d2b7f` (F-07/F-10/F-14) → `53ae35f` (F-04/F-12) → `4cefce7` (suites). `C:\Proyectos\frioseguro` **no se tocó**. Informe auditado: `firmware_modular/VERIFICACION_V3.1_2026-09-04.md` (ahora commiteado).
  - **F-08 — el linter del propio repo reprobaba lo que se iba a flashear, y era peor que eso**: `OTA_ENABLED` / `OTA_PASSWORD` / `OTA_PORT` eran **`#define` huérfanos** — no los leía ni un archivo (ArduinoOTA nunca existió acá). Poner una password no habría cambiado NADA en runtime: es la lección de los headers huérfanos en versión macro. Ahora están **cableados** al único reflasheo por red que existe de verdad (`POST /update`): sin `secrets.h` la ruta **no se registra** (403 explicando que se reflashea por cable); con `secrets.h`, `?pw=` obligatorio, comparación de tiempo constante, decisión tomada en `UPLOAD_FILE_START` (si no pasa, **no se llama a `Update.begin()`**: ni un byte del binario ajeno toca la partición OTA). El token de Telegram salió de `config.h` al mismo `secrets.h` (los 4 módulos de Cerro Moro alertaban **al mismo chat**). Banner de arranque: `OTA red: SI/NO | Telegram: SI/NO` — un define que no se ve no existe.
  - **F-08 (2) — SECRETOS QUE HAY QUE ROTAR SÍ O SÍ**: el token de **Management API de Supabase** (`sbp_…`, control TOTAL de la cuenta) estaba en 10 scripts de `tests/`, más el **JWT service_role** y el **bot de Telegram**. Salieron del árbol (variables de entorno + `secrets.h`), pero **están en el historial de git**: sacarlos no los invalida. **Rotar los tres es obligatorio, no opcional.**
  - **F-02 — el reset cíclico que se dispara con el enlace VIVO PERO LENTO** (o sea, exactamente Santa Cruz): `supabaseSync()` podía disparar hasta 10 HTTPS **en la misma pasada** (los intervalos 5/10/30/60/60/120 s se alineaban cada 120 s), sin un `esp_task_wdt_reset()` entre medio y con el timeout por defecto de `HTTPClient`. 7 × 5 s = 35 s > WDT 30 s → panic + reset, en bucle cada 2 minutos. Tres candados, y hacen falta los tres: `setTimeout()`+`setConnectTimeout()` explícitos (4000 ms, elegido **por presupuesto, no por medición**: 3 × 4 s = 12 s, menos de la mitad del WDT), tope de 3 peticiones por pasada (el que no entra **no toca su marca de tiempo**: se posterga, no se saltea) y `esp_task_wdt_reset()` después de CADA petición. Más intervalos **desalineados** (67/59/127/5,3/11/31 s, primos entre sí: el mcm se va a horas) y `net_witness.h` alimentando el WDT entre sus dos testigos.
  - **F-03 — un 400 ajeno apagaba el reefer B y después no publicaba temperatura NUNCA MÁS**: el apagado-al-primer-400 asumía que todo 400 era por las columnas nuevas. Con un 400 de otra causa (valor fuera de `DECIMAL(5,2)`, RLS, otra columna): apagaba el reefer B por diagnóstico equivocado, gastaba el único reintento, y desde ahí **todas** las lecturas fallaban, en silencio. Ahora se lee el cuerpo del 400 (**antes** de `http.end()`, si no ya no está) y se apaga **sólo la columna que PostgREST nombró** (PGRST204 la trae entrecomillada); si no nombra ninguna nuestra, **no se apaga nada** y se loguea el cuerpo entero. `readings` pasó a tener **dos** grupos independientes (`temp5/6` y `reefer_b_*`). Header nuevo **puro**: `pgrst_error.h`.
  - **F-01 — "cero regresión" no estaba probado, y ahora sí**: el test de 500 ticks compara `rmAggregate/rmEvaluate` contra sí mismos con el flag `dual` cambiado — consistencia interna, no no-regresión (dos mutantes reales lo atravesaban). Nuevo `tools/test_v30_equivalencia.cpp`: **oráculo independiente**, la conducta de v3.0 en modo simple reescrita a mano desde `git show 6a90954`, sin llamar una línea del código nuevo, comparando tick a tick `valid`/`tempAvg`/alerta/`acknowledged`/segundos sobre el crítico. **34 checks** y `mutantes_v30_equivalencia.py` mata **4/4** — incluidos los dos que sobrevivían. El mutante del `acknowledged` sólo se ve **sin alerta activa** (con alerta activa `rmAlertClear()` también lo borra): el escenario real es el comerciante que silencia mientras la temperatura sube y después baja sola — si el ack queda pegado, **el episodio siguiente no alerta nunca**.
  - **F-05 y F-06 — RESTAURADO el comportamiento v3.0** (era lo preferido). La validez de una sonda vuelve a ser **pegajosa** (una lectura mala no la tumba sola; la baja el debounce de 3): un solo 85 °C del power-on-reset del DS18B20 —el que provoca el arranque del compresor— hacía parpadear `/api/status`. Y en modo simple **sólo las 2 primeras sondas** entran al promedio (`RM_SIMPLE_PROBES`), como v3.0, que leía `getTempCByIndex(0)` y `(1)` y nada más; las 3..6 se siguen leyendo y publicando. La regla de validez se mudó a dos funciones **puras** de `reefer_model.h` (`rmProbeReading`/`rmProbeFaulted`) para que sea **la misma** en el firmware y en el test — antes vivía suelta en `probes.h` y el test no podía tocarla.
  - **Las DOS diferencias que quedan están DECLARADAS, con sus dos números y su test** (no son descuidos): (1) con la sonda 1 cortada y la 2 sana, v3.0 quedaba **ciego** y dejaba de alertar (su `valid` colgaba sólo de la sonda 1) y v3.1 sigue midiendo; (2) un 85 °C aislado en la **sonda 2**: v3.0 la sacaba del promedio en el acto (−20,0 °C) y v3.1 conserva su último valor bueno (−15,0 °C) — v3.0 trataba **distinto** a las dos sondas y un parpadeo hacía saltar el `temp_avg` publicado. Escritas en `docs/modulo-doble.md`.
  - **F-07 / F-10 / F-14 / F-04 / F-12**: `/api/probes/forget` era **GET, sin auth y con CORS `*`** — un `<img src="http://192.168.x.x/api/probes/forget">` en cualquier página abierta desde el WiFi del comercio **borraba la configuración de sondas**; ahora es POST + `confirm=<últimos 4 del device_id>` (el resto de la web local **no tiene auth**: queda como deuda escrita). `reefer_b_temp_avg` publicaba **0** sin lectura — 0 °C en un congelado es plausible y alarmante: ahora `null`. `serial_api.h` **borrado** (huérfano y con la conducta v3.0 adentro: si alguien lo incluía, `alertsMirrorState()` le pisaba `state.defrostMode` una vez por segundo y el defrost por serie quedaba roto **en silencio**). F-04: `g_probeDropped` era **otro huérfano** (se escribía, no lo leía nadie) → ahora hay aviso real de **sonda nueva/cambiada** por Telegram+Supabase, las bajas se detectan **por sonda** y no por el conteo (un empalme rápido iba de 6 a 6 y no levantaba nada), y existe `POST /api/probes/forget?rom=…` para borrar **una** (antes, a la 6ª sustitución el equipo quedaba lleno de fantasmas y la sonda nueva se descartaba en silencio). F-12: `probesForget()` con el bus vacío dejaba la NVS en `ver=0` → **el módulo doble se apagaba solo** al reiniciar.
  - **EVIDENCIA (todo corrido)**: compilan las **tres variantes** (arduino-cli 1.4.1, core 3.3.8, `min_spiffs`, `--warnings all`): `kit_v1` **1 315 256 B (66 %)** · `placa_v2` **1 316 684 B** · `mini` **1 316 800 B**, RAM 57 800/57 832 B (17 %); sólo los warnings preexistentes. Tests: `test_reefer_model` 97/0 · `test_v30_equivalencia` **34/0** · `test_pgrst_error` **16/0** · `test_sensor_fault` 39/0 · `test_offline_buffer` OK · `test_net_witness` OK. Checkers: `check_reefer_wiring` **91/91** (+`--mutants` 6/6) · `check_pgrst400_wiring` **9/0** (+3/3 mutantes) · `test_sync_budget` **17/0** (+`--mutantes` 20/0: el mutante v3.1-p1 da **40 000 ms** de pasada contra un WDT de 30 000) · `mutantes_v30_equivalencia` **4/4** · offline_buffer 31/31 · net_witness 16/16 · las 6 suites python OK. **Los dos linters que reprobaban ahora PASAN**: `lint_device_config.py` → *config APTA* (exit 0, era 1 error) y `scan_secrets.py` → *OK, 20 info* (exit 0, era 11 critical + 3 high). Verificado en el **binario** de la Mini: trae `mini`, el 403 de OTA deshabilitada, "SONDA CAMBIADA" y el error de `confirm=`, y **cero** ocurrencias de `kit_v1`.
  - **NO hecho (DoD)**: **nada flasheado**. Siguen abiertos, con motivo: **F-09** (`devices.dual_reefer` se crea en la migración y el firmware no la escribe — el panel no distingue un doble de un simple; es una línea, pero toca el contrato con @backend), **F-11** (alerta que se traba encendida si a su reefer le reasignan la última sonda: `rmEvaluate()` sale por `!tempValid` **antes** de poder emitir `RM_CLEAR`; el fix correcto es emitir CLEAR al perder la lectura y hay que decidirlo con @comercial porque cambia cuándo se apaga una sirena), **F-13** (escritura de NVS en cada cambio de presencia: un conector flojo escribe cada 30 s), **F-15** (deriva de doc en el reparto de puertas), **F-16** (`defrostCheckOne` sin usar en kit_v1/placa_v2), **F-17** (notas de variante). Y todo el banco: B13-B18, la prueba de resistencia, el cuelgue provocado que demuestre el watchdog, y **medir si 4000 ms de timeout alcanzan en el enlace real de Cerro Moro** (si no alcanza: subir el timeout Y bajar `SUPABASE_MAX_REQ_PER_PASS` — el producto manda).
  - **Antes de flashear un equipo de cliente**: crear `firmware_modular/secrets.h` desde `secrets.example.h` con **una password por equipo** y el bot del cliente. Sin ese archivo el binario arranca seguro (OTA de red apagada, Telegram apagado) — a propósito.

- 2026-09-04 [TERMOVIGÍA — **variante `HW_MINI`** y **MÓDULO DOBLE (un equipo, dos reefers)**. Las TRES variantes COMPILAN al 66 %; **nada flasheado**] — worktree `C:\Proyectos\frioseguro-v31`, branch `v3.1`: `0304e88` (Mini) → `6a90954` (modelo) → `e21a02c` (cableado) → `1afde2d` (PLAN). `C:\Proyectos\frioseguro` **no se tocó** (solo lectura: `hardware/mini/PINOUT_MINI.md`, `hardware/ALCANCE_1WIRE.md`, `entrega_scz/firmware/sondas.h`, `servidor/sql/`).
  - **La Mini es el kit v1 hecho placa**, y por eso entró como tercera rama del selector `HW_VARIANTE` que ya existía, no como fork: mismos GPIO, misma polaridad (`RELAY_ON=LOW`). Cambia `MAX_RELAYS 2`, `MAX_DOOR_SENSORS 4` (+`PIN_DOOR_4 25`), `MAX_PROBES 6`, `PIN_DEFROST_INPUT_2 32`, y ahora sí hay buzzer y opto de defrost REALES (en el kit `checkDefrostSignal()` salía por arriba porque el pin estaba al aire).
  - **El pin que se peleaban dos módulos**: `PIN_CURRENT_SENSOR 32` **borrado** en la Mini — GPIO32 pasa a ser el 2º defrost. Si el `#define` sobrevivía, la pinza y el defrost del reefer B compartían pin. Ahora cuelga de `HW_HAS_CURRENT_CLAMP` y **no existe** fuera de la placa v2; lo mismo con `PIN_LED_ALERT` (IO15), que la Mini no tiene. Lección repetida: los pines no son constantes globales, son propiedades de la variante.
  - **El LED de "funcionando" no parpadeaba — principio 1 en versión pin.** `PIN_LED_STATUS` se ponía OUTPUT/LOW en `initPins()` y **no se volvía a tocar nunca**: existía en el esquemático, en el `#define` y en el PINOUT (que lo pide explícito: «Es el LATIDO: debe parpadear»), y no hacía nada. Nuevo `latidoLED()` no bloqueante en `loop()`, en el mismo camino que alimenta el watchdog, con patrón por estado (60 ms cada 2 s normal / 250 ms sin WiFi / 100 ms en alerta). **Un LED fijo no informa**: si el loop se cuelga, uno encendido sigue encendido. Un latido se detiene solo — es el único testigo legible desde la puerta de la cámara sin abrir la caja.
  - **EL BUG QUE ARREGLA EL MÓDULO DOBLE, y que nadie tenía anotado**: `state.defrostMode` y `state.cooldownMode` eran **globales**. El descongelamiento del reefer A suspendía las alertas de **TODO el equipo** y al salir arrancaba **30 minutos de cooldown también global**. O sea: con dos cámaras en un equipo, **el reefer B podía irse a 0 °C sin que sonara nada, con el panel en verde**. Y no es un caso raro: el defrost es un ciclo **diario**. Es la peor forma de fallar de un producto que se vende porque avisa — no se ve roto, se ve bien.
  - **Prerequisito ineludible: la identidad de las sondas.** Asignar sondas a reefers por índice habría sido construir sobre el bug que `hardware/ALCANCE_1WIRE.md` §4.2 documenta con nombre propio: `getTempCByIndex(0)/(1)` depende del ORDEN de las ROM del bus, así que al caerse una sonda la siguiente **se muda a su lugar y se reporta con el nombre de la que se fue**. Con un reefer es una etiqueta mal puesta; **con dos es la temperatura de A publicada como la de B**, o sea la alarma en la cámara equivocada. Ahora cada sonda es su ROM de 64 bits, persistida en NVS con esquema versionado. El patrón no se inventó: se **portó de `entrega_scz/firmware/sondas.h`**, que anda en campo en Cerro Moro desde 2026-08 (regla BIBLIOTECA: `C:\Proyectos\biblioteca\LEEME.md` tiene `esp32\` **vacío**, y esto es candidato a cosechar después del banco).
  - **Arquitectura**: `reefer_model.h` es **puro** (sin Arduino, sin OneWire, sin HTTP, sin `millis()`) — decisión, promedio por reefer, motor de alerta, defrost y cooldown; se compila con g++ y **es el mismo archivo que se flashea**, no un espejo. `probes.h` es la capa Arduino (bus, NVS, escaneo, asignación). `alerts.h` pasó a tener **una máquina por reefer**: `highTempSec` y `lastAlertCheck` eran dos `static` de archivo — UN contador para todo el equipo — y ese es literalmente el cambio que hace posible el módulo doble.
  - **Cero regresión, con test que lo exige**: `g_dualMode` vive en NVS y **viene apagado**; en modo simple toda sonda es del reefer A —hasta una asignación vieja que hubiera quedado en NVS se ignora— y `state.*` / `/api/status` / `readings` dicen lo mismo que siempre. Hay una prueba de **500 ticks** que compara el mismo equipo en simple contra el mismo equipo en doble con todo en A y falla si difieren temperatura, evento o contador.
  - **El puente con `chamber_id`** (lo escribí en la migración porque es la parte que se olvida): el firmware **no conoce los UUID de `chambers`** y no tiene por qué — es la estructura comercial del cliente. El panel escribe **las dos** columnas: `chamber_id` (reportes/multi-tenencia) y `reefer` (para que el equipo sepa a quién silenciar sin red). El firmware sólo lee `reefer`. **Gana la nube**: el firmware nunca manda `reefer` en el upsert periódico —pisaría lo que el panel acaba de configurar—, sólo con un PATCH explícito cuando el instalador asigna desde la web local. Así no hay dos escritores peleando la misma celda cada 2 minutos.
  - **La red de seguridad obligatoria, otra vez**: PostgREST ante una columna que no existe contesta **400 y rechaza el body ENTERO**. Los tres campos nuevos (`sensor_probes.reefer`, `readings.temp5/6`+`reefer_b_*`, `defrost_sessions.reefer`) llevan el apagado-al-primer-400 de `hw_variant`. **Sin la migración aplicada no se rompe nada**: el reefer B se sigue vigilando y alertando (Telegram y `alerts` no cambian), se pierde su historial en la nube. Migración append-only nueva: `supabase/migration_reefer_doble.sql`.
  - **Dos huérfanos cazados de paso**: `INTERVAL_DISCOVERY_SCAN_MS` estaba en `config.h` desde v3.0 y **no lo usaba nadie** (ahora es el ciclo de sincronización de sondas); y la cabecera de `door_sensors.h` decía «este módulo NO está integrado aún» y listaba la puerta 4 en **GPIO27** — las dos cosas falsas y peligrosas: el módulo está integrado y GPIO27 es el relé K2 desde la rev B, un `INPUT_PULLUP` ahí deja el relé pegado.
  - **EVIDENCIA (nada razonado, todo corrido)**: `tools/test_reefer_model.cpp` sobre el header REAL → **97 ok, 0 fail** (incluye como regresiones: el defrost de A con B calentándose, las dos sondas de B caídas sin tocar a A, la sonda que se cae y la vecina que NO se muda, el tope de 3 por cámara, el hueco de muestreo de 15 s exactos y de 15,001). `tools/check_reefer_wiring.py` → **85/85 checks de CABLEADO** y `--mutants` → **6/6 mutantes muertos** (entre ellos "el `.ino` no incluye `probes.h`" y "`checkAlerts()` vuelve a mirar un solo reefer"). Suites preexistentes intactas: `sensor_fault` 39/0, `offline_buffer` 1332/0, `net_witness` TODOS OK, sus dos checkers OK. **Compilan las tres variantes** (arduino-cli 1.4.1, core 3.3.8, `min_spiffs`, `--warnings all`): `kit_v1` **1 312 696 B (66 %)** · `placa_v2` **1 314 004 B (66 %)** · `mini` **1 314 104 B (66 %)**, RAM 57 784 B (17 %); sólo los 2 warnings preexistentes (`device_clock.h:206`, `wifi_utils.h:68`). Verificado en el **binario**: `strings` del `.bin` de la Mini trae `mini`, `/api/probes`, `/api/reefers`, `reefer_b_temp_avg` y los 3 avisos de migración faltante, y **no** contiene `kit_v1` ni `placa_v2`.
  - **NO hecho (DoD)**: **nada flasheado** — la placa Mini no está fabricada. Los 6 escenarios de banco quedaron escritos en el PLAN como **B13-B18**; el que justifica el módulo entero es **B14**: puentear GPIO32 a masa con el equipo en modo doble y calentar la sonda del reefer A → **A tiene que alertar igual**. También: B13 (desenchufar una de dos sondas y ver que la otra NO cambia de nombre), B15 (correr sin la migración y ver que el heartbeat no se cae), B16 (que la NVS de sondas sobreviva un reflasheo), B17 (6 sondas convertidas en 750 ms, no 4,5 s).
  - **Sobre la estimación**: @comercial dio **10 h** y **no alcanzó**, con motivo concreto — el módulo doble es imposible sin pasar la identidad de las sondas **de índice a ROM**, y eso reescribe `sensors.h` entero y arrastra el aviso de sonda caída, el espejo hacia `sensorData`, el buffer offline y la publicación. Sacando esa refactorización, "dos reefers" sí son ~10 h; con ella adentro, **16-18 h**. Y había que hacerla igual: es deuda anotada en `ALCANCE_1WIRE` §4.2 desde el 2026-09-03. Queda escrito en el PLAN §6.
  - **Deuda nueva, no tocada**: `probesRead()` conserva el `delay(750)` de la conversión heredado de `readSensors()` — bloquea el `loop()` 750 ms cada 2 s (el WDT de 30 s lo tolera de sobra, pero es lo próximo a sacar de ahí con `setWaitForConversion(false)`). Preexistente sin resolver: anon key y token de Telegram en `config.h`; `serial_api.h` sigue huérfano y ahora además escribe `state.defrostMode`, que pasó a ser un **espejo** (si algún día se incluye, no haría nada) — está en la lista de borrado del PLAN §5.3.
  - **Próximo paso**: cuando llegue la placa Mini, flashear POR CABLE con `HW_VARIANTE=HW_MINI` y correr B13-B18 en ese orden (B14 primero de los que importan). Doc completa: `docs/modulo-doble.md`.

- 2026-09-03 [NODO GIMAP — arranque en frío: 3 defectos del cableado PC↔nodo corregidos. NADA flasheado (no había placa); worktree `C:\Proyectos\datalogger-gimap`, branch `nodo-gimap/wifi-y-flasheo-2026-08-24`, **commiteado en `3491b72`** al cierre de la sesión] — Los tres venían de una auditoría de arranque en frío, y los tres son del tipo "el código existe pero el efecto no ocurre" (principio 1 en versión herramientas de PC).
  - **El callejón sin salida de `wifi.json`** (el serio): `firmwares/nodo-gimap/red.py:64` hace `_cargar_guardado() or _cargar_secrets()` — la red cargada por el AP de rescate **gana sobre `secrets.py` y no la borra nadie**. Secuencia real de mañana: el nodo no engancha → AP → Matías carga una red desde el celu → a partir de ahí `configurar_wifi.py` + reflasheo **no cambian nada**, el nodo sigue en la red vieja y el visor no ve nada. Se resolvió del lado del **flasheo**, no del firmware (no se toca el nodo la noche antes de usarlo): `flashear_nodo.py` tiene ahora `limpiar_restos()` — si hay `secrets.py`, borra `wifi.json` (el usuario acaba de declarar qué red quiere: esa gana); si NO hay, lo **deja** (sería sacarle la única red que tiene). Avisa en castellano en las tres ramas.
  - **Los `.bak` también se borran** (la auditoría lo daba como menor; decidí que va): son copias de un OTA anterior, o sea **más viejas que lo que se acaba de copiar por cable**. Si el firmware nuevo no llegara a operativo 3 veces, `boot.py:34-57` las restauraría **encima** y desharía el flasheo en silencio, dejando una versión anterior a la que Matías cree tener. El flasheador ya ponía en cero `_arranques_malos` por este mismo miedo, pero eso solo **posterga** el problema: borrar la munición lo cierra. Con el USB en la mano el cable ES el rescate; el próximo OTA crea `.bak` nuevos.
  - **El firewall que decía "ok, regla creada" sin crearla**: `ARRANCAR_GIMAP.bat:57` pasaba los argumentos sueltos a `Start-Process -ArgumentList`. **PowerShell 5.1 no entrecomilla los ítems**, así que netsh recibía `name` / `Nodo` / `GIMAP` / `UDP` como argumentos separados → "Un valor especificado no es válido": salía el UAC, el usuario aceptaba, y no se creaba nada. En esta PC no se veía porque la regla ya estaba de antes. Ahora eleva el `abrir_firewall_gimap.bat` que ya existía (un solo argumento, `'auto'`, que además le saltea el `pause`) y que trae las comillas puestas; se le agregó un `delete` previo al `add` para que el paso sea repetible (esta PC tenía la regla **duplicada**, resto de los intentos fallidos).
  - **Evidencia** (todo corrido, no razonado): sonda con un `.bat` que imprime `%1..%9` → la forma vieja entrega `ARG=[advfirewall] ARG=[firewall] ARG=[add] ARG=[rule] ARG=[name] ARG=[Nodo] ARG=[GIMAP] ARG=[UDP]` y la nueva `ARG=[auto]`; después se corrió el camino elevado **de verdad** y `netsh advfirewall firewall show rule name="Nodo GIMAP UDP"` devuelve **una sola** regla (antes dos), UDP, Dentro, Permitir, LocalPort **50507-50508** — la regla queda puesta para mañana. Prompt de flasheo: 7 respuestas probadas (`no`/`NO`/`n`/`N`/` no ` → salteado; `s`/enter → flashea). Espera del navegador: el lazo abre a los **3,2 s**, justo después de que el puerto empezó a escuchar a los 3,0 s (antes abría el navegador **antes** del `python visor_gimap.py` → "no se puede acceder al sitio" en el primer intento). `limpiar_restos()` probado con un Pico simulado, 3/3 ramas. `py_compile` OK.
  - **El "~30 s" era mentira**: peor caso real de `red.py:19-20` = 3 intentos × 25 s = **75 s** + sensores + los 200 promedios de la línea de base de los piezos (`sensores.py:82`). El `.bat` ahora dice "entre 15 s y ~90 s". Un número optimista hace que Matías dé por muerto un nodo que está por conectar.
  - **NO tocado** (por regla del encargo): `visor_gimap.py`, `configurar_wifi.py`, `MANANA.md`, `firmwares/nodo-gimap/red.py`. **Deuda que queda viva**: `red.py` sigue prefiriendo `wifi.json` sobre `secrets.py` — el flasheo lo tapa, pero un nodo que pasó por el AP y después se reprograma por **OTA** (sin cable) mantiene el problema. Cuando haya banco: invertir la prioridad o que `main.py` borre `wifi.json` al conectar bien por `secrets.py`. Biblioteca: nada nuevo para cosechar (el `flashear_pico.py` de `C:\Proyectos\biblioteca\pc\` sigue en "solo sintaxis"; cuando este flasheador corra contra la placa real, la limpieza de restos es candidata a subir a esa ficha).
  - **[registrado por @cronista]** — lo que esta entrada no decía y define el estado real: **NADA de este nodo está verificado contra hardware.** Sin comprobar: que el MPU responda en **0x68**, que el piezo capte un golpe (**`probar_piezo.py` nunca se corrió**), la alimentación por VSYS, el alcance WiFi y el OTA. Además, **el nodo se murió una vez a los 276 s de uptime**, causa sin determinar (sospecha en la lectura VSYS/WiFi, tocada en v1.0.5 **pero no probada en placa**). Y el piezo mide sucio **por hardware**: 50 Hz de red con GP27 flotando (2,05 V de zumbido) y la ganancia del MCP6004 desajustada (p1 0,847 V / p2 2,645 V contra el riel; objetivo ~1,65 V) — se corrige con el preset multivuelta mirando `ajustar_ganancia.py`, con la placa en la mano. El firmware que se usa es el **v1.0.5 del branch, no el que documenta el README** del repo.

- 2026-09-02 (tarde) [TERMOVIGÍA — **una base de código, dos hardware**: selector `HW_VARIANTE`; las DOS variantes COMPILAN, nada flasheado] — Cambio de plan de Matías: los 5 equipos de demo se arman con **módulos cableados** (`C:\Proyectos\frioseguro\hardware\v1_modulos\`, PINOUT_V1 + CABLEADO de @esquematico con ERC 0/0), no con la PCB v2 que todavía no se fabrica. Worktree `C:\Proyectos\frioseguro-v31`, branch `v3.1`: `b468653` (selector) → `4b65912` (boot seguro) → `74631a3` (publicación de la variante) → `f237f44` (PLAN). No se tocó `C:\Proyectos\frioseguro` salvo para **crear** `hardware/v1_modulos/FLASHEO.md` (esa carpeta está untracked entera, igual que el resto del trabajo de @esquematico).
  - **La decisión de fondo: no bifurcar.** @esquematico eligió para el kit los MISMOS GPIO que la PCB (K1=26, K2=27, K3=18, K4=23; 1-Wire en 4; puertas en 5/13/14), así que **lo único incompatible entre los dos armados era una constante**. Un `#define HW_VARIANTE` (`HW_KIT_V1` / `HW_PLACA_V2`) en `config.h` deriva `RELAY_ON`/`RELAY_OFF` + `HW_HAS_LTE / BATTERY / LVD / MAINS_SENSE / CURRENT_CLAMP / DEFROST_OPTO / GEN_OPTO / ATS / BUZZER` + `HW_VARIANT_NAME`/`_DESC`. Se puede pisar sin editar el archivo (`--build-property compiler.cpp.extra_flags=-DHW_VARIANTE=2`), que es como se compila la segunda variante.
  - **Por qué la polaridad no es un detalle**: el kit ataca módulos optoacoplados **directo** (`RELAY_ON = LOW`); la v2 tiene BC547 que **invierten** (`RELAY_ON = HIGH`). El binario cruzado deja los **cuatro relés pegados apenas arranca**: sirena sonando y, en la v2, transferencia conmutada a grupo. Es el único error de la lista que se escucha desde la calle (CABLEADO §0).
  - **Boot seguro, y la discrepancia que NO se resuelve por firmware**: `BOM_KIT_V1.md` §2.5 (@hardware) dice que con `IN` al aire el módulo cliquea y queda pegado; `CABLEADO.md` §4 (@esquematico) dice que no, porque el LED del opto va de VCC a IN y sin camino a masa no enciende. **El firmware no puede depender de cuál tenga razón**: si es @hardware, cada milisegundo entre el reset y el `setup()` es sirena. Por eso `relaysSafeState()` (`digitalWrite(RELAY_OFF)` y RECIÉN AHÍ `pinMode(OUTPUT)`, con la polaridad de la variante) es ahora la **primera línea de `setup()`, antes incluso de `Serial.begin()`**; `initPins()` la repite. Queda comentado en el código y en FLASHEO §5.1 que **los ~300 ms de bootloader no los cubre ningún firmware**: si la medición confirma el clic, la solución es un 10k de IN a VCC, o sea hardware. Más `static_assert(RELAY_ON != RELAY_OFF)`.
  - **Entradas que no existen no se leen**: en el kit no se toca **GPIO12** (strapping MTDI: en alto la flash se configura a 1,8 V y la placa no bootea — y además no hay módem), ni GPIO33/GPIO36 (no hay optos), y `checkDefrostSignal()` sale por arriba. Esto último es lo que más podría haber mordido en la demo: **una entrada flotando leída como señal de defrost dispara el cooldown, que silencia 30 minutos las alertas de temperatura**. El defrost del kit se fuerza a mano por `/api/defrost`, que sigue cableado.
  - **La variante se puede saber a distancia** (era el pedido, y es lo que evita el diagnóstico al revés: "no tiene batería" y "se le murió la batería" se ven igual en el panel): banner de arranque `HW: kit_v1 (...)` + `Reles: RELAY_ON=LOW | K1=26 K2=27 K3=18 K4=23`; `devices.hw_variant` en el alta y en **cada heartbeat**; `/api/status` con `hw_variant`/`hw_desc`/`relay_on_level`/`has_lte`/`has_battery`. **Red de seguridad obligatoria y poco obvia**: PostgREST, ante una columna que no existe, contesta **400 y rechaza el body entero** — agregar un campo informativo sin esto habría matado el heartbeat y el panel vería los 5 equipos offline. Al primer 400 se apaga el campo, avisa una vez por serie y reintenta sin él. Migración append-only nueva: `supabase/migration_hw_variant.sql`.
  - **Lo que ya estaba y verifiqué en vez de rehacer** (el PINOUT_V1 §2 de @esquematico compara contra `main` v3.0.1, no contra `v3.1`): `MAX_RELAYS 4`, K3=18, K4=23 y el DHT eliminado **ya estaban desde `98afbf7`**; sólo agregué los alias `PIN_RELAY_1..4` por número de canal. `grep -rn -i dht` sobre `*.h`/`*.ino` devuelve hoy **sólo 2 líneas, las dos comentarios de `config.h`** que explican por qué NO se agrega `SENSOR_DHT22_ENABLED false`: no queda código de ese sensor al que habilitar y el define sólo volvería a poner el nombre en el mapa. Es la única desviación consciente respecto del encargo, y está escrita en el archivo.
  - **Compilación (evidencia)**: arduino-cli 1.4.1, core esp32 3.3.8, `esp32:esp32:esp32:PartitionScheme=min_spiffs`, `--warnings all`, `--clean`. **`kit_v1`: 1 296 396 B = 65 % de flash, RAM 57 416 B (17 %)** · **`placa_v2`: 1 297 468 B = 65 %, RAM 57 424 B (17 %)**. Cero warnings propios nuevos (siguen los 2 preexistentes: `device_clock.h:206` `%u` vs `uint32_t`, `wifi_utils.h:68` `wifi_power_t`). Cableado verificado en el **binario**, no en el fuente (principio 1): `strings` sobre cada `.bin` muestra `kit_v1` + "Kit v1 - modulos cableados - RELES ACTIVO-BAJO" en uno y `placa_v2` + "Termovigia Base v2 rev B.1 - RELES ACTIVO-ALTO (BC547)" en el otro, **y ninguno contiene la cadena del otro**; también aparecen `hw_variant` y el aviso de migración faltante.
  - **Guía de flasheo** nueva: `C:\Proyectos\frioseguro\hardware\v1_modulos\FLASHEO.md` (169 líneas) — placa "ESP32 Dev Module", `min_spiffs` (por qué: la partición default deja el OTA sin margen, y NVS queda en el mismo offset así que el WiFi guardado sobrevive), comandos exactos de compile/upload/monitor, el bloque de banner esperado con la línea `HW: kit_v1` señalada, qué avisos son normales (`hw_variant no existe todavia`) y cuáles son falla (`BROWNOUT` repetido, `TASK_WDT` sin provocarlo), y la prueba de banco en el orden que hace que el error más caro aparezca primero: **relés al enchufar → hielo → imán → relés responden → etiqueta → bitácora**.
  - **NO hecho (DoD)**: **nada flasheado** — "compila" no es DoD y esto es sólo compila. Falta: prueba de resistencia con cortes de WiFi/energía, cuelgue provocado que demuestre el watchdog, medición del riel de 5 V con osciloscopio conmutando los 4 relés con el WiFi transmitiendo (DoD compartido con @hardware), y la medición de 30 s que cierra la discrepancia del clic con IN al aire. Sigue pendiente todo §5.3 del plan. Preexistente sin resolver: anon key y token de Telegram en `config.h`.
  - **Próximo paso**: Matías flashea UNA DevKit por cable con `PLACA_NUM=1` + `HW_VARIANTE=HW_KIT_V1`, corre FLASHEO §4 y §5 completas, anota el resultado del §5.1 en `CABLEADO.md` §4 (queda pedido ahí) y la versión en la tabla de arriba. Recién con ese equipo verde se replican los otros 4.

- 2026-09-02 [TERMOVIGÍA v3.1 — 5 merges, config rev B, watchdog + boot seguro; COMPILA, NO flasheado] — worktree `C:\Proyectos\frioseguro-v31`, branch `v3.1` (nace de `main 4b632fa`). Sesión continuada (la anterior se cortó por límite de API con 4 merges hechos). HEAD `v3.1`: `ac46c15`→`4b5ee2e`→`809c824`→`6434393` (merges 1-4, instancia anterior) → `865683b` (merge 5, `08-29-la-sonda-que-se-cae`, limpio, 0 conflictos) → `98afbf7` (config.h pinout rev B/B.1, sin DHT, sin SIM800) → `14d3200` (esp_task_wdt 30 s + boot seguro) → docs. Evidencia completa en `C:\Proyectos\frioseguro-v31\firmware_modular\V3.1_MERGE_LOG.md`.
  - **Config**: K1 26 / K2 27 / K3 18 (arranque grupo, era el DHT22) / K4 23 (ATS), `RELAY_ON HIGH`, `MAX_RELAYS 4`, `MODEM_PWR_EN 12` (LOW en boot; reinicio = CPOF, `Serial2.end()`, LOW ≥ 10 s, HIGH, 12 s, sondear AT), LTE 16/17, PWRKEY 25 sin uso, sin LTE_RST/STATUS, MAINS 34 (ADC ×9,33), VBAT 35 (×5,55), **VBATT_RAW 39** (rev B.1), CT 32, DEFROST_N 33 y GEN_RUN_N 36 activos en bajo. `grep -rn -i dht firmware_modular` = 0; `SIM800_*` = 0; `sim800.h` fuera de la compilación (`HAS_SIM800 false` fijo hasta borrar el código muerto, plan §5.3). `FIRMWARE_VERSION 3.1.0-pN`.
  - **Dos bugs que el hardware nuevo habría convertido en relé pegado**: (1) `door_sensors.h` tenía una "puerta 4" en GPIO27 = K2 rev B activo en alto → `INPUT_PULLUP` = K2 ON; eliminada (`MAX_DOORS 3`). (2) Al achicar el array, `supabase.h:361` leía `doors[3]` fuera de rango — lo atrapó `--warnings all` (`-Warray-bounds`). Siempre compilar con warnings.
  - **Watchdog/boot seguro (§1.9)**: `esp_task_wdt_reconfigure/init` 30 s + panic, `add` en setup, `reset` primera línea de `loop()`; portal WiFiManager desuscribe (`delete`/`add`), descarga OTA y esperas de reconexión alimentan. `setup()` = Serial → `initPins()` (RELAY_OFF escrito ANTES de `pinMode(OUTPUT)` en K1..K4, GPIO12 LOW) → `setupWatchdog()` → `loadConfig()` → WiFi. Banner imprime `esp_reset_reason()` (evidencia para la prueba de cuelgue). Biblioteca `esp32/` vacía: patrón tomado del revival; candidato a cosechar tras el banco.
  - **Compilación** (arduino-cli 1.4.1, core 3.3.8, `PartitionScheme=min_spiffs`, `--warnings all`): exit 0, **1 295 652 B = 65 % de flash**, RAM 57 424 B (17 %); solo warnings preexistentes (`device_clock.h:206`, `wifi_utils.h:68`). Tests offline: 6 suites python OK, g++ `test_offline_buffer` 1332/0 (BufferedSample 28→20 B), `test_sensor_fault` 39/0, `test_net_witness` OK, checkers 31/31 y 16/16.
  - **NO hecho (DoD)**: nada flasheado (el primer v3.1 va POR CABLE: min_spiffs + relés invertidos — un OTA sobre 3.0.x encendería K1..K4 al arrancar el viejo); el WDT no demostró recuperar un cuelgue provocado; sin prueba de resistencia; §5.3 del plan entero (borrar SIM800/serial_api/config_SANTA_CRUZ, perfiles, `cfg_ver`, `ota_rescue.h`, `transport.h`, `power.h`, `lte_a7670.h`, `ats.h`, `notify.h`, `check_v31_wiring.py`). Deuda para @esquematico: `DISENO.md` §2 aún dice IO39 "libre" y módem "LOW 3 s" — tiene que coincidir con `config.h`. Preexistente: anon key + token Telegram siguen en `config.h` (lo marca `test_scan_secrets`).
  - **Próximo paso**: (1) Matías flashea por cable una DevKit con `PLACA_NUM=1` y corre B1 (boot: los 4 relés OFF y GPIO12 LOW medidos con tester) + prueba de cuelgue (`while(1)` por serial → banner `TASK_WDT`); (2) recién después OTA cloud; (3) `lte_a7670.h` (§2) como tarea FreeRTOS propia.

- 2026-08-21 (turno noche) - **FrioSeguro: el uptime daba la vuelta a los 49,7 dias y se publicaba con dos definiciones distintas** (worker nocturno local). Branch `nocturno/local-2026-08-21-b-cuanto-hace-que-esta-prendido` (`53f8f2c`, sale de `main`), cierra **T6** del `08-16-b-cadena-tiempo`.
  - `millis()` es `uint32_t` en el core ESP32: vuelve a cero a los **49,7 dias**. `uptime_sec` se publicaba en **4 sitios** con **2 cuentas distintas**: `millis()/1000` (desde el boot) en `supabase.h:213` y `:288`, y `(millis()-state.uptime)/1000` (desde el **setup**) en el `.ino:300` y `web_api.h:70`. Las dos dan la vuelta -> a los 60 dias el panel informaba 10. Y difieren entre si por lo que tarda el arranque: **la fila de Supabase y la pagina local del propio equipo nunca decian lo mismo**.
  - Ahora `firmware_modular/uptime.h` con `uptimeSec()`/`uptimeMin()` sobre **`esp_timer_get_time()`** (nativo del core, `int64_t` de us desde el boot, no da la vuelta). Se descarto el contador de rollover a mano (`if (now<last) high++`) porque exige ser llamado cada <49,7 dias o pierde una vuelta en silencio. Se **elimino `SystemState.uptime`**, el campo que sostenia la 2a definicion.
  - **Evidencia:** `arduino-cli compile` OK contra el baseline de `main` (1288696 B): **+132 B de flash** (124 son caracteres del JS embebido en `html_ui.h`; el C++ pesa ~+8 B) y **-8 B de RAM**.
  - **Dos avisos para @firmware:** (1) el sketch esta al **98 % del flash** (1288828/1310720) — cualquier cosa que se agregue choca con ese techo; (2) **`serial_api.h` no esta `#include`-ado en ningun lado, no se compila** — es codigo muerto, y el informe del 08-18 apunta a el para forzar defrost en la verificacion de hardware (el camino que si funciona es `web_api.h:221`). O se incluye o se borra.
  - **Falta (banco):** placa flasheada, `curl http://<ip>/api/status` y confirmar que el uptime coincide con la fila de Supabase (antes diferian). Doc: `frioseguro/docs/uptime.md`; informe: `diario/nocturno-local-2026-08-21-b.md`.
- 2026-07-07 — Agente creado por Claude Fable. Próximo paso sugerido: Task 08 del RX (es el bloqueante más grande para octubre).

- 2026-07-08 [BRIEFING GIMAP] — leer ../BRIEFING_EQUIPO_GIMAP.md y los 4 docs (PARTE_GIMAP, PRESUPUESTO_ENERGIA, PROTOCOLO_CALIBRACION, INGENIERIA_NODO_1ANO). Para vos: emisores NO ESP32 (ATmega/STM32L bare-metal, sleep µA real); receptor SÍ ESP32; retomar TX = ventana RX Class A + reed/imán; VERIFICAR sleep con INA219.

- 2026-07-13 [FrioSeguro — COMPILADO para Supabase nuevo, NO flasheado] — arduino-cli 1.4.1, core esp32:esp32 3.3.8, FQBN `esp32:esp32:esp32` (partición default: app 1310720 B). 2 variantes, AMBAS SIN SIM (SIM800 descartado, commit 48ef83f): PLACA_NUM=1 → placa "sin SIM" (flash USB) y PLACA_NUM=2 → placa que porta el SIM800 físico (flash por aire). config.h: PLACA_NUM 4→2 final, DEVICE_NAME "SIM800"→"" (pisaba el nombre de ambos devices). Binarios en `C:\Proyectos\frioseguro\firmware_modular\build_out\` (gitignored): `placa1_nosim_usb/` y `placa2_exsim_ota/` (artefactos completos) + .bin sueltos nombrados. Tamaño: 1288696 B (98% flash — SIN margen, próxima feature no entra), RAM 16%. Verificado con strings: cada bin con su mDNS/versión, URL nueva cjdluhemschrynijzvap, cero rastro del proyecto viejo, Telegram intacto.
  HALLAZGO CRÍTICO (lección headers huérfanos): **ArduinoOTA NO EXISTE en el firmware** (nunca existió — git log -S lo confirma); `OTA_ENABLED/OTA_PORT/OTA_PASSWORD` son defines huérfanos, el doc ARQUITECTURA_2026-07-13.md §6 está equivocado. espota NO sirve ni contra el firmware viejo ni el nuevo. OTA real que SÍ está cableada: HTTP `POST /update` puerto 80 (web_api.h:417, multipart, sin auth) + OTA cloud Supabase (supabase.h:625). Flasheo por aire = `curl -F "update=@<bin>" http://<IP>/update`. Otros huérfanos: serial_api.h, current_sensor.h, power_monitor.h (nadie los incluye). Linter pre-flash: NO apta para CLIENTE por OTA abierta sin password (deuda ya registrada, para el banco OK). PENDIENTE: flashear (Matías/Director), agregar ArduinoOTA o auth al /update antes del primer cliente, resolver el 98% de flash (min_spiffs en placas con acceso USB).

- 2026-07-28 [drive-torno-esp32s3 — UI de seguridad cableada, COMPILA, NO flasheado] — `C:\Proyectos\drive-torno-esp32s3\drive_torno\drive_torno.ino`. Caso de libro del principio 1 al revés: las APIs de la máquina de estados de seguridad (`/api/seguridad`, `/api/campo`, `/api/parada`, `/api/log`) existían en el firmware pero NINGÚN consumidor en el `PROGMEM PAGINA[]` → features fantasma desde la web (había que escribir URLs a mano). Agregado SOLO en la página + su JS (cero cambios en la tarea de control, la lógica de seguridad ni las APIs): tarjeta ESTADO DE SEGURIDAD con semáforo 0/1/2/3 + motivo de bloqueo traducido a castellano + chips campo/FAULT/perilla; control real|off de la detección de campo con cartel rojo permanente cuando queda en `off` (la trampa de auditoría: `off` + 220 V = motor embalado sin que el firmware se entere) + confirm antes de desactivarla; botón PARADA ORDENADA separado del APAGAR duro, con progreso 0/1/2; visor del log de eventos (últimos arriba, color por tipo H/O/T/R/P/A/C). Refresco integrado al `ciclo()` serializado ya existente (`refrescarSeg()` cada 500 ms, `refrescarLog()` 1 de cada 6 ≈ 3 s) — NO se agregó polling paralelo. Compila `arduino-cli compile --fqbn esp32:esp32:esp32 --warnings all`: **1138386 B (86% flash)**, RAM 17%. Único warning: `-Wvolatile` preexistente en `isrEncoder()` (línea 202, `encoderPulsos++` sobre volatile, deprecación C++20, no funcional — NO tocado por regla de no meter mano en el lazo). PENDIENTE: flashear en la placa real y validar en banco la secuencia completa (rechazo por perilla no en cero, campo off → cartel, parada ordenada 1→2, log como evidencia).

- 2026-07-30 [laser-pcb — base Marlin lista, BLOQUEADO por modelo de impresora] — Misión: dejar todo listo para que al llegar la impresora sea "elegir pines y compilar". HECHO: (1) `C:\Proyectos\laser-pcb\firmware\Marlin\` = clone shallow tag **2.1.2.8** (última estable, 2026-06-25; verificado Version.h) — 50 MB, aún sin git repo padre. (2) `firmware\MARLIN_LASER.md`: doc completa con refs archivo:línea verificadas por grep contra ese árbol — hallazgos clave: SPINDLE_FEATURE y LASER_FEATURE **mutuamente excluyentes** (SanityCheck.h:3563) → decisión: UN firmware con LASER_FEATURE inline (M3 I) + Dremel 775 en el MOSFET del fan vía M106/M107 (opción A, sin re-flasheo por cambio de herramienta); G0 fuerza potencia 0 con LASER_FEATURE (gcode.cpp:243-250) → regla CAM: travels=G0; Marlin **NO tiene G81** (case 81 = M81) → taladrado con ciclos Z manuales; SPINDLE_LASER_FREQUENCY solo aplica en AVR/ESP32/LPC, en STM32 medir en banco; USE_WATCHDOG default ON (Conf_adv:2053); LASER_SAFETY_TIMEOUT_MS 1000. Pines por placa (del fuente): RAMPS autoasigna D6/D4/D5; Creality V4 fan PA0 es SOFT-PWM forzado (inútil p/láser) → TTL en header BLTouch PB0/PB1 (TIM3, HW PWM); SKR Mini E3 V2 FAN0=PA8 / V3 FAN0=PC6, láser en SERVO PA1; MKS Robin E3 SERVO PA3; Melzi = peor caso (sin pines libres, plan B: M106+LASER_SYNCHRONOUS_M106_M107 o cambiar placa). (3) `firmware\configs\`: README + Configuration.patch.h + Configuration_adv.patch.h (bloque láser resuelto, SPEED_POWER_STARTUP bajado 80→10 por seguridad, LASER_POWER_TRAP on, todo lo dependiente del modelo con `// TODO-IMPRESORA`). (4) `firmware\GCODE_EJEMPLOS.md`: cuadrado 10 mm S25 inline, matriz 3x2 de taladrado con spin-up manual (G4 S2), procedimiento de medición de offset láser↔mecha (cruz + M114) y convención de ceros Z. PENDIENTE (bloqueado): modelo/foto de placa, 8 datos listados en MARLIN_LASER.md §7. Nada flasheado (no hay hardware) — no entra en la tabla de versiones. NOTA: laser-pcb no es repo git todavía (decisión del Director).

- 2026-08-05 [FrioSeguro ORIGINAL revivido — v2.1.0 COMPILADA, lista para flasheo remoto esta noche 21-22h] — Misión: preparar reflasheo USB (Chrome Remote Desktop a notebook en Cerro Moro SCZ) para habilitar OTA permanente. INVENTARIO completo en `C:\Proyectos\frioseguro\REVIVAL_2026-08.md`: el original es `_ARCHIVO_FRIOSEGURO_copia_vieja` (app LOCAL = `com.parametican.alertarift` → el "alerta rif"; app CLOUD muerta por Supabase `xhdeacnwdzvkivfjzard` expirado); firmware de campo = monolito `firmware\firmware.ino` (ESP32-WROOM-32, protocolo /api/status + UDP :5555 + mDNS reefer.local). HALLAZGOS: (1) el algoritmo "redes abiertas + mayor RSSI" NO sobrevive en ningún fuente ni en git — solo existía el wizard manual del rediseño; lo REIMPLEMENTÉ según spec de Matías en `wifi_open.h` (scan→abiertas→RSSI desc→conectar; re-elección por caída o RSSI<-85 sostenido con mejora ≥10 dB; red guardada prioritaria). (2) `firmware_v2` "v4.0 modular" del archivo NUNCA compiló (web_api.h usa defrostMode/criticalAlert inexistentes en types.h) — descartado como base. FIRMWARE: `C:\Proyectos\frioseguro\firmware_revival\` = monolito + wifi_open.h + ota_update.h (ArduinoOTA:3232 + POST /update?pw + manifiesto HTTP horario con compareSemver de galgas + SHA-256 + rollback por flag NVS y reset-reason, patrón ota_wm_pp) + task WDT 120 s + comandos serial (STATUS/SCAN/WIFI:ssid:pass/OTA:URL/REBOOT). Compila `--warnings all` CERO warnings propios (25 de libs), FQBN `esp32:esp32:esp32:PartitionScheme=min_spiffs` (default no entra: 1.33 MB; min_spiffs mantiene NVS en mismo offset → credenciales de campo sobreviven), 1331119 B (67%), RAM 17%. Binarios+SHA en `firmware_revival\build\`. RUNBOOK de la noche en el mismo md (backup read_flash obligatorio, write_flash SIN erase, smoke test serial esperado, prueba OTA LAN con curl). NO tocado: BD cloud (renovación = decisión de Matías). PENDIENTE post-noche: anotar versión flasheada en la tabla, resistencia de horas + demo watchdog en HW (DoD incompleto: hoy solo compila+banco no hubo).

- 2026-08-05 [KIT SANTA CRUZ — `C:\Proyectos\frioseguro\FRIOSEGURO_SANTACRUZ_KIT.zip`, 7.2 MB, 27 archivos] — Kit autocontenido para instalar Claude Code EN la notebook remota y testear el revival con las redes abiertas reales. Contiene: (1) `CLAUDE.md` = system brief de la sesión de allá (contexto sitio/Andrés/Matías, 7 reglas duras de seguridad — backup obligatorio, USB durante toda OTA, nada de erase_flash, 3 ciclos OTA-LAN OK antes de OTA con módulo montado, un solo COM — plan F1-F6 con criterios de éxito, ORDEN DE OPERACIONES: guardián de red PRIMERO); (2) `firmware\` fuentes + build v2.1.0 + `build_test\` PRE-COMPILADOS fw_v2.1.1_sano.bin y fw_v2.1.2_ROTO_rollback.bin (el roto cuelga el loop a los 60 s sin feed → WDT → rollback; verificado por strings: cada bin con su versión) + SHA256SUMS; para esto se agregó al firmware `device.version` en /api/status (aditivo) + guard `#ifdef TEST_ROTO` + `FW_VERSION_OVERRIDE` → base 2.1.0 RECOMPILADA (1331155 B, 0 warnings propios, hash nuevo 40516574... actualizado en REVIVAL md); (3) `herramientas\` 5 scripts Python (flashear.py con backup 4 MB integrado y obligatorio, monitor_serie.py con eventos+resumen, test_api.py con discovery UDP/mDNS + validación de esquema, test_ota_lan.py incluida verificación de rollback, arma_build_test.py) — todos ast-parseados OK; (4) `guardian_notebook\` = anti-lockout de la notebook (miedo #1): guardian_red.ps1 porta el algoritmo del ESP a Windows (netsh scan → abiertas por señal → perfil open connectionMode=auto → connect → re-test, hotspot de Andrés parametrizado como botón de pánico, log SIEMPRE) + instalar (schtasks SYSTEM 2 min + ONSTART, powercfg sin suspensión, WiFi sin ahorro de energía, WU sin auto-reboot; auto-login solo documentado) + desinstalar (reversible, PC de Andrés). PROBADO ACÁ: internet-check, parseo real de netsh es-AR (bug encontrado y arreglado: la ñ de "Señal" son 2 chars en codepage OEM → regex `Se.{1,2}al`; 27 redes parseadas con señal real, 1 abierta detectada), alta/baja de perfil open sin tocar la red; queda para allá la conexión real y el end-to-end tras disconnect (anotado dentro del script); (5) `runbook\` (REVIVAL md + instalación de notebook con links de drivers CP210x/CH340 y Claude Code); (6) `app\` FrioSeguro-LOCAL-v18.apk (AlertaRift) para Andrés. Fuente del kit sin comprimir: `C:\Proyectos\frioseguro\kit_santacruz\`.

- 2026-07-13 [gimap-ap v1.0.0 — COMPILADO, NO flasheado] — Firmware nuevo pedido por Lucas Oxarango (GIMAP): ESP32 como "router" para los 2 sensores comerciales WiFi/MQTT del labo. Repo nuevo `C:\Proyectos\gimap-ap\` (git init, commit 62dc360). SoftAP fijo `Gimap_Sensores`/`Gimap2218` (WPA2, canal 6, max 8, sin WiFiManager) + **broker MQTT embebido PicoMQTT 1.3.0 en 1883** (SI entro: lib en el index con soporte esp32; subclase de `ServerLocalSubscribe` con `subscribe("#")` -> buffer circular 20 msgs + hooks on_connected/on_disconnected) + pagina de estado en 192.168.4.1 (clientes MAC/IP/RSSI via `esp_wifi_ap_get_sta_list` + `esp_netif_dhcps_get_clients_by_mac`, uptime, ultimos topics) + watchdog task 30 s + heartbeat serial 60 s. Compila limpio: FQBN `esp32:esp32:esp32`, core 3.3.8 -> **963948 B flash (73%), 51016 B RAM (15%)**. Binarios versionados en `build_out/` (.bin/bootloader/partitions; elf/map/merged gitignored). Flasheo: `arduino-cli upload --fqbn esp32:esp32:esp32 --port COMx --input-dir build_out gimap_ap` (comando en README). PENDIENTE: Matias flashea la placa elegida + smoke test (banner serial, celular visible en la pagina, publish/subscribe con mosquitto o el script Python de Lucas) -> recien ahi anotar en la tabla de versiones.
- 2026-08-21 [stitch-ainho — OTA por WiFi integrada, COMPILA + tests de PC, NO flasheada] — Misión: darle al robot de Ainho actualización por aire, copiando lo que ya funciona (nodo GIMAP del datalogger + FrioSeguro revival). El Stitch es **ESP32-S3 Arduino** (`C:\Proyectos\stitch-ainho\firmware\stitch_esp32\stitch_esp32.ino`), así que va el estilo `Update.h` con manifiesto+SHA-256, no el de MicroPython. NUEVO: `stitch_esp32\ota_stitch.h` (366 líneas, autocontenido, sin ArduinoJson: parser propio de 4 campos planos), `secretos.h` (gitignored) + `secretos.ejemplo.h`, `firmware\publicar_ota.py`, `firmware\iniciar_servidor_ota.bat` (puerto **8001**: el 8000 lo usa el datalogger), `firmware\ota_server\`, `.gitignore` del repo (no era repo git todavía).
  ORDEN DE BOOT (la lección del nodo GIMAP, comentada en el .ino): serie → `otaRescateBoot()` (solo NVS) → motores en STOP → WiFi acotado → `otaArranque()` → recién ahí sensores/ojos/lógica. El WiFi que estaba al FINAL del setup se movió arriba; no quedó duplicado.
  TRES REDES ANTI-LADRILLO: (1) aplicación atómica — se escribe la partición OTA que no corre, verificando tamaño + SHA-256 al vuelo; el punto de no retorno es `Update.end()` con hash OK; corte de WiFi/batería a la mitad = arranca el firmware viejo. (2) rollback: flag `pend` en NVS, si el boot siguiente es panic/WDT/brownout vuelve a la partición anterior (`esp_ota_mark_app_invalid_rollback_and_reboot` + fallback manual `esp_ota_set_boot_partition`, patrón galgas); se confirma con `mark_app_valid` a los 60 s de vida sana. (3) **MODO RESCATE anti crash-loop**: contador `arranq` en NVS que se limpia a los 60 s; 3 resets seguidos sin llegar → `otaArranque()` NO vuelve nunca: motores frenados, cero sensores, solo WiFi+OTA cada 15 s. Un firmware con bug de arranque se arregla igual sin cable. Vía 2 de rescate: ArduinoOTA :3232 (host `stitch`, pass en secretos.h). Hook `otaAntesDeFlashear = frenar` para que no flashee con las ruedas girando. Tecla `o` en el menú serie = chequear ya + estado (versión, servidor, partición en uso).
  VERIFICADO SIN HARDWARE: compila `arduino-cli compile --fqbn esp32:esp32:esp32s3:CDCOnBoot=cdc` → **1109000 B (84% de 1310720)**, RAM 15% — o sea la partición default del S3 SÍ tiene 2 slots OTA (con "Huge APP" no habría OTA: anotado en el header). Cableado verificado por grep (principio 1): include + las 6 llamadas en el .ino. 15/15 tests en g++ del parser de manifiesto y del comparador de versión (1.2.9 < 1.2.10 numérico, JSON roto/cortado no rompe). Cadena de PC end-to-end contra `http://192.168.0.232:8001`: `publicar_ota.py 1.0.0` publicó bin+version.json, el servidor sirvió Content-Length 1109152 = `size` del manifiesto y el sha256 del bin servido = el publicado.
  NO PROBADO (falta hardware): descarga y flasheo reales, rollback provocado, modo rescate, resistencia de horas. **El primer flasheo tiene que ser por USB** (el S3 no tiene el firmware nuevo todavía). PRÓXIMO PASO: grabar v1.0.0 por cable, después `python publicar_ota.py 1.0.1` + `iniciar_servidor_ota.bat` y ver el ciclo completo por serie; probar a propósito un 1.0.2 con `while(1)` en setup para ver saltar el modo rescate. Deuda: el sketch no tiene watchdog de hardware (el modo rescate solo actúa si algo llega a resetear la placa); los ojos Heltec (`firmware\ojo_heltec`) no tienen WiFi ni OTA — hoy se actualizan por cable.

- 2026-08-27 [FrioSeguro `firmware_modular` — testigo de internet arreglado, COMPILA, NO flasheado · branch `nocturno/local-2026-08-27-el-testigo-que-certificaba`] — `wifi_utils.h` decidia `internetAvailable = (httpCode == 204 || httpCode == 200)` contra `/generate_204`. Esa URL existe **precisamente** para separar internet real (204 sin cuerpo) de portal cautivo / router sin uplink / ISP suspendido (**200 con HTML**, o 302 hacia el): el `|| 200` convertia al unico detector capaz de verlo en su certificador. Es el **H4 de la auditoria del 2026-08-07**, que llevaba 20 dias escrito y ningun branch tocaba `wifi_utils.h`. Pesa porque `internetAvailable` es la compuerta de todo lo que se cobra (20 `return` en `supabase.h` + Telegram en `alerts.h`/`door_sensors.h`/`current_sensor.h`/`power_monitor.h`): el comercio se veia verde y la alerta no salia. **Era drift entre familias**: `firmware_revival` 2.6.0 (Santa Cruz) ya lo tenia bien; la familia de los abonos no. NUEVO `firmware_modular/net_witness.h` (128 lineas, autocontenido): solo 204 con cuerpo vacio (`c == 204 && h.getSize() <= 0`), `HTTPC_DISABLE_FOLLOW_REDIRECTS` (el destino del 302 si contesta 200), **dos testigos** (Google + Cloudflare, alcanza uno — con uno solo un bloqueo de ese dominio veta una red que anda), y el veredicto deja de ser bool: 4 diagnosticos `ok`/`portal`/`sin_red`/`wifi_off` publicados en `GET /api/status` (`internet_diag` + `internet_http_code`) y en el panel embebido. En `wifi_utils.h`: `checkInternet()` delega y no vuelve a decidir; `connectWiFi()` deja de asumir internet al asociarse (habia hasta 30 s posteando contra el portal) y vence `lastInternetCheck` — ojo, `= 0` NO sirve (con `millis()` chico la resta no supera el intervalo), va `millis() - INTERVAL_INTERNET_CHECK_MS`; y la cadencia sale de esa constante en vez del `30000` pelado que la dejaba de adorno.
  VERIFICADO SIN HARDWARE: `arduino-cli compile --fqbn esp32:esp32:esp32 firmware_modular` **OK**, `1289764 B (98%)`, RAM 16%; contra `main` compilado en worktree limpio (`1288696 B`) el delta es **+1068 B, RAM igual**. Ademas 4 arneses: `tools/test_net_witness.cpp` compila el **C++ real** con stubs de `HTTPClient` y corre **15 checks** (incluye "el 2do testigo no se consulta si el 1ro alcanza" y "sin WiFi no se gastan timeouts"); `tools/test_net_witness_model.py` = oraculo Python espejo 1:1, **17 tests**; `tools/check_net_witness_wiring.py` = **16/16**, verifica que el firmware lo USE (no que exista); `tools/test_check_net_witness_wiring.py` = **15 tests**, 11 mutantes que reabren cada agujero de a uno y exigen que el check se ponga en rojo. Doc: `docs/net-witness.md`.
  PENDIENTE (campo, no banco): ver `internet_diag = portal` contra un portal real o desenchufando el WAN del router. Sin confirmar: que `getSize()` devuelva algo util con respuesta `chunked` (da -1 → un 204 chunked pasaria; direccion segura del error).
  RECORDATORIO que sigue vivo desde el 2026-07-13: **98% de la particion de programa** (~21 kB libres) — la proxima feature no entra y el OTA cloud queda en riesgo. Pasar a `min_spiffs`/`huge_app` en el proximo flasheo (que ya esta pendiente por la rotacion de claves del 07-10-b y el `OTA_PASSWORD` del 07-12). Los headers huerfanos (`serial_api.h`, `power_monitor.h`, `current_sensor.h`) siguen sin incluirse: por eso el diagnostico se expuso por `/api/status` y el panel, y no por el `STATUS` serial que la auditoria del 08-07 mandaba a mirar (ese comando no existe en esta familia).

- 2026-08-28 [galgas emisores modulares A/B — calibracion cableada + tara remota · branch `nocturno/local-2026-08-28-la-calibracion-que-nadie-lee`, commit `c048b93`] — Detalle completo en `muestreador.md` (es su dominio: la cadena galga → numero). Lo que toca a firmware: (1) **`CMD_FORCE_CAL` guardaba su estado en `CommandSideEffects`, una struct de RAM, y el `.ino` hacia deep sleep dos pasos despues** — el flag no lo consumia nadie y de todas formas no sobrevivia; ahora la etapa va a NVS (`nvsSaveCalPending`) y la ejecuta el wake siguiente. Regla que sale: **cualquier side effect de un comando que tenga que sobrevivir al sleep va a NVS o a `RTC_DATA_ATTR`, nunca a una struct de RAM**; el `.ino` hace `goSleep()` a pocas lineas del handler y eso no se ve leyendo el `command_handler.cpp` solo. (2) Las 3 etapas contestaban `ack=OK` sin hacer nada; ahora la 1 (tara) esta implementada y la 2 y la 3 devuelven error — **un NACK honesto vale mas que un OK vacio**, sobre todo cuando el operario esta parado al lado del REDLER esperando. (3) `set_calibration` validaba con `0.1f`/`10.0f` escritos a mano, los mismos numeros que en otro archivo: ahora salen de `CAL_K_MIN`/`CAL_K_MAX` de `firmware/shared/cal_model.h`. (4) A y B se mantienen **byte a byte identicos** (salvo `config.h` y la cabecera del `.ino`) y hay un check que lo vigila: la mitad de la deuda de este repo nacio de un arreglo que entro en una sola de las dos.
  Compila A y B: `1237624 B (94%)` / RAM 18%, `+2264 B` sobre `main`. **94% de la particion** — mismo aviso que la entrada del 2026-08-27 de FrioSeguro (98%): sin margen no hay OTA, y con la caja montada eso es un viaje a planta.

- 2026-09-01 [galgas RX + emisores modulares A/B — rescate post-OTA cableado · branch `nocturno/local-2026-09-01-el-rescate-desarmado`, commit `567aeef`] — **El rollback post-OTA del RX estaba entero y nunca se armaba.** `esp_rx_receptor.ino` tenia la maquina completa —`ota_pending_verify`, `checkOtaRollback()` llamado en `setup()`, `doRollbackAndReboot()` con fallback manual, `ROLLBACK_VERIFY_WAKES` en su `config.h`— y `grep -rn "ota_pending_verify" firmware/shared/` devolvia **cero lineas**: la bandera se declaraba, se chequeaba y se limpiaba, y nadie la prendia. Arriba de `doOtaCheck()` un comentario decia que "lo maneja `supabase_client.cpp`"; la mitad de arriba (el bug 3.5.3) era cierta, la de abajo describia un cableado que nunca se hizo — se movio la descarga al modulo compartido y el armado se quedo en el camino. Los emisores A/B no tenian nada (Task 09, `TODO` en sus README). O sea: **las tres familias modulares, las de octubre, flasheaban OTA sin red**, y un bin que no arranca solo se recupera con cable USB sobre una caja atornillada al REDLER. Mismo patron que el `08-28` (la calibracion que no leia nadie) y que el `08-27` de FrioSeguro: **drift entre la familia monolitica y la modular, donde lo que se pierde no es el codigo sino el cable.**
  NUEVO `firmware/shared/ota_rescue.h` (header-only sobre NVS). El armado es **una sola linea** en `supabaseDownloadFirmware()`, despues de `Update.end(true)` OK y antes del `ESP.restart()` — como es el downloader compartido, cubre RX + A + B de una. Nunca antes de la descarga: eso era el bug 3.5.3 (un crash bajando el `.bin` mandaria a rollback al bin viejo, que es el que anda). Los tres `setup()` chequean al bootear, **antes de la radio y del ADC**; crash (`PANIC`/`INT_WDT`/`TASK_WDT`/`WDT`/`BROWNOUT`) con verificacion pendiente → rollback, 3 boots sin crash → confirmado (~20 min con periodo NORMAL). El RX pierde su copia muerta y deja de definir la cuota por su cuenta. Los emisores publican `metadata.ota_rescue` (`verifying`/`confirmed`/`aborted`) en `readings`.
  **Dos arreglos sobre el original monolitico, los dos defectos reales de aquel:** (1) el estado pasa de **RTC memory a NVS** — la RTC memory se pierde con el corte de energia que provoca el propio bin malo al disparar brownout, o sea que la red desaparecia justo en el caso para el que existia; y el layout de `.rtc.data` lo decide el linker de **cada bin**, y el que lee la bandera no es el que la escribio (que es literalmente el punto del OTA). (2) se **chequea el retorno de `esp_ota_set_boot_partition()`** — si el otro slot esta vacio (equipo flasheado por cable una sola vez), apuntar el arranque ahi convierte un equipo que anda en uno que no arranca: el rescate fabricando el ladrillo. Ahora si no valida, aborta y sigue con el bin actual.
  VERIFICADO SIN HARDWARE: `tools/test_ota_rescue.cpp` = **62 checks** de la maquina de decision sobre el header REAL con shims de host en `tools/fakes/`; `tools/check_ota_rescue_wiring.py` = **50 checks de cableado** + `--mutants` mata los **9 mutantes** (no armar nunca, que el panic no cuente, confirmar al primer boot, reiniciar sin validar el otro slot, …), y corrido con `--root` **contra `main` REPRUEBA con 27 fallas** — un checker que no reprueba el bug que dice cazar no vale nada. Compilan las tres familias (esp32 3.3.8): A/B `1236988 B` (**+1628 B** sobre main), RX `1235828 B` (**+1152 B**), RAM sin cambios. Sigue el **94% de particion** avisado el 08-28: quedan ~74 kB.
  PENDIENTE (banco): todo lo de arriba prueba la **decision**, no el **efecto** — nadie vio todavia que `esp_ota_set_boot_partition()` mueva el arranque de un ESP32 real. Procedimiento en `docs/ota-rescue.md` §5: flashear a proposito un bin que panica en `setup()` y ver el rollback ocurrir; ademas el caso del slot vacio y el `factory_reset` con verificacion en curso.
  PENDIENTE (Task 09, otra mitad): **el watchdog**. Un bin que se *cuelga* sin resetear no genera `reset_reason` y este rescate no lo ve. No se hizo esta noche por una razon concreta: la API de `esp_task_wdt` cambio entre arduino-esp32 2.x y 3.x, y el timeout tiene que superar un wake **con OTA adentro** (~110 s: 3 intentos de 30 s) — elegir mal ese numero reinicia nodos sanos a mitad de una descarga, o sea convierte la red de seguridad en la falla. Se elige en banco, con cronometro.
  BIBLIOTECA: `ota_rescue.h` es candidato claro (generico a cualquier ESP32 con OTA; FrioSeguro lo querria igual — su `firmware_revival` tiene su propia version del mismo patron). **No se cosecha todavia**: ningun modulo entra sin decir donde se probo, y esto no vio un rollback real. Cuando pase el banco, va.

- 2026-09-01 [TERMOVIGÍA — PLAN FIRMWARE v3.1 escrito, sin código todavía] — `C:\Proyectos\frioseguro\firmware_modular\PLAN_V3.1_TERMOVIGIA.md` (521 líneas): gap analysis módulo por módulo contra lo que el precio vende (TERMOVIGIA §3/§6), diseño de `lte_a7670.h` (máquina OFF→PWRKEY→AT→SIM→CEREG→PDP→READY, tarea FreeRTOS propia en core 0 con cola de trabajos —nunca en `loop()`—, AT concretos para SMS/`ATD;`+`AT+CHUP`/HTTPS con `CSSLCFG` + SNI, watchdog del módulo), `ats.h` (12 estados, un relé de conmutación OFF=red como estado seguro, enclavamiento de fuerza a cargo del electricista, `atsDrive()` única función que toca los relés), `power.h` (red con confirmación 5 s, SLA por tensión con `analogReadMilliVolts`, dos estimadores de autonomía, escalada Telegram→push→SMS→llamada por nivel), orden de merge de los 24 nocturnos (5 de firmware entran: 08-21-b, 08-18, 08-27, 08-26, 08-29 — verificado hoy con `merge-tree`: el único conflicto de todos es `QUE_FALTA.md`), plan de banco B1-B12 y estimación (~44 h subagente / ~22 h Matías / ~10 h @backend).
  HALLAZGOS que condicionan (todos verificados por grep/git, no por memoria): (1) **`firmware_modular` no tiene watchdog de ningún tipo** — `grep esp_task_wdt` da cero; con un grupo electrógeno colgado de un relé eso es lo primero que entra. (2) `power_monitor.h` y `current_sensor.h` siguen huérfanos, y además `power_monitor.h` llama a `sim800SendPowerAlert()` que no existe en ningún lado y `current_sensor.h` redefine `PIN_CURRENT_SENSOR 36` contra el 32 de `config.h`: no se integran, se reescriben. (3) GPIO33 ya es `PIN_DEFROST_INPUT`; la placa v2 lo pide para estado-grupo → pedido a @esquematico de dos optos/dos GPIO. (4) **El A7670 no hace VoLTE**: la llamada de voz sale por 2G (CSFB) y Movistar Argentina apagó el 2G el 31/12/2023 — la "llamada" vendida se valida en banco el día 1 con la SIM real y tiene plan B por nube (`notify-call` → Twilio) que @backend tiene que armar. (5) 98 % de flash: v3.1 obliga a `min_spiffs` y por lo tanto a un flasheo por cable antes de cualquier OTA. (6) `AT+CREG?` (lo que usaba `sim800.h`) es registro 2G: en LTE hay que mirar `AT+CEREG?`, si no el módulo parece "no registrado" para siempre.
  BIBLIOTECA: `esp32\` está vacía; el plan reusa `ota_rescue.h` de galgas, `net_witness.h`/`offline_buffer.h`/`device_clock.h` de los branches y `compareSemver()` del revival. Candidatos a cosechar después del banco: `lte_a7670.h`, `power.h`, `ats.h`, `transport.h`.
  NADA flasheado (no entra en la tabla). PRÓXIMO PASO: cuando @esquematico entregue los GPIO → completar los 8 `__TBD__` del plan y lanzar el subagente con el orden de §6 (merge → WDT → power → lte); día 1 de placa = escenarios B5-B8 (LTE) antes que cualquier otra cosa.

- 2026-09-02 [GALGAS nodo v3 (ATmega328P + ADS1220 + RA-02) — el firmware NO EXISTÍA; nace alineado a la rev C, COMPILA, nada flasheado] — Encargo: alinear el nodo emisor a la rev C y resolver por firmware el brownout de la última TX. **Lo primero que encontré al orientarme: el firmware del nodo v3 no existe en el repo.** `grep -rli "atmega|ads1220|328p"` sobre `C:\Proyectos\galgas` devuelve **4 archivos y los 4 son .md de hardware** (`NOTAS_CALCULO.md`, `VERIFICACION_P6_2026-09-02.md`, `perfboard/GUIA_ARMADO_PERFBOARD.md`, `kicad/LAYOUT.md`). Todo `firmware/` es ESP32 (emisores A/B, RX, ota_wm_pp…). O sea que no había nada que "alinear": había que escribirlo. Tampoco hay nada reusable en `C:\Proyectos\biblioteca` (`esp32/` está vacía; `micropython/` y `pc/` no aplican a AVR bare-metal; el driver SX127x de RuView es de otro micro y otro runtime).
  **Entregado** en `C:\Proyectos\galgas\firmware\nodo_galga_v3\`: `config_nodo.h` (pines rev C + fusibles + umbrales + tiempos), `pines.h/.cpp`, `energia.h/.cpp` (reloj, WDT, sleep, VBAT, escalón de potencia), `ads1220.h/.cpp`, `sx1278.h/.cpp`, `nodo_galga_v3.ino` (máquina de estados MEDIR→ESPERA_RECARGA→TX→VENTANA_RX→DORMIR + DEGRADADO), `PINOUT.md` (293 líneas: tabla de pines, fusibles, la resolución del brownout con números, pendientes de banco). Más `tools/check_pinout_nodo_v3.py`.
  **G7 (DIO0 PD1→PB0)**: implementado como **PCINT0** (`PCICR.PCIE0` + `PCMSK0.PCINT0`), no INT0/INT1; la ISR mira el nivel del pin porque PCINT dispara en los dos flancos. PD0/PD1 salen a J3 sin poblar → con `DIAG_SERIAL=0` van entrada con pull-up (flotando pagan corriente en power-down) y el UART se apaga por PRR.
  **El cruce de pines no se hizo a ojo**: `tools/check_pinout_nodo_v3.py` parsea el **netlist rev C** (51 redes, parser de s-expr propio), `config_nodo.h` y la **tabla de la §4 de NOTAS_CALCULO**, y las cruza pin por pin contra el mapa TQFP-32 del datasheet, con alcance a un salto por pasivos (PD6 no toca el LED: toca RL1, y RL1 toca LD1). Resultado **OK** y `--mutantes` mata **5/5** (incluido "DIO0 vuelve a PD1"). **La §4 de NOTAS_CALCULO cruza limpio contra el netlist: no hay ningún pin mal en el documento.**
  **BODLEVEL 1,8 V: NO es seguro a 8 MHz, SÍ a 4 MHz — y no hay BODLEVEL intermedio.** Datasheet §32.3: `0-4 MHz @ 1,8 V`, `0-10 MHz @ 2,7 V`, curva **lineal** entre 1,8 y 2,7 V → a 8 MHz el mínimo garantizado es **2,40 V** (2,52 con el RC +10 %), no los 2,7 V que dice la tabla de pisos de C23 (**corrección a @hardware, a favor**: 300 mV más de margen del que se estaba dando). Pero con `BODLEVEL=110` (VBOT 2,0 V máx) queda una **banda 2,0-2,52 V sin protección y fuera de especificación**, que es cambiar un reset predecible por comportamiento indefinido. Tabla 32-8: sólo existen 111/110/101/100 → **no hay nivel intermedio**. Solución sin componentes: **correr a 4 MHz** (RC de 8 MHz ÷2 por `CLKPR` en `.init3`, antes de `main()`, porque el core de Arduino configura timer0 y el UART en `init()` y si el prescaler cambiara en `setup()` millis y el baudrate quedarían al doble). A 4,4 MHz (peor caso del RC) el mínimo es **1,86 V, por debajo del VBOT máximo de 2,0 V**: el agujero desaparece. Bonus: baja el consumo activo (1,7 mA típ a 4 MHz/3 V, Tabla 32-4) y sostiene el renglón de C10. **R1 se adopta con el prescaler, no sola.** Agregado que no estaba en C23b: **BOD apagado durante el sleep** (`BODS/BODSE`), porque durmiendo no protege de nada y consume permanente contra un reposo de 29 µA.
  **R2 vs. el back-off de @esquematico: se pisan, y no se implementan los dos.** Mismo disparador (VBAT < 3,35 V), distinto escalón (+17 vs +14 dBm). Rehice la cuenta con la ESR de peor caso que manda el propio C16 (**0,8 Ω**, no los 0,4 Ω de catálogo que usó C23): con R3 aplicada, VOUT tras la TX da **+20 dBm → 2,877 V · +17 → 2,961 V · +14 → 3,067 V**; contra un BOD de 2,9 V **sólo +14 dBm entra** (+17 pasa por 61 mV, y **sin** R3 por 4 mV, que no es margen). O sea: **@hardware tiene razón en el fusible y @esquematico en el escalón.** Va **un solo escalón, a +14 dBm**, con histéresis (3350/3450 mV) y trama corta por debajo de 3050 mV. La discrepancia de fondo: C7b llama "apagón" a que el LDO salga de regulación, y eso **no es apagarse** — un LDO en dropout es una resistencia (`VOUT = VIN − I·RDS`); por eso C7b declara fin de vida a 3,3 V y C23, que sí calcula el VOUT hundido, llega bastante más abajo. **Con el fusible corregido, el fin de vida lo fija el BOD y no la salida de regulación** → `VBAT_MV_AGOTADA = 3050` en vez de 3300, y eso **lo tiene que firmar @energia**. Si @verificador concluye distinto sobre el mismo par de secciones, la diferencia va a estar en qué ESR usó y en si cuenta "fuera de regulación" como falla.
  **BLOQUEANTE que ninguna de las dos secciones vio: el escalón de potencia no se puede usar sin calibrar la referencia interna.** El bandgap de 1,1 V tiene **±10 %** (Tabla 32-7) → ±330 mV sobre un umbral de 3350 mV: dispararía en cualquier punto entre 3,02 y 3,72 V. Y el error es **asimétrico en consecuencia**: si la referencia real es baja, el firmware **sobreestima** la pila y baja la potencia **tarde**, que es justo el caso que mata al nodo. Por eso `cfg_nodo.h` es config persistente **versionada** en EEPROM (magic + versión + CRC-16 + migración), `HFUSE` lleva **EESAVE programado** para que un chip erase no se lleve la calibración, y el nodo arranca con `cal_valida=0` avisando en vez de mentir un número. **Medir el bandgap por unidad es paso obligatorio del bring-up, no un refinamiento.**
  **Consecuencias de los componentes nuevos**: (a) CSC pasó a 2 celdas en serie → `τ = 10 s, 3τ = 30 s` (C16): el arranque en frío espera **30 s antes de la primera TX** (condicionado a `MCUSR & PORF`) — **el "minuto de despasivación" del checklist quedó viejo: son 30 s**; (b) R3 **se duerme, no se hace `delay()`**: 30 s activo a 1,7 mA son 51 mAs, más que el ciclo entero (48,5 mAs), y encima el supercap no se recargaría — la regla no serviría para nada; (c) pull-ups G6 → los selectores ya arrancan deseleccionados y el RA-02 **fuera de reset**, así que el reset del RA-02 es un LOW deliberado (no el flanco de un pin que pasa de flotante a salida) y al ADS1220 se le manda `RESET` **siempre**, porque MISO/MOSI/SCK son compartidos con el ISP; y todo selector se escribe **ALTO antes de pasar a salida** (misma lección que los relés de Termovigía); (d) G14 → constante del divisor **0,2481 → 0,2357** (5,3 % de error si no se cambia, justo el que rompe el escalón) + 12 ms de espera + conversión dummy; (e) G22 → la ventana de RX de 200 ms se temporiza con `RegSymbTimeout` del **cristal del SX1278**, no con el RC del ATmega. Aparte, PB2 está marcado NC en el esquemático pero **se configura salida en alto igual**: es el `SS` del SPI y si queda entrada y alguien la baja, el hardware SPI se pasa a esclavo y el nodo deja de hablarle a los dos periféricos.
  **Regla dura E−/REFN**: garantizada por `static_assert` (MUX fijo AIN1-AIN2, `VREF=10b`, `PSW=1`) — medir "contra masa" **no compila**. Y `ads1220BuscarGanancia()` sube por escalones desde **G=1** (paso 5 del bring-up de la guía de perfboard, implementado y no comentado).
  **Un hallazgo del principio 1, encontrado en el desensamblado y no en el fuente**: con `DIAG_SERIAL=0` el único consumidor de `motivoDeReset()` desaparecía y el linker se llevó puesta **la captura entera de MCUSR** (`avr-nm` no encontraba el símbolo). Arreglado con `volatile` + **MCUSR viaja en la trama**: el nodo va atornillado al REDLER y nadie le va a enchufar un serial; `BORF`/`WDRF` por aire son la evidencia del brownout y del watchdog.
  **Verificación real**: `arduino-cli compile --fqbn arduino:avr:pro:cpu=8MHzatmega328 --build-property build.f_cpu=4000000L --warnings all firmware/nodo_galga_v3` → **exit 0, 4478 B de flash (14 %), 70 B de RAM (3 %)**, cero warnings propios (los únicos son de `new.cpp` del core AVR 1.8.7). Los 5 `.cpp` linkean (sus `.o` en el build path). Desensamblado con `avr-objdump`: `.init3` con `CLKPR=0x80/0x01` y el `sts` de MCUSR a `.noinit`; vectores `INT0`/`PCINT0`/`PCINT2`/`WDT` cableados (no `__bad_interrupt`); `out 0x35` (BODS) pegado al `sleep`.
  **NADA FLASHEADO — no entra en la tabla de versiones.** Pendiente de banco (lista completa en `PINOUT.md` §7): grabar fusibles (`lfuse 0xE2 / hfuse 0xD1 / efuse 0xFE`) y confirmar los 4 MHz; **provocar el brownout** con la impedancia de pila de fin de vida y ver que no resetea; **provocar un cuelgue** y ver al WDT recuperarlo; medir el consumo real del RA-02 en PA_BOOST a +14/+17 dBm (C23d punto 8 — hoy los 45 mA son estimación y de eso depende la tabla); calibrar el bandgap por unidad; medir el reposo con el INA219 contra los 29 µA de C10c; P3 (pinout real del módulo RA-02 con téster). No es de este dominio: formato de trama y reintentos (@comms), las 60 conversiones (@muestreador), firma de umbrales (@energia). **Y una que hay que decir en voz alta: en un ATmega328P con ISP no hay OTA — el plan de recuperación de un nodo colgado es físico, así que el watchdog y el fusible son toda la red que hay.** Sin commit (lo pidió el encargo). **Candidato a biblioteca cuando pase el banco**: `check_pinout_nodo_v3.py` (netlist KiCad ↔ config.h, con mutantes) es genérico; hoy no se cosecha porque ningún módulo entra sin decir dónde se probó en hardware.
  **Contraste con @verificador** (`hardware/VERIFICACION_P6b_revC.md`, mismo día, en paralelo): coincidimos en el fusible (**BODLEVEL = 1,8 V**) y en R3; **diferimos en el escalón** — él elige +17 dBm porque los 87 mA salen de la Tabla 6 de Semtech y **los 45 mA de +14 dBm no tienen fuente** (objeción correcta, es la regla dura del proyecto). Pero hay un dato que no está en ninguno de los tres análisis y que desactiva buena parte de la discusión: **con R1 aplicada el escalón casi no compra nada.** Despejando la OCV mínima con R3, `OCV_min = 2,0 + 0,020 + dV_TX + dropout` → **+17 dBm: 2,24 V · +14 dBm: 2,13 V**: **110 mV de diferencia, y los dos muy por debajo del codo de la LiSOCl2** (la celda está muerta mucho antes de 2,5 V de OCV). O sea que **lo que salva la última TX son R1 y R3, no el escalón**. Por eso el escalón se implementa igual (cuesta una línea y es la única red que queda si un nodo termina en campo con `BODLEVEL=101` por un grabado mal hecho, donde pasa de irrelevante a imprescindible), con `TX_DBM_PLENA = 17` (numero de datasheet) y `TX_DBM_BACKOFF = 14` marcado **PROVISORIO**: si la medición de C23d-8 muestra que bajar a +14 dBm en PA_BOOST no baja la corriente, se cambia esa línea a 17 o se saca. Está escrito en `PINOUT.md` §4.5.

## 2026-09-02 — Pedido de @backend: dejar de mandar la contraseña del WiFi del cliente

`firmware_modular/supabase.h` **línea 296** manda `wifi_password` en cada lectura. Es la contraseña del
WiFi **del cliente** — la misma con la que se entra a su red y probablemente la misma que usa en otras
cosas. El servidor propio ya la descarta (`TV_GUARDAR_PASSWORD_WIFI=0`) y no loguea el cuerpo, pero el
firmware la sigue transmitiendo en cada petición.

No es una deuda técnica: es un dato personal que **no necesitamos**, en un sistema que vamos a vender
por confianza. Si un cliente o un auditor pregunta "¿qué se llevan de mi red?", la respuesta tiene que
ser "nada", no "lo recibimos pero lo tiramos".

**Lo que hay que cambiar (prioridad alta, ~1 hora):**

1. **Borrar el campo del JSON de `supabaseSendReading`** (`supabase.h:296`). Es **una línea**. El
   servidor ignora tanto los campos desconocidos como los que faltan, así que un equipo con firmware
   nuevo y otro con firmware viejo conviven sin tocar nada del lado servidor: **no hace falta coordinar
   el despliegue**, puede ir en el próximo OTA junto con lo demás.
2. **Seguir mandando `wifi_ssid`.** Sirve para diagnosticar ("se pasó a la red del vecino") y no es un
   secreto. Solo se saca la contraseña.
3. **Si alguna vez hace falta verificar qué credencial tiene cargada un equipo**: que sea bajo pedido
   explícito y puntual — un comando `WIFI_DIAG` en `wifi_commands` (la tabla ya existe en `sql/010`;
   antes no existía y el firmware venía recibiendo 404 en silencio) que el equipo responde **una vez**
   en el campo `result`, con la contraseña **enmascarada**: `Casa1234` → `Ca****34`. Alcanza para
   confirmar que se cargó la correcta sin transportar el secreto.

Recién **después** de que ningún equipo en campo mande el campo, @backend dropea la columna
`readings.wifi_password`. **El orden importa**: dropearla antes haría que PostgREST rechace la
**lectura entera** de todo equipo con firmware viejo, y perderíamos temperaturas por proteger un dato
que ya estábamos tirando.

Contexto completo en `C:\Proyectos\frioseguro\servidor\SEGURIDAD.md` §4.2. Sigue abierto también §4.1
(el firmware no valida el certificado TLS: con servidor propio se puede pinnear la ISRG Root X1, dos
líneas y una constante).

- 2026-09-02 (segundo turno) [GALGAS nodo v3 — **DIP-28 vs TQFP-32 por bandera, `VBAT_SENSE` remapeado a ADC3/PC3, y el plan de bring-up en banco**. Compilan las dos variantes, nada flasheado. Sin commit (lo pidió el encargo)] — Encargo: el banco se arma con ATmega328P en **DIP-28**, y ese encapsulado **no tiene ADC6 ni ADC7** — que es justo donde la §4 de `NOTAS_CALCULO` pone `V_BAT_SENSE`.
  - **Remapeo: `V_BAT_SENSE` → ADC3 / PC3 / pin 26.** Ocupados PC0 (`CS_ADC`) y PC1 (`NSS_LORA`); PC4/PC5 se reservan para SDA/SCL; **PC2 se deja libre a propósito**, porque queda entre `NSS_LORA` —que conmuta en cada transacción SPI— y un nodo analógico de **236 kΩ**, y en el banco además va atado a GND, o sea que hace de pantalla. **Dos reglas que el TQFP-32 no necesitaba y el DIP-28 sí**: (1) `DIDR0 |= _BV(ADC3D)` — ADC6/ADC7 son pines solo-analógicos y no tienen buffer digital, PC3 sí, y con Q3 cerrado el nodo queda en ~0,87 V, **entre VIL (0,9 V) y VIH (1,8 V) con VCC = 3,0 V**, que es la zona donde un buffer habilitado conduce de VCC a GND; (2) **sin pull-up interno**: 30 kΩ en paralelo con una pata de divisor de 308 kΩ destruye la medición. El resto de la cadena (RD1/RD2/RD3/CVB1, k = 0,2357, 12 ms, dummy, mediana de 5) es idéntica.
  - **Una constante, no un fork**: `-DMCU_TQFP32` / `-DMCU_DIP28`, y **sin default** — sin flag el `#error` corta el build (verificado). Porque el error a evitar no es compilar mal: es **flashear el binario equivocado**, que lee un canal sin pin, mide cualquier cosa y decide el escalón de potencia y el fin de vida con ese número, en silencio.
  - **Red en runtime: strap de PC2.** En la PCB PC2 está NC → pull-up interno → alto; en el banco, cable a GND → bajo. `mcuStrapCoincide()` lo lee **una vez** en el arranque y deja el pin **salida en bajo** (0 µA en los dos casos, y no queda una entrada flotando en power-down). Si no coincide: `EST_ENCAPSULADO_MAL`, **no transmite**, 3 destellos largos (distinguible del destello único de `EST_DEGRADADO`) y reintenta cada 8 s por si fuera falso contacto. Costo: **cero en la PCB** (el pin ya estaba NC), un cable en el banco. Precedente del propio repo: el chequeo de MAC `EN_DREYFUS` de los ESP32.
  - **Corrección de Matías sobre el reloj, incorporada**: el firmware ahora banca **tres caminos con un solo `F_CPU` de 4 MHz**. `CLK_FUENTE_HZ` (8 MHz por defecto) elige el prescaler en `.init3`: RC interno o **cristal de 8 MHz** → `/2` (**cero cambios de código, cambia solo el `LFUSE`**); **cristal de 16 MHz a 5 V** → `-DCLK_FUENTE_HZ=16000000UL` → `/4`, y sigue dando 4 MHz. El dato que decide la compra y quedó escrito en grande: **16 MHz pide 4,5 V** (Speed Grades: 0-10 MHz @ 2,7 V, 0-20 MHz @ 4,5 V) → **16 MHz a 3,0 V está fuera de especificación** y falla de forma no reproducible con frío o tensión baja. Si va cristal, **el de 8 MHz**.
  - **Lo demás que cambia entre encapsulados y podía mordernos** (revisado contra el datasheet, no asumido): **alimentación** — el DIP tiene VCC 7 / GND **8 y 22** / AVCC 20 / AREF 21, dos puntos de desacople en vez de tres, y AVCC sigue siendo obligatorio aunque no se use el ADC; **la numeración de PORTB/PORTD se corre** (PC0..PC5 son los únicos que caen en 23..28 en los dos); y la trampa de compra: **los ATmega328P-PU "con bootloader" vienen con `LFUSE 0xFF` (cristal externo)** → sin cristal **no tienen reloj y el ISP no los puede tocar** (`rc=-1`, parece un chip muerto). Rescate: cristal prestado o **inyectar 1-8 MHz en XTAL1** (con `ledcAttach` de un ESP32). Fusibles, RC, BOD, WDT, EEPROM y SPI son del die: **todo el análisis de brownout vale igual**.
  - **Programador: ESP32 como ISP (ArduinoISP), no USB-UART.** (1) Los fusibles del proyecto —`BODLEVEL=110`, que **es** el arreglo del brownout, y `EESAVE`— **solo se escriben por ISP**; (2) grabar un bootloader también es ISP, o sea que el camino UART empieza igual con una sesión de ISP; (3) **el bootloader estándar es de 16 MHz y el nodo corre a 4**: habría que compilarse uno propio para llegar a algo peor; (4) tensión: el ESP32 a 3,3 V contra el nodo a 3,0 V son 0,3 V, por debajo de `VCC+0,5` y del Vf del diodo de protección, mientras que **un USB-UART de 5 V lo destruye**. El USB-UART **sí se usa, pero como consola** (`-DDIAG_SERIAL=1`, 9600, PD0/PD1): ISP y consola conviven, usan pines distintos.
  - **Entregable principal**: `C:\Proyectos\galgas\hardware\BRINGUP_BANCO.md` (620 líneas) con las 5 etapas de Matías, cada una con qué se conecta, **qué evidencia observable la da por aprobada** y qué hacer si falla; grafo de dependencias sin fechas (2 y 3 son paralelas; 4 no tiene sentido sin 2 y 3 armadas). Adentro: el `MCP1700` **en TO-92 tiene el pinout al revés que el SOT-23 del símbolo del esquemático** (`1=GND 2=VIN 3=VOUT`) y **quien lo suelde "según el esquemático" lo quema** — escrito en grande al principio de la etapa 4; `E−`/`REFN` no es masa (y en el firmware es `static_assert`: medir contra masa **no compila**); PGA desde G=1; P3 (continuidad del RA-02 con téster) como paso 0 de la etapa 2; el paso de 2,0 mm del RA-02 que no entra en protoboard; **arranque en frío de 30 s, no de un minuto** (el checklist viejo era del diseño con 1 F); y los dos ensayos que son DoD: **brownout provocado** (10 TX sin `BORF`) y **cuelgue provocado** (reinicio en ~4 s con `WDRF` en la trama). Etapa 5 dejada explícitamente a **@muestreador**, con la lista de lo que el firmware ya ofrece (EEPROM versionada con CRC + `EESAVE`, `CAL_EN`, crudos y ganancia en la trama, aviso `cal_valida=0`) y **lo que falta y depende de @comms**: comando de shunt-cal por downlink, las 60 conversiones crudas (hoy viaja solo la media), modo banco con timeout, `K`/offset por canal (bump de `CFG_VERSION`) y temperatura de calibración (que hoy no se mide: sería el sensor interno del ADS1220 o hardware).
  - **Verificación real, sin hardware**: las dos variantes compilan con `--warnings all` y **cero warnings propios** — **TQFP-32 4608 B (15 %) / DIP-28 4622 B (15 %)**, RAM 70 B (3 %); la de banco con `-DDIAG_SERIAL=1` da 6336 B / 249 B. **Sin flag de encapsulado el build falla.** Desensamblado: TQFP-32 carga `ADMUX = 0xC7` (ADC7) y DIP-28 `ADMUX = 0xC3` (ADC3); el acceso a `DIDR0` (0x7E) **aparece solo en el binario de DIP-28**; `CLKPR = 0x01` con fuente de 8 MHz y `0x02` con 16; y `mcuStrapCoincide()` tiene **2 sitios de llamada** en los dos binarios (principio 1: no alcanza con que la función exista). `tools/check_pinout_nodo_v3.py` reescrito para cruzar **los dos encapsulados** (mapa físico de cada uno desde el datasheet, comentario con "pin TQFP/DIP", desvíos de banco declarados y verificados contra el netlist, y el canal de ADC de la pila validado por variante): **OK**, y `--mutantes` mata **12/12** (los 5 viejos + 7 nuevos: ADC7 en DIP-28, la pila encima del `CS`, canal remapeado sin el pin, canal del TQFP cambiado, pin DIP mal escrito, el encapsulado fijado en el fuente, y el `#error` comentado).
  - **Hallazgo de paso (principio 1 al revés): `PB6`/`PB7` (XTAL1/XTAL2) quedaban como dos entradas flotando** en los dos encapsulados —el nodo no lleva cristal— y una entrada flotando paga corriente en power-down (S14.11), contra un reposo de 29 µA. Ahora van con pull-up; con cristal el driver del oscilador se queda con los pines y la línea es inocua, así que es correcta en los tres caminos de reloj.
  - **El receptor: el README y el PORTFOLIO están viejos.** Dicen "heartbeat-only: falta Realtime + LCD + buzzer + gateway HTTP". Leído el fuente (`esp_rx_receptor.ino`, 49 KB, `3.6.7-RX-palways`): **LCD 20×4, buzzer, LED, gateway HTTP :80 con `POST /reading` + `GET /health`, cola asíncrona de reenvío a Supabase, `PATCH local_ip`, OTA cloud + `ota_rescue`, factory reset por botón y chequeo de MAC — todo eso YA ESTÁ**. Los comandos van por **polling de 60 s** en vez de Realtime, lo cual con 2 nodos a 300 s no es un bloqueante. **Lo que falta no es una feature: es la radio entera** — `grep -in "lora|sx127"` sobre el `.ino` y su `config.h` da **cero líneas**. El RX de hoy es un gateway WiFi para los emisores ESP32. Tamaño del laburo: driver SX1278 (chico-medio, se porta `sx1278.cpp` del nodo, que es el mismo chip), parser de la trama y **mapearla por donde hoy entra `handlePostReading()`** (chico, y así el LCD, el buzzer, el "stale", la cola y Supabase funcionan sin tocarlos), pines en `config.h` (chico). **El riesgo real es contestar dentro de la ventana de 200 ms del nodo**: `fwdWorkerTick()` bloquea 2-3 s posteando a Supabase y la rama de reconexión tiene `delay(2000)`/`delay(3000)` — un paquete que caiga ahí llega tarde y el nodo ya se durmió, o sea un downlink que anda el 90 % de las veces y falla sin patrón. Salida sana: **la radio en su propia tarea FreeRTOS** (el RX vive en 220 V y el ESP32 tiene dos cores). No implementado: primero la etapa 2 mide la ventana y @comms cierra el formato. **Y el ESP32 con LoRa de la etapa 2 ES el prototipo del receptor.**
  - **Deuda anotada**: `NOTAS_CALCULO` §4 lista PC2 entre los "sin uso, marcados NC" — sigue siendo cierto a nivel PCB (no hay red), pero el firmware ahora lo maneja como strap; que lo refleje @esquematico. Y actualizar el `README.md` del RX y el PORTFOLIO, que describen un firmware que no es el que está.
  - **NADA FLASHEADO — no entra en la tabla de versiones.** Próximo paso: etapa 1 completa (fusibles grabados y **releídos**, banner por serial a 9600 con `mcu=DIP28 adc_vbat=3`, y **sacar el cable del strap para ver al firmware negarse a arrancar**, que es la única forma de probar que esa red corre).
  - **Addendum (mismo día): las etapas 1 y 2 hubo que reescribirlas.** @hardware y yo escribimos `BRINGUP_BANCO.md` en paralelo y se pisaron; sobrevivió su sección H y mis etapas 3-5 + receptor, se perdieron **1 y 2** (justo las dos que Matías puede armar sin esperar compras). Repuestas e insertadas **después de H y antes de "El receptor"**, sin tocar una línea de lo ajeno (el script de inserción verifica prefijo y sufijo byte a byte). Archivo: **1038 líneas**, con las 5 etapas presentes. Aprovechando el rehacerlas quedaron **mejor cruzadas con la sección H**: la **tabla de traducción TQFP-32 → DIP-28** completa (H3.1-4 la delega explícitamente en @firmware), los dos olvidos clásicos del perfboard como checkpoint de téster **antes** de dar tensión (**AVCC pata 20** y **AREF pata 21 con 100 nF**), el kit de rescate de cristal de H3.2 usado en el punto de la trampa del chip con bootloader, y la resolución de H3.4: **el USBasp de 5 V sólo fuera del circuito; el ESP32-ISP a 3,3 V es el único que se puede dejar pinchado con el nodo armado**, que es lo que hace falta para iterar firmware en las etapas 2-4. En la etapa 2 entraron las dos advertencias físicas de H3.5 (**8 alambres Kynar con fijación mecánica — vale para banco, no para planta** y **no hay adaptador 2,0→2,54 en ML AR**; **nunca alimentar sin antena**) y un checkpoint nuevo que sale de ahí: **2.f, mover cada alambre con el enlace corriendo sin perder tramas** — si eso no pasa, todo lo demás de la etapa es ruido. **Lección de coordinación, no técnica: dos agentes en el mismo archivo necesitan bloques con dueño declarado y punto de inserción fijo, o se pierde trabajo.**

- 2026-09-02 (tercer turno) [GALGAS nodo v3 — **la barrera de fusibles: de 3 definiciones / 0 usos a verificación en ejecución**, + H-16 (PINOUT §8 contra la rev D). Compila, nada flasheado. Sin commit] — Hallazgo de @verificador auditando la rev D: `FUSE_LOW/HIGH/EXT_ESPERADO` estaban en `config_nodo.h` **y no los leía nadie** — el mismo patrón del header huérfano que yo mismo reporté (H35), pero sobre la constante que protege el producto entero, y en el mismo firmware que sí implementa `mcuStrapCoincide()` en ejecución para un riesgo mucho más barato. La magnitud con los números de la rev D: **fusible bien → transmite hasta OCV 2,56 V; fusible mal → se muere a OCV 3,46 V, con la pila casi nueva. 900 mV de OCV = la meseta entera.** Y de los tres estados a los que se llega sin querer **ninguno es el correcto**: chip virgen `EFUSE 0xFF` (BOD apagado), "burn bootloader" `0xFD` (BODLEVEL=101, el bug), tipeo mal.
  - **Implementación**: `fusiblesVerificar()` en `energia.cpp` lee los fusibles **reales** con `boot_lock_fuse_bits_get()` y compara. **Con las interrupciones apagadas**, porque por dentro es escribir `SPMCSR` con `BLBSET|SELFPRGEN` y ejecutar `LPM` **dentro de los 3 ciclos siguientes** (los bits se auto-limpian a los 4): una ISR en el medio devuelve basura. Se llama **una vez** en el arranque, antes del banner.
  - **Máscaras, que no son adorno**: del `EFUSE` **solo bits 2..0 (BODLEVEL)** — comparar el byte entero daría falsa alarma entre `0xFE` y `0xFF`. Del `HFUSE` **todo**: `SPIEN` (si se desprograma el nodo **no se reflashea nunca más**), `WDTON` (si se programa, el WDT queda en reset permanente y **el sleep por interrupción de watchdog deja de funcionar**), `EESAVE`, `BOOTRST`. **`CKDIV8` se chequea aparte de la máscara** porque vale en las tres opciones de reloj: si queda programado, `millis()`, el baudrate y los tiempos del ciclo mienten **todos por 8**.
  - **Tres veredictos, no dos.** `OK` → normal. `MAL` → **no mide, no enciende el puente, no publica telemetría**; repite cada ciclo una **trama de diagnóstico `0xA4` con los tres fusibles CRUDOS** (no un "ok/mal": el que la recibe sabe qué regrabar sin subir a la máquina), **a potencia de back-off y trama corta a propósito** — un nodo con el BOD mal grabado es justo el que se resetea por su propio pulso de TX, y lo que importa es que el aviso salga —, y parpadea **5 destellos cortos** (1 = sin radio, 3 largos = encapsulado, 5 cortos = fusibles: los tres distinguibles a ojo). `ILEGIBLES` (los tres bytes en `0xFF`, imposible en un chip grabado por ISP porque `SPIEN` estaría sin programar) → **opera igual y avisa**: una barrera que apaga un nodo sano por una lectura en la que no confía es peor que la falla que evita.
  - **Todo arranque manda una trama de diagnóstico**, salga como salga el veredicto: es la cédula de la unidad (fusibles + `MCUSR` + encapsulado + versión + `cal_valida`) y la única forma de saber cómo quedó fusilada **sin enchufarle un ISP**. Misma decisión que hacer viajar el `MCUSR`, por la misma razón. Cuesta una trama corta por boot. Tipo de trama propio (`0xA4`): **no toca el contrato de la trama normal, que es de @comms**.
  - **Evidencia del desensamblado** (lo que prueba que corre, no que existe): `cli` → `ldi r24,0x09` (`BLBSET|SELFPRGEN`) → `Z = 0x0000 / 0x0003 / 0x0002` → **`sts 0x0057` (SPMCSR) inmediatamente seguido de `lpm`**, sin una instrucción en el medio, ×3 → resultados a `s_fus[0..2]` → `out 0x3f` (SREG restaurado). Y las cuatro comparaciones: **`cpi 0xE2`** (LFUSE), **`cpi 0xD1`** (HFUSE), **`andi 0x07` + `cpi 0x06`** (BODLEVEL enmascarado = 110b) y **`sbrs r18,7`** (CKDIV8, aparte de la máscara). Compila con `--warnings all` y **cero warnings propios**: TQFP-32 **5008 B (16 %)**, DIP-28 **5010 B**, RAM 77 B; la de banco con serial 7200 B / 254 B.
  - **H-16 cerrado, y con un hallazgo que lo agranda: la rev D adoptó el DIP-28 como encapsulado de PRODUCCIÓN.** `U2` = `ATmega328P-PU`, footprint `DIP-28_W7.62mm_Socket`. O sea que **el "armado de banco" ES el nodo**, no una maqueta. Además: **`V_BAT_SENSE` ya está en PC3/pin 26 en la placa** (el remapeo que yo había hecho para el banco es el de la PCB) y **el strap de PC2 se pobló**: `RSTRAP1` de 1 kΩ a GND, así que la rev D lee BAJO igual que el banco — el strap dejó de significar "PCB vs banco" y significa lo que siempre tuvo que significar: **qué encapsulado es**. Corregido en `PINOUT.md` §8 y en `config_nodo.h`: **Q1/Q3 son 2N3904** con resistencias de base (`RB_Q1` 22k, `RB_Q3` 100k) y los comentarios decían "Q1.G" y "canal N"; **Q2 y RG1 salieron**, el supercap es de **un cuerpo** (`CSC1`) y **`/V_SC_MID` ya no existe**.
  - **Deuda que sale de eso y NO es mía**: `ESPERA_TX_S = 30` salía de `3·tau` con **dos** supercaps en serie. Con un solo cuerpo **la constante cambió y hay que recalcularla** — es de @energia. Queda marcado en `PINOUT.md` §8 y el número sigue como está hasta que lo firme.
  - **El chequeador ahora cruza cada variante contra SU placa**: rev D (`nodo_galga_v3.net`, DIP-28) con cruce completo —pines, conectividad, referencias citadas en los comentarios, NC y barrido inverso con su propio mapa de patas de alimentación, que **no son las mismas que en TQFP**— y rev C (`nodo_galga_v3_revC.net`, TQFP-32, archivada) con lo que sigue importando: números de pin físico y canal de la pila. Y una sección **BARRERAS** nueva que es la respuesta directa al hallazgo: verifica que cada símbolo esté **usado** y no solo definido, descontando las líneas de comentario (nombrar algo en un comentario no lo ejecuta). Tres cosas que aprendí escribiéndolo, porque los mutantes me las mostraron vivas: (1) pedir que "alguien le asigne `EST_FUSIBLES_MAL`" no sirve — **el bucle del propio estado se lo asigna a sí mismo** y queda inalcanzable desde afuera; ahora se exige que **toda asignación de estado que consulte `fusiblesEstado()` lleve a él**, y que haya al menos dos rutas (arranque y re-arranque); (2) pedir el símbolo `boot_lock_fuse_bits_get` no sirve si hay tres llamadas y se rompe una: se exigen los tres `GET_*_FUSE_BITS`; (3) `armarTramaDiag` tiene que aparecer **≥2 veces** (definida **y** llamada). Y **se verifican los valores esperados**, que es como mueren las barreras: la forma más cómoda de callar un check que falla es cambiar el esperado por lo que tenga el chip — el script exige `BODLEVEL=110`, `EESAVE` y `SPIEN` programados y `WDTON` sin programar. **`--mutantes` mata 22/22** (los 12 viejos + 10 nuevos). Los mutantes que tocan `.cpp`/`.ino` mutan el fuente real y **restauran en un `finally`**; verificado corriendo el check limpio después.
  - **Etapa 1 de `BRINGUP_BANCO.md` actualizada** (único archivo que toqué ahí): la tabla de los tres `EFUSE` a los que se llega sin querer, los números de la rev D, y **dos checkpoints nuevos** — **1.i** (el banner tiene que decir `fusibles L=0xE2 H=0xD1 E=0xFE -> 0 motivo=0x0`) y **1.j**, que es el ensayo que convierte el hallazgo en algo verificado: **grabar a propósito `efuse 0xFD`**, ver al nodo negarse a operar con 5 destellos cortos y la trama `0xA4` por aire, y volver a `0xFE`. Más filas de diagnóstico por `motivo` (`0x4` BODLEVEL, `0x8` CKDIV8, `0x2` HIGH, `0x1` LOW). **NO toqué** `hardware/kicad/` ni los cuatro documentos de rev D.
  - **Lo que sigue siendo verdad y hay que decirlo**: todo esto prueba la **decisión**, no el **efecto**. Nadie vio todavía a un ATmega real leerse los fusibles con `boot_lock_fuse_bits_get()` desde la sección de aplicación. **NADA FLASHEADO.** El checkpoint 1.j es exactamente el ensayo que falta.

- 2026-09-02 (cuarto turno) [GALGAS nodo v3 — **@energia firmó E11: límite de ráfaga de 120 s (bug que el banco no ve), arranque en frío 30→90 s, y `ESPERA_TX_S` deja de ser una protección**. Compila, nada flasheado. Sin commit] — Los tres cambios salen de que **el diodo de bloqueo de la rev D eliminó la constante de tiempo**: el diodo es logarítmico, para pasar 1 mA necesita 135 mV de déficit y el droop de medición sólo deja 36,8 mV, así que la recarga **queda clavada en ~0,19 mA** (se autopolariza para pasar la corriente media del nodo) y la recuperación es **lineal a 0,20 mV/s**, no exponencial.
  - **`ESPERA_TX_S` = 30, mismo número y fundamento nuevo.** Corregido el comentario, que era el pedido explícito: **no es "3·τ" — no hay τ**. Estos 30 s compran **6,0 mV**, no los ~47 que sugería el relato viejo: el **4 %** del margen de fin de vida, no el 40 %. **R3 dejó de ser una protección**, y lo escribí en el código en primera persona para el que lo lea dentro de un año: *"si estás buscando qué sostiene la última TX, no es este 30: es BODLEVEL=110"*. Se mantiene porque moverlo da ±5 mV sobre un margen de 138 mV y obliga a re-verificar firmware ya probado.
  - **`BODLEVEL=110` pasó de importante a imprescindible** (E11.3): con piso de 2,9 V la ESR requerida sale **negativa** — ni con ESR cero el nodo llega al corte transmitiendo. **Ninguna medición ni ningún componente puede salvar un fusible mal grabado.** Eso convierte la barrera de fusibles del turno anterior en la protección del producto, no en una mejora; queda dicho en `energia.h` y en la etapa 4.
  - **`ARRANQUE_FRIO_S` = 90 s** (constante nueva; antes la rama de `PORF` reusaba `ESPERA_TX_S`). Los 30 cableados **y** los 60 propuestos estaban los dos mal: ninguno incluía el diodo. El capacitor recién puede **medir a los 53 s**, TX +17 dBm a los 66 y +20 dBm a los **74**. 90 = 75 + 20 %. El motivo que puse en el comentario es el práctico: **a los 30 s el nodo ni siquiera puede medir**, así que uno recién instalado y consultado a los 30 s **parece roto**, con la ventana de la parada corriendo.
  - **El límite de ráfaga: `RAFAGA_MIN_S = 120`, y cómo quedó.** **Chokepoint único** en `EST_MEDIR`, que es el único punto por el que pasan **todos** los disparadores — el periódico, el reed y el que se agregue mañana. No lo puse en el camino del self-trigger justamente para que un disparador nuevo **no lo pueda saltear por olvido**. **Límite duro, no dependiente del estado de pila**, y la razón es concreta: un límite que dependiera de `VBAT` dependería de la referencia interna sin calibrar (±10 %), y **el caso en que el firmware sobreestima la pila es justo el caso en que dejaría pasar la ráfaga que la hunde**; un número fijo con 25 % de margen sobre el peor caso medido (96 s pasado el corte) no tiene ese modo de falla. **El disparo no se pierde: se posterga** — si no se puede, duerme lo que falta y vuelve a entrar.
  - **El detalle de implementación que era el riesgo real: el reloj.** No hay RTC y **`millis()` no avanza en power-down**, o sea que el nodo pasa dormido casi todo el intervalo que hay que medir. El tiempo lo lleva `dormirSegundos()` sumando los segundos **efectivamente dormidos**. Eso **subestima** el transcurrido (no cuenta los pocos segundos despierto) y subestimar es el lado seguro: el límite espera de más, nunca de menos. Y `s_ult_evento_s` arranca "permitido": si el primer evento tras un reset tuviera que esperar 120 s, **un reset por watchdog dejaría al nodo mudo un ciclo entero por nada**.
  - **Bandera huérfana cerrada de paso**: `g_reed` la prendía la ISR y **no la consumía nadie** (mi propio principio 1, en mi propio código). Ahora se consume en `EST_DORMIR` con las interrupciones apagadas —para no perder un evento que llegue justo en el medio— y pasa por el mismo guard. **Falta** habilitar el despertar por PCINT2 durante el sleep y definir qué trama produce un evento de reed: **no es de este dominio** (@comms/@muestreador), y cuando se cablee **no puede saltear `rafagaPermitida()`** — lo vigila el checker.
  - **Evidencia del desensamblado** (el pedido: que se vea que corre, no que existe). Arranque en frío: `sbrs r24,0` sobre el bit `PORF` → **`ldi r24, 0x5A` (90)** → `call dormirSegundos`. Límite de ráfaga, inlineado en `loop()`: resta de 32 bits `s_reloj_s − s_ult_evento_s` (`sub`/`sbc`×3) → **`cpi r20, 0x78` (120)** + `cpc`×3 → `brcc` al camino permitido, que es el que escribe `s_ult_evento_s` (o sea `rafagaMarcarEvento()` **sólo** en la rama que mide); y en la rama bloqueada, **`ldi r24, 0x78` − transcurrido → `call dormirSegundos` → `rjmp` de vuelta al tope de `EST_MEDIR` sin medir**. O sea: el guard bloquea, duerme el resto y no marca evento. Compila con `--warnings all`, cero warnings propios: **TQFP-32 5188 B (16 %) / DIP-28 5202 B**, RAM 86 B; banco con serial 7436 B / 263 B.
  - **Chequeador**: 6 barreras nuevas y 4 chequeos semánticos —que `rafagaPermitida()` **bloquee** y no sólo se consulte, que `rafagaMarcarEvento()` esté **antes** de `medir()`, que `rafagaPermitida()` compare **contra la constante** y no contra un número escrito a mano, que el reloj **cuente el tiempo dormido**, que la rama de `PORF` use `ARRANQUE_FRIO_S`, y que `g_reed` **se limpie fuera de la ISR**— más **piso a los valores** (`RAFAGA_MIN_S >= 96`, `ARRANQUE_FRIO_S >= 75`), por lo mismo que con los fusibles: bajar la constante es la forma cómoda de hacer callar un check que molesta. **`--mutantes` mata 31/31** (los 22 previos + 9 nuevos). **Dos mutantes me sobrevivieron primero y los dos eran huecos reales**: pedir el símbolo `RAFAGA_MIN_S` en el archivo no sirve porque también está en el comentario y en `rafagaFaltanS()` (hay que mirar **el cuerpo de `rafagaPermitida()`**), y pedir `g_reed = false` no sirve porque **ese texto también es la declaración de la variable** (hay que exigir ≥2 apariciones). Tras correr los mutantes el árbol queda intacto (restauran en `finally`; verificado con el check limpio después).
  - **Docs**: `PINOUT.md` §10 nueva (los tres tiempos, el bug de ráfaga y qué lo sostiene) y §8 actualizada — la deuda que yo había dejado marcada para @energia quedó **firmada y cerrada el mismo día**. En `BRINGUP_BANCO.md` **sólo la etapa 4**: arranque en frío 90 s (con "cualquier checklist que diga 30 s o un minuto está viejo"), R3 reencuadrado, `BODLEVEL` imprescindible, y el **ensayo 4.j** — 6 disparos cada 10 s con la pila **emulada a fin de vida** (OCV 3,20 V), aprueba si sale ≤1 evento cada 120 s y `VOUT` nunca baja de 2,70 V — con su **contraprueba 4.k a 3,67 V**, que es la que demuestra **por qué el bug es invisible en el banco**. **NO toqué** `hardware/kicad/` ni los cuatro documentos de rev D.
  - **NADA FLASHEADO.** Lo que falta sigue siendo el efecto, no la decisión: los ensayos 1.j (fusibles), 4.j y 4.k (ráfaga) son exactamente lo que convierte esto en verificado.

- 2026-09-02 (quinto turno) [GALGAS nodo v3 — **el arranque en frío deja de ser un número y pasa a ser una condición medida: el nodo mide su propio riel bajo carga**. Compila, nada flasheado. Sin commit] — @energia rehizo el arranque (E12) y **mi `ARRANQUE_FRIO_S = 90` del turno anterior no alcanza en 6 de 7 casos realistas**: supercap en el +30 % de tolerancia 107 s, frío 225 s, **celda pasivada de estante 216 s** —que **es el caso normal de instalación**, la pila sale de un cajón y la hoja de EEMB lo advierte—, pila a media vida 647 s. El error de fondo es el mismo que el de usar `VBAT` como prueba de listo: **se puso un número donde hacía falta una condición**.
  - **Cómo quedó la condición de listo.** El ATmega mide **su propia alimentación** sin un solo componente nuevo: bandgap de 1,1 V **contra AVcc** (`ADMUX = (1<<REFS0) | 0b1110`), que devuelve la salida del LDO. Se toma la referencia **en reposo**, se transmite a **+20 dBm** —el peor caso de consumo **es** la prueba, no un proxy— muestreando el riel **mientras el PA está encendido**, y se compara: cargado 0,0 %, a medias 9,9 %, casi vacío 12,8 %. **Umbral ≤ 1,5 %.** El nodo sondea cada 30 s (`EST_ARRANQUE_FRIO`) y no publica telemetría hasta pasar; típico 75 s, hasta ~10 min con celda pasivada.
  - **El muestreo durante la TX se hizo con un hook** (`sx1278HookDuranteTx`, mismo patrón que `otaAntesDeFlashear` del stitch): la radio no sabe nada de energía, y el único lugar del código donde el PA está encendido es el lazo de espera de TxDone. Se engancha antes de transmitir y se suelta después.
  - **El modo de falla peligroso de esta medición, y cómo lo cerré**: si el hook no se llegara a llamar, `adc_tx` quedaría igual al de reposo, la caída daría **0 % = LISTO sin haber medido nada**, y el nodo se declararía listo con el capacitor vacío. Por eso `vccProbeCaidaPorMil()` **cuenta las muestras tomadas durante la TX y devuelve el peor valor (1000) si no hubo ninguna**. Lo encontré escribiendo la función, no después.
  - **Y el detalle de signo que invierte el resultado si se lo pasa por alto**: el código del ADC es **inversamente** proporcional a `VCC`, así que el peor caso durante la TX es el código **más alto** y la caída sale como `1 − adc_reposo/adc_tx`.
  - **Lo que esto resuelve gratis**: como el criterio es una **relación entre dos lecturas del mismo chip**, el **±10 % del bandgap se cancela** y **no hay que calibrar por unidad** para esto. Ojo con la lectura fácil: **el escalón de potencia sigue necesitando `vref_mv` medida**, porque usa un umbral **absoluto** en mV. Son dos usos distintos de la misma referencia y sólo uno se salva.
  - **`VBAT` degradado a telemetría y escalón de potencia, nunca más como prueba de listo**, con el motivo escrito en `config_nodo.h` para que no vuelva: cuelga de `+BATT`, **antes** de `RSC`, así que `VBAT = V_SC + I_carga·RSC` y **el error va siempre en la dirección peligrosa** — infla la lectura justo cuando el nodo no está listo. La ventana de falso "pasa" dura **53 s**; en el peor punto declararía "3,19 V, listo" con el capacitor al 64 %.
  - **Contador de arranques: 1 byte, en EEPROM.** Va **fuera** de la struct `CfgNodo` a propósito: cambia en cada boot y meterlo adentro obligaría a reescribir toda la struct y su CRC cada vez, gastando las celdas de la calibración y dejando una ventana en la que un brownout a mitad de escritura se lleva puesta la calibración. Viaja en la trama de diagnóstico, que es **mía** (`0xA4`): **no toqué el contrato de la trama normal** — eso es de @comms, y le queda pedido que el byte entre también ahí.
  - **La aceptación la cierra el propio nodo**: `EST_ARRANQUE_CONFIRMA` manda la segunda trama a +20 dBm **120 s después**, así que las **dos tramas separadas ≥120 s con el mismo contador** salen sin que el instalador tenga que hacer nada. Si la segunda no pasa, **no se da por bueno el arranque y se vuelve a sondear**: que la primera pase y la segunda no es exactamente el caso que la prueba existe para cazar. Los sondeos fallidos también salen al aire, con `listo=0` y la caída medida — deliberado: le muestran al instalador que el nodo está vivo y cargando, sin poder confundirse con una aceptación.
  - **Decisión de orden que evita un brownout garantizado**: en arranque en frío **no** se manda la cédula de boot al toque. A t=0 el capacitor está vacío y esa TX es justo la que garantiza el reset; se va derecho al sondeo, que duerme 30 s antes de transmitir nada.
  - **Evidencia del desensamblado.** `vccProbePreparar`: `ADCSRA &= ~ADEN` → **`ldi r24, 0x4E` → `sts 0x007C` (ADMUX)** = `(1<<REFS0)|0b1110`, o sea **bandgap contra AVcc** → `ldi 0x85 → sts 0x007A` (ADCSRA `/32`) → `call delay(2)` → `call vccConv` (la dummy). Y en `vccProbeCaidaPorMil` **lo primero que hace es `lds s_n_tx` + `breq` al retorno de `0x03E8` = 1000**: sin muestras, el peor valor y no listo. Después los guards de cero, `cp reposo,tx`/`brcc` y la cuenta real: `__umulhisi3` con `0x03E8` (1000·reposo), `__udivmodsi4` (÷ tx) y `1000 −` el resultado. Conviven las dos configuraciones sin pisarse: **`0x4E` para el riel y `0xC3` (ADC3 + referencia interna) para `VBAT`**. Compila con `--warnings all`, cero warnings propios: **TQFP-32 5820 B (18 %) / DIP-28 5836 B**, RAM 96 B; banco con serial 8208 B / 273 B.
  - **Chequeador: 9 barreras y 6 chequeos semánticos nuevos, `--mutantes` 39/39.** Verifica que el sondeo tome la referencia **en reposo antes** de transmitir, que **todas** las TX de sondeo enganchen el muestreo, que el `ADMUX` sea AVcc+bandgap **y no** la referencia interna, que la rama de `PORF` **no** vuelva a terminar en una espera fija, que el contador de arranques **suba y viaje**, y pone **techo al umbral** (`RIEL_CAIDA_MAX_PERMIL ≤ 15`) y **piso** a `TX_DBM_SONDEO` y `ARRANQUE_CONFIRMA_S`. **Cinco mutantes me sobrevivieron primero y los cinco eran huecos reales del chequeador**, con dos lecciones que ya generalicé: (1) **comentar una línea es la forma más común de desactivar una barrera, y mis chequeos miraban el texto crudo** — ahora todo pasa por `solo_codigo()`, que descarta las líneas comentadas; (2) **buscar un símbolo no sirve cuando hay varios sitios que lo usan**: romper uno deja los otros y el check pasa en verde. Se arregló contando (enganches del hook contra TX de sondeo) y exigiendo **por estado** (que `EST_TX_DIAG`, `EST_ARRANQUE_FRIO` y `EST_ARRANQUE_CONFIRMA` manden cada uno su trama).
  - **Docs**: `PINOUT.md` §10.3 nueva (la condición, los dos detalles de implementación que son la diferencia entre que funcione y que mienta, el criterio de aceptación y sus tres puertas). En `BRINGUP_BANCO.md` **sólo las etapas 1 y 4**: en la 1, el checkpoint **1.g** ahora exige ver **`boots` subir en cada reset** —es la pieza sin la cual el criterio de aceptación no vale— y quedó dicho que el instalador ya no espera ni mide; en la 4, el arranque en frío reescrito, `VBAT` degradado, el criterio de aceptación con sus tres puertas y los ensayos **4.c** (supercap descargado), **4.c-bis** (**50 Ω en serie = celda pasivada; es el ensayo que la espera fija de 90 s reprobaba**) y **4.c-ter** (los sondeos fallidos dan 9-13 % contra ≤1,5 %: la separación de casi un orden de magnitud es real). **NO toqué** `ENERGIA_REV_D.md` ni `hardware/kicad/`.
  - **Confirmado de E12.5 y ya estaba bien implementado**: los 30 s de `ESPERA_TX_S` van **dentro** de los 120 de ráfaga, no se suman — el evento se marca al **empezar** a medir, así que el ciclo `medir → 30 s → TX` cuenta como un solo evento.
  - **NADA FLASHEADO.** Sigue faltando el efecto, no la decisión: 1.j (fusibles), 4.j/4.k (ráfaga) y ahora 4.c/4.c-bis (arranque en frío por condición, con la celda pasivada emulada) son los ensayos que lo convierten en verificado.

- 2026-09-03 [GALGAS nodo v3 — **la trama no llevaba identificador de nodo: sin eso el producto no funciona.** Contrato de enlace de @comms implementado. Compila, nada flasheado. Sin commit] — @comms encontró el defecto: `armarTrama()` empezaba en el tipo y seguía en el ciclo, **sin un solo byte que dijera si era del nodo A o del B** — con dos nodos idénticos, en la misma frecuencia, y con **toda la detección del sistema basada en comparar la cadena A contra la B**. Los emisores ESP32 se identifican por `device_id` en el JSON; el nodo LoRa no tenía equivalente.
  - **Cómo quedó `nodo_id`: en EEPROM, con bump `CFG_VERSION` 1→2 y migración de verdad.** No es un `#define` por binario, y el argumento es el que ya pagué una vez con el encapsulado: **dos binarios distintos son dos formas de flashear el nodo equivocado** — por eso existe el strap de PC2, y un `#define NODO_ID` habría metido el mismo agujero por la puerta de al lado. Default de fábrica **`0x00` = SIN ASIGNAR**: el nodo transmite igual y el receptor lo muestra como `NODO SIN ID` sin mapearlo a ninguna cadena. **Falla ruidosa, no silenciosa**, y el banner lo dice en la primera línea.
  - **La migración no era un trámite y casi me lleva puesta la calibración.** El código viejo hacía `EEPROM.get()` con el tamaño de la struct **nueva** y validaba el CRC sobre ese tamaño: con un layout más grande, los campos que la v1 no tenía habrían venido de EEPROM sin escribir (`0xFF`), el CRC habría dado mal y el nodo se habría ido a defaults — o sea que **un bump de schema mal hecho se lleva puesto `vref_mv`, que se mide por unidad en el bring-up y para el cual existe `EESAVE`**. Ahora hay `struct CfgNodoV1` (sólo para migrar) y `migrarV1aV2()`, que lee el blob viejo **con su propio tamaño**, valida **su** CRC y copia los cuatro campos que existían. El checker exige que `cfgCargar()` **llame** a la migración, no que exista.
  - **Los tres parámetros de enlace que estaban mal.** (1) **433,000 MHz está FUERA de la banda ISM** (433,05–434,79) → **433,500**, dentro de banda y 420 kHz por debajo de 433,92, donde vive el 95 % de los telemandos. (2) **No se escribía sync word**, o sea que el enlace corría con el default `0x12` — **que es el que usa RuView, también 433 MHz**: dos proyectos de la misma casa escuchándose entre sí, y el síntoma habría sido "llegan tramas raras" en un banco donde a nadie se le ocurre sospechar del otro proyecto. Ahora `0x2D` (ni `0x12` ni `0x34`, que es de LoRaWAN público). (3) Tampoco se escribía el **preámbulo**; ahora va explícito.
  - **Hallazgo para @comms: el `RegFrf` de la tabla C1 está 41 kHz corrido.** El doc da `0x6C5F5C` = **433,459 MHz**; la cuenta correcta es `433,5e6 × 2^19 / 32e6 = 7 102 464 = 0x6C6000`, exacto (el paso intermedio del doc, "7 100 006,4", tiene un desliz). Con BW125 esos 41 kHz se comen buena parte del presupuesto de error de frecuencia del receptor. **El nodo calcula el Frf y no copia la constante**; si el receptor la copia tal cual, los dos extremos quedan desalineados y el enlace fallaría por un motivo que nadie estaría buscando. Está avisado en `PINOUT.md` §11.3 y en la etapa 2.
  - **Estadísticas sin mandar las 60 crudas** (`medstats.h/.cpp`, nuevo): media, desvío, mínimo y máximo calculados **al vuelo**, 6 bytes contra 240 B de muestras. Dos decisiones numéricas porque esto es un AVR de 8 bits: todo se acumula **respecto de la primera muestra** —así la varianza no sufre la cancelación catastrófica de `E[x²]−E[x]²` con números grandes y parecidos— y `d_min`/`d_max` viajan como **delta respecto de la media** para entrar en `int16` en vez de pedir dos `int32` más por ciclo para siempre. Raíz entera por Newton: traer el punto flotante de AVR para un número que después se satura a 16 bits no paga.
  - **Downlink `0xC0`, y la regla que toca al resto del firmware**: el nodo **NUNCA ejecuta el comando dentro de la ventana**. Lo encola, cierra la ventana, duerme y lo ejecuta **al empezar el ciclo siguiente, después del guard de ráfaga**. El motivo es concreto: la ventana termina con el nodo yéndose a dormir, así que ejecutar ahí **saltearía `rafagaPermitida()`** — un comando por aire podría fabricar la ráfaga que hunde el riel a fin de vida. El ACK es `ack_eco` en el uplink siguiente, no un mensaje aparte. Implementados NOP/SHUNT_CAL/MODO_BANCO(con timeout)/SET_POTENCIA/SET_CICLO(persiste)/CEDULA/REBOOT(con arg mágico). **`VOLCADO_CRUDO` (0xA5) NO está implementado y no se ecoa a propósito**: el receptor lo reintenta 5 ciclos y registra el fallo — es la lección del 08-28 (las tres etapas de calibración que contestaban `ack=OK` sin hacer nada), **un NACK honesto vale más que un OK vacío**. Pedido a @comms para la v2: un código de NACK explícito, porque hoy "no implementado" y "se perdió el downlink" se ven igual desde el receptor.
  - **Jitter anti-colisión**: `(nodo_id*3 + ciclo*7) & 0x07`. **Determinístico y no `random()`**: en un AVR sin fuente de entropía los dos nodos arrancarían con la misma semilla y el "azar" sería idéntico en los dos. Cuesta cero energía (es tiempo dormido).
  - **Evidencia del desensamblado**: la cabecera escribe `b[0] = 0xA4`, después **`lds r24, s_cfg+0xF` → `std Y+1`** (el `id` sale de la config cargada de EEPROM, no de una constante) y `s_ciclo` big-endian en `b[2]/b[3]`. En `sx1278Init`: **`reg 0x06 = 0x6C`, `0x07 = 0x60`, `0x08 = 0x00`** → `RegFrf = 0x6C6000` = **433,500 MHz exacto** (el valor del doc habría dado 41 kHz menos); **`reg 0x39 = 0x2D`** (sync word) y **`reg 0x20 = 0x00`, `0x21 = 0x08`** (preámbulo de 8). Compila con `--warnings all`, cero warnings propios: **TQFP-32 8468 B (27 %) / DIP-28 8484 B**, RAM 138 B (6 %); banco con serial 11256 B / 331 B.
  - **Chequeador: 14 barreras nuevas, 5 chequeos semánticos y `cruzar_largos_de_trama()`.** Este último es el que más falta hacía y es específico de un protocolo por offsets: **cuenta los bytes que el código escribe en cada trama** y los cruza contra los largos declarados. Agregar un campo y no mover el largo —o al revés— **no rompe la compilación, no rompe la TX**, y del otro lado corre todo un byte: la medición sigue llegando y **sigue teniendo pinta de número**. Es el error que se descubre semanas después mirando datos raros. Más: que el `id` salga de `s_cfg.nodo_id`, que `ejecutarComandoPendiente()` **no** aparezca en el estado de la ventana y **sí** en el del ciclo, que el parser tenga las **tres** barreras (eco_seq, no-broadcast, clave de sitio), y valores con piso/techo (frecuencia dentro de ISM y lejos de 433,92; sync word ≠ `0x12` y ≠ `0x34`). **`--mutantes` mata 52/52.** Cuatro me sobrevivieron primero, y los cuatro eran la misma lección ya conocida en versión nueva: **buscar un símbolo no alcanza cuando hay dos frames o dos funciones que lo usan** (el contador de arranques estaba también en la cédula; la migración estaba definida pero sin llamar), y **un chequeo que lee el archivo del disco en vez del que se está verificando no ve los mutantes de configuración** (los largos de trama).
  - **Etapa 2 de `BRINGUP_BANCO.md` (sólo esa)**: tabla de parámetros del contrato con los tres que estaban mal, el aviso del `RegFrf`, "asignar los `id` antes de la prueba 1", y **el 2.d reescrito**: aprueba con 10 de 10 **y con el Δt logueado entre 10 y ~30 ms**, porque **un receptor que contesta demasiado rápido falla igual que uno que contesta tarde** —si transmite antes de que el nodo esté en RX no hay preámbulo que detectar y el síntoma es idéntico—, lo que llevaría a agrandar la ventana, que es lo contrario de lo que hace falta y cuesta 26 días de autonomía cada 150 ms. Regla escrita: **si 2.d falla, nunca se agranda la ventana primero; primero se mira el Δt**. Más **2.d-bis** (el comando se ejecuta en el ciclo siguiente y vuelve en `ack_eco`), **2.g** (dos nodos con id distinto, el sniffer separa las cadenas y cuenta huecos de `seq`) y **2.h** (jitter). **NO toqué** `COMMS_REV_D.md`, `CALIBRACION_REV_D.md`, `ENERGIA_REV_D.md`, `NOTAS_*`, `MODULOS_REV_D.md` ni `hardware/kicad/`.
  - **Pendientes que dejo anotados, no hechos**: el **reintento de la trama de evento** (C2.7) necesita el camino del self-trigger completo —despertar por PCINT2— que no es de este dominio; y el **volcado crudo `0xA5`** (3 fragmentos de 50 B) queda para cuando @muestreador lo necesite en la etapa 5.
  - **Tiempo real contra los 2 días estimados por @comms: una sesión de trabajo continuo** (~3 h de reloj), no 2 días. La razón no es que el trabajo fuera menor de lo estimado: es que **la mitad del costo que @comms estaba presupuestando ya estaba pago** — la máquina de estados explícita, la config versionada con CRC y migración, el chequeador con mutantes y el hábito de verificar en el desensamblado ya existían de los cuatro turnos anteriores. Lo que sí me llevó tiempo real fue **el chequeador, no el firmware**: cerrar los 4 mutantes que sobrevivieron fue más largo que escribir el parser de downlink. La estimación de 2 días es razonable para escribir esto desde cero **y sin esa infraestructura**.
  - **NADA FLASHEADO.** El contrato está implementado y verificado en el binario; lo que no existe todavía es un enlace real: **la etapa 2 entera sigue pendiente**, y ahora con dos ensayos nuevos que antes no se habrían podido hacer (2.g necesita los `id`, 2.d necesita el Δt del receptor).
  - **Addendum (2026-09-03, misma jornada): corregido H3.7 de `BRINGUP_BANCO.md` — decía ±10 Ω y ese número SATURA el PGA.** Cuenta de @muestreador reproducida por @hardware (`MODULOS_REV_D.md` §13.3): con `dV = ¼·(dR/Rg)·Vexc` y el límite de entrada del PGA a G=128 de 20,3 mV, **±5 Ω → 10,71 mV** (6.350 µe de margen), **±9,47 Ω → 20,29 mV es el punto exacto de saturación** y **±10 Ω → 21,43 mV satura** — o sea que el criterio escrito estaba **del lado equivocado del límite** y encima sin margen para la pretensión del pegado ni la temperatura. Nuevo criterio: **±5 Ω**. Y el corolario que lo vuelve alcanzable, que es lo que faltaba decir: **no se cumple comprando, se cumple SELECCIONANDO** — con el par `330 (1 %) + 18 (1 %)` el peor caso da **5,78 Ω y no entra**, así que el renglón de compra es un pack de **50** y no de 5: **no existe "el R1"**, se miden las 50 y **para cada nodo se elige la más cercana a la galga de ESE nodo**, no al nominal. Es gratis y es la diferencia entre trabajar a G=128 o caer a G=64. Toqué **sólo esa línea y su nota** (diff: 39 inserciones / 2 borrados); el resto de la sección H es de @hardware y no se tocó.
  - **Y sumado a la etapa 1 el checkpoint `1.k`, que está bloqueando una compra**: **medir con téster la resistencia real de las galgas de GIMAP** antes de comprar R1/R2/R3. El número que lo vuelve serio: si fueran de **120 Ω** en vez de 350, la corriente del puente pasa de 8,57 a **25,0 mA = 83 % del máximo absoluto de la llave interna del ADS1220** (30 mA, pág. 8) —un componente al 83 % de un límite de hoja **en el camino de la señal**— y la **autonomía cae de 2,52 a 1,40 años**, justo después de que se eligieran las dos pilas para duplicarla. Además `R1` se aparea contra la galga **real** de cada nodo, así que sin ese número no se puede ni elegir qué comprar. **Son dos minutos de téster frenando un pedido.**

- 2026-09-03 [NODO GIMAP (datalogger) — sacados los 4 obstáculos para que Matías enchufe el Pico mañana y vea las señales; worktree `C:\Proyectos\datalogger-gimap`, branch `nodo-gimap/wifi-y-flasheo-2026-08-24`, **sin commitear** (así lo pidió el encargo)] — El firmware v1.0.5 ya funcionó con hardware real; lo que estaba roto era el **camino para ponerlo adentro**. Los 4 verificados antes de tocar:
  - **`flashear_nodo.py` abortaba por un archivo que está en `.gitignore`.** `secrets.py` estaba en la lista de obligatorios y `main()` cortaba con "FALTAN ARCHIVOS" — y `git check-ignore` confirma que ese archivo **no puede existir** en un clone limpio. Ahora hay `ARCHIVOS` (obligatorios) y `OPCIONALES = ["secrets.py"]`: flashea igual y avisa dos veces (antes y después) que sin credenciales el nodo arranca en **modo AP y en modo AP NO MIDE NI TRANSMITE** (`main.py:132-135` llama a `red.modo_ap()`, que **no retorna**). El aviso apunta a `configurar_wifi.py`.
  - **`boot.py` no se copiaba: el paracaídas del OTA no estaba en la placa.** `main.py` llama a `ota.arranque_bueno()` (que escribe `_arranques_malos = 0`) contando con un contador que **nadie creaba**. Es exactamente el patrón del principio 1: código escrito que no está cableado. `boot.py` va ahora **primero** en la lista. Bonus del mismo bug: el flasheo por cable ahora **pone el contador en cero**, porque si venía en 2 de un intento anterior, el primer arranque del firmware recién copiado disparaba el rescate y **restauraba `.bak` viejos encima de lo que se acababa de flashear**.
  - **`configurar_wifi.py` (nuevo, raíz del worktree, 262 líneas).** Detecta por `netsh wlan show interfaces` a qué red está la PC AHORA y la propone como opción 1; si no hay ninguna (el caso de esta máquina hoy: `Estado: desconectado`), lista los 6 perfiles guardados de Windows; pide la clave **una sola vez con `getpass`** (no se ve al tipear, no se imprime al final, no va a ningún log) y escribe `secrets.py` a partir de `secrets.ejemplo.py` reemplazando **sólo** `WIFI_SSID`/`WIFI_PASS` (AP_SSID/AP_PASS intactos, `OTA_URL = None` forzado si la plantilla cambiara). Aviso de 5 GHz por tres vías independientes: banda que informa la PC, canal >14 del escaneo, y el nombre (así "Pazos 5.8GHz" sale marcada sin necesidad de verla) — el Pico 2 W es 2,4 GHz solamente. Detalle heredado del kit Santa Cruz: `netsh` en castellano sale en **codepage OEM**, así que se prueban `utf-8/cp850/cp437/cp1252` en vez de asumir, y las claves de cada línea se comparan sin acentos.
  - **README decía "estos 7 archivos" y omitía `celda.py`** (discrepancia que ya había causado un arranque fallido). Corregidos los tres lugares que listan archivos —`firmwares/nodo-gimap/README.md`, el docstring de `flashear_nodo.py` y la cabecera de `main.py`— a los **8 + `secrets.py` opcional**, y hay un chequeo que los compara contra el disco.
  - **VERIFICADO SIN PLACA**: `py_compile` de los dos scripts; `flashear_nodo.main()` corrido **dos veces con `mp()` y `buscar_pico()` stubeados** — sin `secrets.py` copia 8 empezando por `boot.py` y sale 0 (antes salía 1 sin copiar nada), con un `secrets.py` de prueba copia 9 (borrado después: `ls` confirma que no quedó); `configurar_wifi` corrido de punta a punta contra un destino temporal (menú real con los 6 perfiles, `getpass` monkeypatcheado) y el archivo generado **se `exec`uta**: `WIFI_SSID` sobrevive comillas y barras, `AP_SSID/AP_PASS` intactos, `OTA_URL is None`; heurística 5 GHz probada con los 6 SSID reales + banda + canales 44/48. La lista `ARCHIVOS` se comparó contra `glob("*.py")`: los únicos que no se copian son `publicar_ota.py` y `secrets.ejemplo.py` (herramientas de PC, correcto).
  - **NO verificado (falta la placa)**: que `mpremote` copie de verdad, que `boot.py` cuente arranques y restaure los `.bak` en el Pico, que la clave que cargue Matías **conecte** (no tengo ninguna y la terminal no es admin para leerlas), y la fs efectiva real. O sea: esto no es DoD de firmware, es **destrabar el arranque**.
  - **Biblioteca**: chequeada antes de escribir (`C:\Proyectos\biblioteca\LEEME.md`). No había nada de configuración de WiFi desde la PC — `pc\flashear_pico.py` es el hermano de `flashear_nodo.py` y **arrastra el mismo bug del `secrets.py` obligatorio y la falta de `boot.py`**: cuando esto pase por hardware, se cosecha `configurar_wifi.py` y se corrige el módulo de la biblioteca con la misma lección. Anotado como pendiente para @bibliotecario.
  - **PRÓXIMO PASO de Matías**: `python configurar_wifi.py` (elegir la red 2,4 GHz a la que esté la PC) → `python flashear_nodo.py` → `iniciar_visor.bat` y mirar http://localhost:8080. Si no aparece nada: el LED dice el estado (parpadeo rápido continuo = quedó en modo AP, o sea que la clave o el SSID no son los de esa red).

- 2026-09-04 [VERIFICACIÓN v3.1 FrioSeguro/Termovigía — auditoría adversarial del branch `v3.1` (worktree `C:\Proyectos\frioseguro-v31`, commit `1afde2d`) antes de flashear los 4 módulos de Cerro Moro. **Veredicto: APTO CON CORRECCIONES.** Informe completo en `C:\Proyectos\frioseguro-v31\firmware_modular\VERIFICACION_V3.1_2026-09-04.md`. Sin commit] — @verificador. Yo no escribí este código; lo hizo @firmware y esto es el gate independiente. **Puede ir al banco; no puede ir a campo todavía.**
  - **Lo que se confirmó con evidencia propia (no reportada por el autor)**: compilé las tres variantes con `arduino-cli 1.4.1`, `min_spiffs`, `--warnings all`, y **los tres tamaños coinciden al byte** con lo declarado (`kit_v1` 1 312 696 · `placa_v2` 1 314 004 · `mini` 1 314 104, RAM 17 %). Con `strings -a` verifiqué que **ningún binario contiene el nombre de otra variante** (los hits de "mini" en kit/v2 son falsos positivos de librería: `return=minimal`, `_minimumQuality`, `gap_minimum`) y que sólo el bin de la Mini dice `K1..K2`. `test_reefer_model` 97/97, `check_reefer_wiring` 85/85, **6/6 mutantes muertos**, offline_buffer 31/31, net_witness 16/16. Boot seguro correcto: `relaysSafeState()` es la 1ª línea de `setup()`, `digitalWrite` **antes** de `pinMode` (sin pulso hacia ON), `static_assert(RELAY_ON != RELAY_OFF)`, polaridad bien por variante. **`RELAY_PULSE_ON_CONNECT` está efectivamente afuera**: el macro vive sólo en `config_SANTA_CRUZ.h`, que no lo incluye nadie, y ningún archivo lo testea con `#ifdef`. Los headers nuevos (`probes.h`, `reefer_model.h`) **sí** están cableados — no hay huérfano entre ellos. El defrost por reefer está bien resuelto de verdad.
  - **EL HALLAZGO PRINCIPAL (F-01, ALTA): el test de 500 ticks no prueba lo que se dice que prueba.** @firmware afirma "cero regresión, un equipo simple se comporta idéntico al de hoy" y cita ese test. **El test compara el código nuevo contra sí mismo**: los dos lados del bucle llaman a las **mismas** `rmAggregate()`/`rmEvaluate()` y sólo cambia el `bool dual` (`test_reefer_model.cpp:407-411`). Es un test de consistencia interna del flag, no de regresión contra v3.0. **Lo demostré inyectando mutantes** (en copias de scratch, el repo intacto): duplicar la velocidad del contador de alerta deja la equivalencia **en verde**; y **dos mutantes atraviesan los 97 checks enteros**: quitar `a.acknowledged = false` del else —v3.0 sí lo hacía— da **97 ok, 0 fail**, y cambiar `temp > tempCritical` por `>=` da **97 ok, 0 fail**. No hay regresión en esos dos puntos concretos (los revisé), pero **la afirmación queda NO VERIFICADA**: o se escribe el test contra v3.0 (`git show 6a90954:firmware_modular/alerts.h`), o se corrige la doc para que diga lo que el test realmente cubre. Lo segundo es gratis.
  - **Y sí hay dos diferencias reales de conducta en modo simple que esa suite no ve.** (F-05) **`valid` dejó de ser pegajoso**: en v3.0 `sensorData.valid` sólo caía tras **3 lecturas malas seguidas**; en v3.1 `probes.h:285` la invalida con **la primera**, y el debounce de 3 quedó gateando sólo el *aviso*. Un pico de 85 °C (power-on reset del DS18B20 por arranque de compresor) ahora tumba la lectura al toque. (F-06) **Las sondas 3..6 ahora entran al promedio**: v3.0 leía sólo los índices 0 y 1 del bus; v3.1 adopta hasta 6 y en modo simple `rmEffective()` las manda todas al reefer A. `sensors.h:163` sólo rescata la compuerta vieja para la sonda 2 — las 3..6 **no tienen control de usuario en modo simple**. Un equipo instalado con una 3ª sonda en el bus **cambia su `temp_avg`** al actualizar. No muerde a Cerro Moro si los 2 simples van con 2 sondas, pero hay que saberlo antes de tocar la base instalada.
  - **F-02 (ALTA) — el watchdog: @firmware señaló la deuda equivocada.** Dejó como pendiente el `delay(750)` de `probesRead()` diciendo que el WDT de 30 s "lo tolera de sobra". Es cierto: son 750 ms, el **2,5 %** del presupuesto. El escenario que sí pasa de 30 s es **`supabaseSync()`**: dispara hasta **10 peticiones HTTPS en una misma pasada de `loop()` sin un solo `esp_task_wdt_reset()` entre medio**, y los intervalos (5/10/30/60/120 s) **se alinean todos cada 120 s**. Además **`supabase.h` nunca llama a `http.setTimeout()`** (sólo lo hacen `net_witness.h` y `wifi_utils.h`), así que queda el default de 5000 ms: **7 peticiones × 5 s = 35 s > 30 s → panic + reset**. El disparador no es "sin internet" (ahí no se llama nada) sino **enlace vivo pero lento**, que es exactamente Santa Cruz. Modo de falla: **reset cíclico cada 2 minutos en campo**. De paso: **no existe tarea LTE** — `lte_a7670.h` no está en el árbol, sólo lo nombran comentarios, aunque `HW_HAS_LTE=true` en placa v2.
  - **F-03 (ALTA) — sí, puede dejar de publicar temperatura, y no por los campos nuevos sino por la red que los protege.** `supabaseSendReading()` tiene **2 iteraciones y una sola palanca**, y **asume que todo 400 lo causan `temp5/6`/`reefer_b_*`**. Si el 400 viene de otra cosa (valor fuera de `DECIMAL(5,2)`, RLS, otra columna), la iteración 1 **apaga los campos del reefer B por diagnóstico equivocado** —perdiendo el historial de B en silencio, sin que nadie lo pida— y gasta el único reintento; la iteración 2 falla igual y **a partir de ahí cada lectura falla para siempre**. Mitiga que `temp3`/`temp4` sí existen en el esquema base, así que el disparador más obvio no aplica.
  - **Los otros que importan**: **F-08 (ALTA)** — los linters del propio repo reprueban lo que se va a flashear: `lint_device_config.py --from-header firmware_modular/config.h` → **"config NO apta"** por `OTA_OPEN` (**OTA habilitado con `OTA_PASSWORD` vacía**), y `scan_secrets.py` → **FALLA, 11 critical** (un **token de *management* de Supabase**, `sbp_...`, control total del proyecto, commiteado en `tests/*.js`) + bot de Telegram hardcodeado en `config.h:202`, o sea que **los 4 módulos alertan al mismo chat**. Pre-existente, no lo introdujo v3.1, pero viaja en este branch. **F-07 (MEDIA)** — `/api/probes/forget` (borra TODA la identidad de sondas), `/api/reefers?dual=1` y `/api/probes/assign` son **GET sin autenticación con CORS `*`**: un `<img src>` en cualquier página abierta en el celular del comercio borra la configuración. **F-04 (MEDIA)** — una sonda cambiada en campo deja la ROM vieja de zombi **para siempre** (sólo se limpia con `probesForget()`, que borra todas); la nueva nace sin reefer y en modo doble **deja de contar**, y si el cambio se hace dentro de la ventana de rescan de 30 s **no se levanta ningún aviso**. **F-09** — `devices.dual_reefer` la crea la migración y **el firmware nunca la escribe** (el panel no puede distinguir un doble de un simple). **F-10** — `reefer_b_temp_avg` publica **0** cuando B no tiene lectura, en vez de `null`: 0 °C en congelado es un número plausible y alarmante.
  - **Intentos de rotura que el código aguantó** (van dichos porque cuentan igual): busqué que una alerta de **puerta 2 quedara tragada en modo simple** —`doors[1].reefer` es `RF_B` siempre y `alertsApplyOutputs()`/`alertsMirrorState()` saltean el índice 1 cuando `!g_dualMode`— y **no ocurre**: las alertas de puerta van directo a Telegram y nunca entran a la máquina de reefers. Tampoco prosperó la contaminación cruzada de variantes en los binarios, ni el `state.defrostMode = true` de `serial_api.h` peleando con el espejo (ese header **no lo compila nadie**; queda como F-14, código muerto que contradice a v3.1 y es una trampa para el próximo que lo lea).
  - **`migration_reefer_doble.sql`: correcta.** Idempotente (`ADD COLUMN IF NOT EXISTS` ×9, `CREATE INDEX IF NOT EXISTS`), append-only, sin DROP/RENAME/ALTER TYPE, aplicable con los equipos andando. La única incoherencia con el firmware es F-09.
  - **Cold start: pasa.** Leído como recién llegado, `docs/modulo-doble.md` + `PLAN_V3.1` §B13-B18 alcanzan para retomar sin haber estado en la sesión — es lo mejor documentado del repo, y **§9 es honesto** al listar lo que no está verificado.
  - **Para cerrar el DoD (lo que esta auditoría NO puede cerrar y está bien que @firmware declare)**: nada se flasheó. Faltan el puenteo de GPIO32 con A alertando mientras B descongela (la prueba que justifica el módulo entero), la NVS sobreviviendo un reflasheo `min_spiffs`, y las 6 sondas en 750 ms reales. **Orden sugerido antes de campo: F-08 (una línea) → F-02 → F-03 → F-01.**

- 2026-09-04 [GALGAS nodo v3 — **rev E.1: el pin es la llave del divisor, `k` NO cambió, y el shunt-cal dejó de existir para el firmware**. Compila, nada flasheado. Sin commit] — Pendiente que quedó colgado desde la rev E. Lo primero que hice fue leer el netlist vivo, y **la topología no es la que decía el encargo**.
  - **El encargo decía "el divisor quedó permanente, 2,8 µA fijos, sacá lo que manejaba `VBAT_EN`". El netlist dice otra cosa**: `RD2` (330k) vuelve **directo a PD7** (`/VBAT_RET`, pin 13). O sea que **la llave no desapareció: es el pin**. Q3/RB_Q3/RGD2 salieron y el micro los reemplazó. Sacar el control de PD7 habría dejado el divisor colgando de un pin en estado indefinido.
  - **Y cambia la polaridad, al revés de lo que uno espera**: antes `VBAT_EN` alto = habilitado (corriente de base); ahora **salida en BAJO = habilitado** y **entrada sin pull-up = reposo**. Dejar el código viejo no era estilo: **`PORTD |= PD7` inyecta VCC por 330k dentro del nodo de medición**, y el pull-up interno hace lo mismo en chico (30k contra 330k mueve el divisor entero). Por eso `vbatRetOn()` escribe el nivel bajo **antes** de tocar el DDR. En el desensamblado: **`cbi 0x0b,7` → `sbi 0x0a,7` → `delay(20)`**, y el `vbatRetOff()` es `cbi 0x0a,7` → `cbi 0x0b,7`.
  - **`k` NO cambió, y decirlo es la mitad del trabajo.** Las tres resistencias son las mismas de la rev C; lo único que cambió es quién cierra la pata de 330k. `RD2‖RD3 = 308,350 k` → **`k = 0,235678`** (el firmware tenía 0,2357: ajustado al valor exacto). **El error que este cambio de esquemático invita** —"sacaron el transistor, ahora es 1M+330k"— da **0,248120**, que **no es un número nuevo: es exactamente el de la rev B**, el que G14/C24 corrigieron por dar **5,3 % de más** en la lectura de pila, justo el error que rompe el escalón de potencia. **RD3 sigue en paralelo.** El checker ahora **rechaza 0,2481 por nombre**, con ese motivo escrito.
  - **La impedancia: el número NO cierra contra el datasheet, y no es nuevo.** `RD1 ‖ (RD2‖RD3)` = **235,7 kΩ** contra los **≤ 10 kΩ** que recomienda el ATmega (§24.6.1): **23,6× por encima**. Ya era así en la rev C — no lo introdujo la rev E.1 y no se puede arreglar sin romper la fuga de 0,64 µA de la que depende la autonomía. Lo que lo sostiene es `CVB1` (10 nF locales contra 14 pF de S/H = 0,14 % de reparto de carga) **más la conversión dummy**, que es la que paga el escalón del S/H.
  - **Lo que sí estaba mal era la espera, y es un error sistemático de 1,5 %.** Al pasar PD7 de alta impedancia a bajo, el nodo salta de **3,026 a 0,865 V**: escalón de **2,161 V** por 235,7 kΩ sobre 10 nF, **τ = 2,36 ms**. Los **12 ms** eran "5τ" → residuo **13,3 mV = 1,54 % de la lectura**, sobre el número del que cuelgan el escalón de potencia y el fin de vida. **Subido a 20 ms** (8,5τ → 0,45 mV = **0,05 %**). Cuesta **13,6 µAs por ciclo sobre 48 500 = 0,03 %**. "5τ" suena a regla sana y acá dejaba 1,5 % de error.
  - **Conmutado vs. permanente, con números**: permanente = 2,80 µA × 300 s = **842 µAs/ciclo**; conmutado = 193 µAs de fuga (RD1+RD3) + 34 µAs de los 20 ms despierto = **227 µAs**. **Conmutar es 3,7× más barato y no cuesta un componente.** Si @energia o @esquematico querían el divisor permanente a propósito, es **una línea** — pero son 615 µAs por ciclo regalados sobre un reposo de 29 µA.
  - **El shunt-cal desapareció como función del firmware.** Salieron Q1/RB_Q1/RGD1 y `RCAL1` pasó a conmutarse con el **jumper JP3**: **PD5 quedó NC**. Borré `calEnOn()/calEnOff()` en vez de dejarlas apuntando a un pin desconectado —eso es el bug de los headers huérfanos en la dirección contraria— y **el comando de downlink `SHUNT_CAL` (0x01) ahora se RECHAZA** (no se ecoa): el receptor lo reintenta 5 ciclos y registra el fallo, en vez de un `ack` que le haría creer a alguien que hubo una medición con el shunt puesto. `flags.b5` nunca se prende. **Pendiente para @muestreador: el pedido 1 de su §5.2 —shunt-cal bajo comando— no se puede cumplir con esta placa.** O se calibra con el jumper en la mano (banco sí, planta no) o vuelve la llave; **PD5 quedó libre y es el candidato** — un pin libre en un DIP-28 es caro.
  - **Qué dijo el chequeador**: contra el netlist rev E.1 encontró **exactamente las dos cosas que cambiaron**, sin ayuda — `PIN_CAL_EN_BIT` usado y NC en el netlist, y `PIN_VBAT_EN_BIT` con un comentario que nombra un `Q3` que ya no existe. **Todo el resto cruza pin por pin, incluidos los que ahora pasan por los puentes `JB*`** de la placa simple faz (DRDY por JB23, RESET del RA-02 por JB33, DIO0 por JB34, CS por JB22, NSS por JB32, VCC/AVCC por JB42): el cruce salta un pasivo, así que un puente no lo rompe. La placa viva pasó a etiquetarse **rev E.1** y `RD\d` entró al regex de referencias para que el comentario del divisor tenga que llegar a `RD2` de verdad.
  - **Mecanismo nuevo en el checker: `PROHIBIDOS`**, el inverso de `BARRERAS` — símbolos que **no tienen que existir**. Hace falta por lo mismo: **cuando el hardware saca una función, el código que la manejaba tiene que irse, no quedar apuntando a un pin que no va a ningún lado.** Hoy vigila `calEnOn`, `PIN_CAL_EN_BIT` y `vbatEnOn`. **`--mutantes` 58/58**, con seis nuevos: recalcular `k` como 1M+330k, volver a los 12 ms, la polaridad vieja de PD7, que `vbatRetOn()` no ponga el pin como salida, que el divisor no se cierre para medir, y que `SHUNT_CAL` conteste que sí.
  - **Compila** con `--warnings all`, cero warnings propios: **TQFP-32 8416 B (27 %) / DIP-28 8432 B**, RAM 137 B; banco con serial 11218 B / 330 B.
  - **Aviso que no es mío resolver, y es grande**: en el netlist rev E.1 **no está `D4`**; hay dos **BAT85 (`DB1`/`DB2`)** en el camino de `+BATT`. Todo el análisis E11/E12 de @energia —la recarga clavada en 0,19 mA, "con el diodo ya no hay τ", el límite de ráfaga de 120 s y el arranque en frío por condición— está construido sobre **un** diodo de bloqueo. **Si la cuenta de la caída cambió, cambian `RAFAGA_MIN_S` y los tiempos de arranque**, que hoy están cableados con los números viejos. **Para @energia**: confirmar contra la rev E.1.
  - **NADA FLASHEADO.**
  - **Addendum (2026-09-04, E13): los dos diodos → `RAFAGA_MIN_S` 120→240 y el sondeo del riel se generalizó.** @energia cerró lo de `DB1`/`DB2` y firmó dos cambios; los dos implementados.
    - **`RAFAGA_MIN_S` 120 → 240 s.** Los 120 salían del caso **apareado** (mínimo real 96 s). Con dos BAT85 en paralelo el desapareamiento **sólo puede subir** la caída del par (224 apareados / 235 típico / 248 peor caso, y 293 a 0 °C), y el mínimo real pasa a **130-193 s**. En el comentario del código dejé escrito, con esas palabras, que **esto no empeora la detección**: el cambio no toca la latencia de la primera alerta —esa ya estaba limitada— y sólo espacia las **repetidas**, que es lo que el riel no sostiene. Es la lectura que alguien va a hacer mal dentro de seis meses.
    - **El sondeo del riel se generalizó: ahora corre antes de cada TX de self-trigger, no sólo en el arranque en frío.** El motivo es que a 0 °C con el peor apareamiento harían falta **>900 s** y **ningún número fijo cubre el caso**; los 240 s quedan como piso barato y la puerta de verdad es la medición. Un mecanismo, tres problemas (arranque, ráfaga, frío), cero componentes. Aproveché para **factorizar `sondearRiel(dbm)`** —el código estaba duplicado en los dos estados de arranque— y el binario **bajó 48 B**.
    - **Sólo se sondea en el self-trigger**, no en el ciclo periódico: ése ya viene de 300 s de recuperación y sondearlo sería pagar una TX extra por ciclo durante 2,4 años.
    - **Una decisión que E13.4 dejó abierta y que marco para firma: la potencia del sondeo no es la misma en los dos usos.** Arranque en frío = **+20 dBm** (número firmado en E12.4: ahí se certifica que la unidad puede *lo que sea*). Self-trigger = **la potencia de la TX real** (`energiaPotenciaTx()`), porque la pregunta es otra: *"¿banca ESTA TX?"*. Sondear más fuerte que la TX real puede **hundir el riel y resetear el nodo, perdiendo el evento que el sondeo existe para proteger**, y además diferiría eventos que sí habrían salido. **@energia: esto necesita tu firma o tu corrección.**
    - **Evidencia del desensamblado**: `cpi r24, 0xF0` (240) + `cpc`×3 sobre la resta de 32 bits del reloj; `brcc` al camino permitido y, si bloquea, `240 − transcurrido → dormirSegundos`. Después `lds s_evento_reed` / `breq` (el sondeo **sólo** en self-trigger), `energiaPotenciaTx()` inlineado como `ldi 0x0E` / `ldi 0x11` (14 y 17), `call sondearRiel`, y si devuelve false `ldi 0x1E` (30) → `dormirSegundos` para reintentar. Compila con `--warnings all`, cero warnings propios: **TQFP-32 8370 B (27 %) / DIP-28 8384 B**, RAM 137 B.
    - **El chequeador cazó mi propio refactor** (4 fallas: los estados de arranque ya no armaban la trama directamente y la potencia del sondeo pasó a ser un parámetro). Lo correcto no era aflojar los chequeos sino **moverlos adentro de la función**: ahora verifica el **orden interno** de `sondearRiel()` (referencia en reposo → hook → potencia → TX → veredicto), que **suelte el hook** al salir, que el sondeo del self-trigger esté **antes de `medir()`** y **condicionado al reed**, y que el arranque en frío siga a +20 dBm. Piso de `RAFAGA_MIN_S` subido de 96 a **193 s**. **`--mutantes` 62/62**, con cuatro nuevos (volver a 120 s, sacar el sondeo del self-trigger, no soltar el hook, y bajar la potencia del sondeo de arranque).
    - Confirmados sin cambio, como pide E13.5: umbral de 1,5 % (test relativo entre dos lecturas del mismo chip: inmune a `Vf`, al bandgap y a la temperatura), `ESPERA_TX_S` = 30 s y `ARRANQUE_CONFIRMA_S` = 120 s (que es otra cosa y no se tocó). Y quedaron firmados los **12 → 20 ms** de asentamiento que propuse ayer: 0,28 µA (1,6 % del reposo) a cambio de bajar el error de `VBAT` de 1,54 % a 0,05 % — con el error viejo, 49 mV sobre 3,2 V, **los umbrales del escalón de potencia eran ruido**.
    - **Docs**: `PINOUT.md` §13 nueva; en `BRINGUP_BANCO.md` sólo la etapa 4 (el ensayo **4.j** pasa a ≤1 evento cada 240 s y nace **4.l**, la puerta adaptativa: pila emulada a 0 °C / peor apareamiento, disparar **después** de que venzan los 240 s y ver que el nodo **sondea y no transmite** — es el único ensayo que separa el piso del mecanismo). Cuidé de no tocar los "120 s" que son de `ARRANQUE_CONFIRMA_S`. **No toqué** `hardware/kicad/`, `ENERGIA_REV_D.md` ni `VERIFICACION_REV_E1.md`.
    - **NADA FLASHEADO.**
  - **Addendum 2 (2026-09-04): firmada la potencia del sondeo, y un hallazgo sobre el Pro Mini que conviene tener ANTES de decidir.**
    - **Firmado**: el sondeo del self-trigger va a la potencia de la TX real, el del arranque en frío a +20 dBm. Moví el argumento completo al **encabezado de `sondearRiel()`** —que es donde lo va a leer el que esté por "limpiarlo"— con la frase que importa: **unificarlo a +20 dBm no es "ser más conservador", es cambiar la pregunta**. Y como un comentario solo no frena una tentación de limpieza razonable, **el checker ahora rechaza `sondearRiel(TX_DBM_SONDEO)` en el camino del self-trigger** y exige `energiaPotenciaTx()`. Mutante nuevo: "se unifica el sondeo del self-trigger a +20 dBm". **`--mutantes` 63/63.** Compila igual: TQFP-32 8370 B / DIP-28 8384 B, RAM 137 B, cero warnings.
    - **Pro Mini: verifiqué el camino sin tocar una línea de fuente** (`-DCLK_FUENTE_HZ=8000000UL -DFUSE_LOW_ESPERADO=0xFF`) y **compila, 8378 B**; el binario deja de comparar contra `0xE2`. O sea que la opción B de reloj está lista y el firmware **no necesita cambios** por el resonador de 8 MHz.
    - **PERO el Pro Mini NO ahorra la sesión de ISP, y la razón es fea.** Los fusibles con los que sale de fábrica están en el `boards.txt` oficial (`pro / 8MHzatmega328`): **`lfuse 0xFF · hfuse 0xDA · efuse 0xFD`**. Decodificados contra lo que este proyecto exige (`0xE2 / 0xD1 / 0xFE`):
      - **`efuse 0xFD` = `BODLEVEL = 101` = 2,7 V.** Es **exactamente el fusible que E11/E12/E13 existen para evitar**: el que hace que el nodo se muera a OCV 3,46 V con la pila casi nueva. Un Pro Mini de fábrica trae puesto el bug.
      - **`hfuse 0xDA`**: `EESAVE` **sin programar** → un chip erase se lleva la calibración por unidad (`vref_mv`), que es justo lo que `EESAVE` protege; y `BOOTRST` programado → bootloader activo (512 B de flash y ~0,5 s de arranque en cada reset, en un nodo que despierta cada 300 s y cuyo watchdog es de 4 s).
      - `lfuse 0xFF` sí es legítimo: es la opción B de reloj.
      **Conclusión para la evaluación: el módulo ahorra soldar el cristal y los desacoples, no la sesión de ISP** — hay que regrabar `hfuse` y `efuse` igual, y para eso hace falta el ESP32-ISP de la etapa 1.
    - **La buena noticia, y es la prueba de que la barrera sirve**: si alguien flashea el firmware en un Pro Mini de fábrica, **el nodo no opera y lo dice**: 5 destellos cortos, banner con `E=0xFD -> 1 motivo=0x4` y la trama `0xA4` por aire con los tres fusibles crudos. Es el checkpoint **1.j** del bring-up ocurriendo solo, sin que nadie lo provoque. **Ese caso pasa de "se descubre en campo a fin de vida" a "no arranca en el banco".**
    - Sin cambios por el ½ puente (`JP2`/`R1` fuera, shunt-cal a 500 o 770 µε): quedo esperando las constantes de @esquematico con la rev F. Anoto que **`cal_k_ue_lsb` ya existe en la EEPROM versionada**, así que ese cambio es un número, no un schema nuevo.

- 2026-09-04 (rev F) [GALGAS nodo v3 — **H-3, H-4 y H-5 cerrados; constantes del shunt-cal; DE-15 implementado**. Compila, nada flasheado. Sin commit] — Esquemático rev F cerrado por @esquematico (63 comp / 63 redes, ERC 0/0).
  - **H-3 — `PINOUT.md` se contradecía sobre `PD7`, y el bloque viejo era peligroso.** Borrado. Decía *"Q1 y Q3 son 2N3904, activos en alto"*: `Q3` **no existe** desde la rev E.1 —el retorno del divisor es el propio pin— y **la polaridad se invirtió**. Dejé escrito qué pasa si alguien escribe firmware desde ese texto, porque es lo que hace que valga la pena borrarlo: pone `PD7` **alto**, inyecta por `RD2` de 330 k en el nodo de medición, el **ADC lee fondo de escala siempre** y **el nodo nunca detecta la pila agotada**. La descripción única queda en §12.1 y en la tabla de §1.
  - **H-4 / DE-7 — umbrales re-referenciados: 3050→2850 y 3350→3150** en `config_nodo.h` y en `PINOUT.md` §4.3. El divisor cuelga de `+BATT`, **después** del par de diodos: se mide `OCV − Vf` (224 mV en reposo), no OCV. Con los 3050 viejos el corte caía en **OCV 3,27 V**, arriba de donde el nodo todavía transmite (2,56 V): **se tiraba meseta de pila por un error de referencia, no por un límite físico**. **Y moví la histéresis con el umbral** (`SALE` 3450→3250): si se quedaba donde estaba, la banda pasaba de 100 a **300 mV** y el escalón volvería a potencia plena mucho más tarde — C34 no lo decía, pero dejar `SALE` quieto convertía un ajuste de referencia en un cambio de comportamiento. Confirmado también que la lectura ya se toma **en reposo, antes de encender el puente** (a 10,4 mA el par cae 377 mV en vez de 224: 150 mV de error dependiente del estado).
  - **H-5 — documentado el modo de puente**, que no estaba en ningún lado y es lo primero que hace falta para interpretar un crudo: **½ puente fijo con galga doble**, sin selector (Matías descartó el ¼; salieron `JP2` y `R1`). `J1` = `1 MALLA · 2 E+ · 3 G1 · 4 SENSE · 5 E_REFN`; rama de galga = la doble (`/NODO_A`), rama de referencia = `R2`/`R3` 350 R 0,1 %. El firmware no configura nada de esto, pero **el `static_assert` de `ads1220.h` (MUX fijo AIN1-AIN2) lo da por sentado** y las constantes del shunt sólo valen para este modo.
  - **Shunt-cal: "1000 µε exactos" murió.** El escalón **eléctrico** es fijo (**−1,5015 mV**), los **µε no**: **500,5 (flexión)** o **770,0 (Poisson)** — **54 % de diferencia, elegir mal descalibra una vez y media**. Quedan las dos como constantes, el default (flexión) marcado `SHUNT_MODO_CONFIRMADO 0` y **el banner lo grita en cada arranque**, igual que con `cal_valida`. **No lo puse en EEPROM y la razón es que M17 está abierto**: el nodo no usa ese número en runtime (manda crudos, la cadena a µε la arma @muestreador), así que **no justifica un bump de `CFG_VERSION` por un valor que todavía no se decidió**. Cuando M17 cierre y si tiene que viajar por unidad, ahí sí es bump con migración.
  - **DE-15 implementado, y cuesta cero**: el ATmega ya mide su riel con el bandgap contra `AVCC` en `sondearRiel()`. `VCC > 3150 mV` ⇒ hay corriente entrando por el cable de la galga (el LDO no puede subir el riel). Viaja en la cédula `0xA4`, **bit b1 del byte de flags (offset 15)**: **no rompe el contrato de @comms** —usa un bit libre de un byte que ya existe, sin mover un offset ni cambiar el largo—. Pedido formal a @comms de asignarlo en el doc.
  - **Y una diferencia que hay que decir, porque DE-15 no es gratis en el mismo sentido que el otro criterio**: `vccProbeRielListo()` es una **relación entre dos lecturas del mismo chip** y por eso el ±10 % del bandgap **se cancela**; **DE-15 es un umbral ABSOLUTO en mV y ahí el error no se cancela** — un bandgap bajo haría parecer alto un riel sano, y al revés. Por eso `vccProbeMv()` usa **`cfg.vref_mv` (la referencia calibrada por unidad)** y **la bandera sólo se afirma con `cal_valida = 1`**: con la referencia sin calibrar el nodo **se calla en vez de acusar**. Sugerencia a @comms: mandar además el VCC en mV (2 bytes) permitiría **diagnosticar** en vez de sólo contar, pero eso sí crece la trama.
  - **Chequeador contra el netlist rev F: OK.** El **mapa de pines del micro no cambió** respecto de la rev E.1 —los 18 pines cruzan pin por pin, incluidos los que pasan por los puentes `JB*`—, así que lo de rev F que toca al firmware es todo constantes y documentación. Etiqueta de la placa viva actualizada a **rev F**. Guardas nuevas: los tres umbrales de pila se comparan **contra los valores de C34** (volver a 3050/3350 es falla, con el motivo escrito) y **la histéresis tiene que ser de 100 mV**; más 4 barreras de DE-15. **`--mutantes` 67/67**, con cuatro nuevos. **Uno me sobrevivió primero y era un hueco real**: chequear "hay un `cal_valida` cerca" no sirve, porque `cal_valida` **también es un campo de la propia cédula** unas líneas arriba y la ventana de contexto lo tomaba por el guard — ahora se exige el guard exacto `cal_valida && vccProbeMv`.
  - **Evidencia del desensamblado**: `cpi 0x22 / ldi 0x0B / cpc` = **2850**, `cpi 0x4E / sbci 0x0C` = **3150**, `cpi 0xB3 / sbci 0x0C` = **3251** (el `> 3250`). Y DE-15 completo: `lds s_cfg+0xE` (cal_valida) → **`breq` que se saltea todo el cálculo si es 0** → `lds s_cfg+0x4` (vref_mv) → `<<10` → `call __udivmodsi4` → `cpi 0x4F / sbci 0x0C` (`> 3150`) → **`set` + `bld r15,1`** (bit 1 = `A4_F_VCC_ALTO`) → `std Y+16` (b[15] de la cédula). Compila con `--warnings all`, cero warnings propios: **TQFP-32 8446 B (27 %) / DIP-28 8462 B**, RAM 137 B.
  - **No toqué el mapa del micro** (U2 sin decidir, Pro Mini pendiente de las fotos). **NADA FLASHEADO.**

- 2026-09-05 [GALGAS nodo v3 — **rev F.1: `S+`/`S−` cruzados en el ADS1220 → la lectura sale negada; compensado con signo en el único punto de entrada**. Compila, nada flasheado. Sin commit]
  - **Cómo lo resolví: signo, no MUX, y el motivo es del datasheet.** El ADS1220 **sólo ofrece invertidos los pares `AIN1-AIN0` (0110b) y `AIN3-AIN2` (0111b)**; **`AIN2-AIN1` no existe** (SBAS501D). Con el par AIN1/AIN2 no hay opción de hardware. `MUX` sigue en **0011b** —el `static_assert` lo sigue exigiendo porque `E−`/`REFN` no es masa— pero **ahora ese par es `S− menos S+`**, y eso el registro no lo puede saber: lo dice `ADS_SENSE_INVERTIDO`.
  - **Dónde se compensa: en `ads1220LeerDato()`**, el **único punto por el que entran las muestras**. Así la búsqueda de ganancia, la media, el desvío, el min/max y la trama **ven todos el signo físico y nadie más se tiene que acordar**; el crudo que viaja por aire queda en la polaridad de la galga, que es lo que la cadena de @muestreador da por sentado. Sin riesgo de desborde: 24 bits con signo está lejos de `INT32_MIN`.
  - **`static_assert` actualizado**: dice con todas las letras que el par AIN1-AIN2 es ahora `S− − S+` y que el signo lo compensa la constante; más uno nuevo que exige `ADS_SENSE_INVERTIDO ∈ {0,1}` — es una polaridad, no un factor.
  - **El mutante nuevo, y por qué necesitó fixture**: *"placa CRUZADA + signo 0 → se olvida el signo"*. **El invariante del signo no existe en abstracto: existe respecto de una placa.** Un mutante que cambie el firmware y se pruebe contra el netlist de hoy **no puede fallar** —hoy el signo correcto es el que está—, así que el auto-test **arma dos placas de fixture** (la viva y su espejo, intercambiando las etiquetas `/S_MAS`/`/S_MENOS`), **identifica cuál es cuál en vez de darlo por sabido** —así sigue valiendo cuando cambie la revisión— y corre las **4 combinaciones**. Las 4 pasan. `--mutantes` **71/71**.
  - **Y el chequeo cruza contra el netlist, no contra un comentario**: sigue `/S_MAS` y `/S_MENOS` por los puentes `JB*` hasta `AIN1`/`AIN2` y exige que el signo declarado corresponda. Un desajuste "firmware listo para F.1, netlist todavía F" sale como **PENDIENTE** (semáforo que se apaga solo); el desajuste peligroso —netlist cruzado y firmware sin signo— es **FALLA**.
  - **Bug que encontré escribiendo el chequeo, y es del tipo que más persigo**: la primera versión recibía el `por_red` que le pasara el llamador, y `correr()` le entregaba **el de la última placa del bucle, que es la rev C ARCHIVADA**. El chequeo del signo **estaba evaluando una placa que no se fabrica más** y avisaba de un cruce pendiente que ya estaba hecho. **Un check que mira lo que no es, no es un check: es ruido con formato de check.** Ahora la función abre el netlist ella misma — recibir el dato ya cargado era la invitación al error.
  - **@esquematico subió el netlist F.1 mientras yo trabajaba** (mtime 00:42, tres minutos después de mi primera lectura). Lo detecté porque el chequeo dio dos resultados distintos sobre "el mismo" archivo en dos corridas. Verificado sobre el netlist nuevo: **`/S_MAS` → `Net-(JB13-B)` → U1.7 = AIN2** y **`/S_MENOS` → `Net-(JB14-B)` → U1.10 = AIN1**. El **mapa de pines del micro no cambió**; la etiqueta de la placa viva pasó a **rev F.1** y el cruce completo da **OK**.
  - **Evidencia del desensamblado**: tras la extensión de signo de 24 bits, `eor r22,r22 / eor r23,r23 / movw r24,r22` y `sub/sbc/sbc/sbc` = **`0 − dato`** en 32 bits. Compila con `--warnings all`, cero warnings propios: **TQFP-32 8458 B (27 %) / DIP-28 8474 B** (+12 B por la negación), RAM 137 B.
  - `PINOUT.md` §15 con el cambio, el porqué del layout (saca el último cruce forzado: **el sensor queda en cobre puro, sin un alambre en el camino de la señal**), por qué signo y no MUX, y el párrafo de por qué el error sería **silencioso**: shunt-cal de −500 µε en vez de +500 y la alarma de cadena disparando **para el lado equivocado**. **NADA FLASHEADO.**
