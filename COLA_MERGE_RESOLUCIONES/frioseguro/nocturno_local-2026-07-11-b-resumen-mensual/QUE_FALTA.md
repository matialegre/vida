# QUE_FALTA — FrioSeguro · LA PALANCA DE PLATA (meta: 3 abonos antes del 18-ago, 5 antes de octubre)

> Escrito por Claude Fable 2026-07-07. Estado de referencia: `ESTADO_ACTUAL.md` (v4.0.0, mar-2026) + `docs/FRIOSEGURO_MEGAPROMPT.md` (spec maestra). Doctrina comercial: PLATA.md en github.com/matialegre/vida. Inventario: 5 PCBs fabricadas (2 SIM800 + 3 WiFi), 20 DS18B20, 10 reed, relés.

## 🎯 Definición de "vendible"
Un kit instalable en un comercio en <2 horas, con caja estanca, alertas al celular DEL cliente, dashboard que entiende de un vistazo, credenciales únicas por cliente, y contrato simple firmado.

## 🔴 Bloqueantes para el PRIMER abono
1. **Compilar y flashear los módulos nuevos** del firmware v4 en una placa WiFi (el código está "arquitecturalmente completo" pero sin flashear). [@firmware]
2. **Piloto casero**: placa pelada en la heladera de Matías — jueves 9-jul según PLAN_MES. Es la demo de venta. [MATÍAS + @firmware]
3. **Migración SQL de columnas nuevas** (`connection_mode`, `gsm_signal`, `free_heap`...). [@backend]
4. **Credenciales de producción**: sacar defaults (`OTA_PASSWORD`, teléfono SMS, APN) y credencial única por dispositivo/cliente. [@backend + @firmware]
   - ✅ **MERGEADO a main** (commit `659a8d7`, verificado @verificador 13 checks) — `nocturno/local-2026-07-10-b-secret-scan`: **barrida de secretos** (mitad de software, sin hardware/cloud). `tools/scan_secrets.py` (stdlib) detecta y clasifica secretos hardcodeados trackeados por git — distingue anon (público por diseño) de `service_role`/`sbp_` (crítico). `tools/test_scan_secrets.py` = 13 checks, **OK**. Sobre el repo hoy: **14 secretos a rotar** (10 Management tokens `sbp_` + 1 `service_role` JWT en `tests/*.js`, 3 tokens de Telegram en `config.h`/`config_SANTA_CRUZ.h`/`DISPOSITIVOS.md`) + `OTA_PASSWORD ""`. Auditoría + **runbook de rotación** en `SECURITY_AUDIT.md`; `.gitignore` endurecido. Cierra parcialmente el **hueco #6 del PORTFOLIO**. **Falta (día, Matías, cloud+hardware)**: rotar las 3 familias de claves, setear `OTA_PASSWORD`, verificar RLS activo. Ver `MATI-HQ/diario/nocturno-local-2026-07-10-b.md`.
   - ✅ **MERGEADO a main** (commit `2cc2df5`, verificado @verificador 35 checks) — `nocturno/local-2026-07-12-lint-device-config`: **guardarraíl de provisioning**. `tools/lint_device_config.py` (stdlib) valida la config de un device ANTES de flashear/onboardear — `--from-header config.h` o `--json-in` fila de Supabase. Caza los 4 modos de falla que no rompen la compilación pero sí al cliente: orden `temp_min<temp_max<temp_critical` roto (alertas nunca/siempre disparan), `alert_delay_sec=0` (tormenta de falsas alarmas), `OTA_PASSWORD ""` (reflasheo abierto en la LAN), bot Telegram compartido. Exit 0/1/2 (gate pre-flash), `--json`, `--fail-on`. `tools/test_lint_device_config.py` = **35 checks, OK**. **Corrida real sobre el `config.h` (PLACA_NUM=4): 1 error (OTA_OPEN) + 1 warn (Telegram) + 2 info** → una placa flasheada tal cual todavía NO es client-ready; los umbrales por defecto sí están bien. No es scanner de secretos (eso es `scan_secrets.py` del branch 07-10-b): valida corrección operativa. Doc: `docs/lint-device-config.md`. **Falta (día)**: definir `OTA_PASSWORD`/chat por cliente y sumarlo al alta. Ver `MATI-HQ/diario/nocturno-local-2026-07-12.md`.
5. **Caja estanca IP65** de ferretería + prensacables (decisión tomada: NO carcaza 3D para v1). Comprar 2-3. [@hardware / MATÍAS]
6. **Precio + pitch + contrato simple** con límite de responsabilidad ("el servicio avisa, no garantiza la mercadería"). [@comercial + MATÍAS decide precio]

## 🟡 Importantes (antes de escalar a 5 abonos)
7. Validar **SIM800 con SIM real** + armar su circuito físico (picos de 2A — ojo alimentación). Solo bloquea la línea minera/sin-WiFi, no los comercios con router. [@esquematico + @hardware + @comms]
8. Validar **OTA en hardware** (código completo, sin prueba de subida real). [@firmware]
9. Validar **NTP** (offset sin confirmar en hardware). [@firmware]
10. Dashboard: renderizar bloque "resiliencia" que el firmware ya emite; vista mobile "de un vistazo" para el comerciante. [@frontend + @diseno]
    - 🔀 **EN BRANCH** `nocturno/local-2026-07-14-vista-estabilidad-comercio` (pendiente de merge): render de la **mitad de resiliencia** en la vista del COMERCIO (hasta ahora solo estaba en el panel admin). `App.jsx` mapea al `reading` del cliente los campos que el firmware ya emite (`connection_mode`, `wifi_rssi`, `gsm_signal`, `watchdog/reboot/sensor_fault_count`) **sin tocar el query** (`readings` sin `select=` trae la fila completa → cero riesgo si falta una columna; **no** se expone `wifi_password`) + tarjeta "🛡️ Estabilidad del equipo" de un vistazo (CONEXIÓN / SEÑAL / REINICIOS) reusando `.status-grid`/`.status-card` (mobile 2-col ya existente). Helpers puros `describeConnection`/`describeSignal` (WiFi RSSI dBm y GSM CSQ 0-31, defensivos ante null/99). **Verificado offline:** `npm run build` OK (vite 1.53s); eslint del código nuevo limpio. **Falta (ojos + datos vivos):** que @tester lo vea en pantalla con un device reportando y ajuste copy/umbrales con @diseno. Ver `MATI-HQ/diario/nocturno-local-2026-07-14.md`.
11. **Resumen mensual automático por cliente** ("este mes: 2 alertas, 0 pérdidas") — retiene abonos. [@backend]
    - 🔀 **EN BRANCH** `nocturno/local-2026-07-11-b-resumen-mensual` → veredicto @verificador **MERGE-CON-FIXES** (RLS faltante + falso "protegida" con 0 % cobertura). Lógica pura + wiring + tests (13). Doc: `docs/monthly-summary.md`.
    - 🔀 **EN BRANCH** `nocturno/local-2026-07-13-resumen-mensual-fixes` (pendiente de merge, **sale del branch anterior**): aplica los 2 fixes bloqueantes + 2 deudas del veredicto → **ya mergeable**. FIX 1: `migration_monthly_summaries.sql` con `ENABLE ROW LEVEL SECURITY` + policy anon-SELECT (consistente con readings/alerts; cierra fuga cross-tenant). FIX 2: veredicto de 3 estados — no afirma "protegida todo el mes" con cobertura < 50 %, dice "datos insuficientes". DEUDA 3: `temp_over_critical` respalda el veredicto sin la fila de alerta. DEUDA 4: severidad `emergency` cuenta como crítica. Tests **13 → 16, `node --test` OK**. **Falta (Supabase, día)**: mergear ambos branches en orden y aplicar migración + `functions deploy cron-monthly-summary` + re-correr `SETUP_CRON_JOBS.sql`. Ver `MATI-HQ/diario/nocturno-local-2026-07-13.md`.
    - 🔀 **EN BRANCH** `nocturno/local-2026-07-11-b-resumen-mensual` (pendiente de merge): **lógica pura + wiring**. `supabase/functions/_shared/monthly_summary.ts` (sin imports, corre en Deno y en Node) agrega el mes desde `readings`/`alerts`/`door_events`/`power_events` y arma **objeto JSON + texto castellano** para WhatsApp/dashboard ("📊 Resumen de Julio 2026 · 2 alertas · 0 pérdidas · cobertura 50.1 %..."). `monthly_summary.test.ts` = **13 checks, `node --test` OK**. Wiring **deploy-pendiente**: `cron-monthly-summary/index.ts` (edge function que lee la DB e importa la lógica, sin duplicar reglas), `migration_monthly_summaries.sql` (tabla cache), job `cron-monthly-summary` agregado a `SETUP_CRON_JOBS.sql`. Doc: `docs/monthly-summary.md`. **Falta (Supabase, día)**: aplicar migración + `functions deploy` + re-correr cron + render en dashboard/WhatsApp. Ver `MATI-HQ/diario/nocturno-local-2026-07-11-b.md`.
12. Retención de datos (pg_cron). [@backend]
13. Hoja de mostrador + plantillas WhatsApp Business. [@diseno + @comercial]
18. **Cerrar hallazgo de la decisión de alerta** (el core que se cobra, "el servicio avisa"). [@firmware, en banco]
    - 🔀 **EN BRANCH** `nocturno/local-2026-07-18-alert-model` (pendiente de merge): **modelo de referencia offline** de `firmware_modular/alerts.h::checkAlerts` (`tools/alert_model.py`, stdlib) + suite de regresión (`tools/test_alert_model.py`, **12 tests OK**) + doc `docs/alert-model.md`. Hasta ahora la lógica que dispara la alerta **no tenía ningún test offline** (el `esp32_simulator.py` la finge: `alert_active = temp_avg > -10`). El modelo es un *oráculo* fiel (espejo 1:1, no versión limpia). **Hallazgo latente para @firmware (con test que lo demuestra, NO corregido de noche):** las ramas de defrost/cooldown de `checkAlerts` hacen `return` sin reiniciar `highTempSec`/`lastAlertCheck` → la 1ª muestra crítica **post-descongelamiento** dispara la alerta al instante, salteándose `alert_delay_sec` (dirección falso-positivo → riesgo de churn). Fix candidato de 2 líneas en banco (detalle en el doc). **Verificado offline:** `python -m unittest tools.test_alert_model` → OK. **Falta (banco):** que @firmware confirme el hallazgo en hardware y aplique el fix + actualice modelo y test en el mismo commit. Ver `MATI-HQ/diario/nocturno-local-2026-07-18.md`.

## 🟢 v2 (con 5+ abonos)
14. Revisión B de la PCB con lecciones de campo (brownout, SIM800, montaje en estanca) — hay base KiCad en `hardware/` + gabinete 3D en `gabinete_3d_viewer/` + `gabinete_industrial.FCMacro` (rescatados de la copia archivada). La carcaza 3D custom con marca = pieza para Diseño y Manufactura UTN. [@pcb + @hardware]
15. Upsell relés: "puerta abierta → sirena" + control remoto (10 módulos en stock). [@firmware + @comercial]
16. WhatsApp Business API integrada (con 10+ abonos). [@backend]
17. Línea minera Panamerican: retomar deploy Cerro Moro cuando la línea comercial esté rodando. [MATÍAS]

## Nota de identidad
Dos destinos, un código: Cerro Moro (minero, SIM800) y SaaS comercios Bahía (WiFi). La v1 comercial va por WiFi — el SIM800 NO la bloquea.

## Hecho
- 2026-07-07 — Consolidado en C:\Proyectos\frioseguro + GitHub (223 archivos; ganó la copia Desktop con +3 meses de trabajo; rescatados KiCad/gabinete 3D/esquemáticos v4-v6 de la copia vieja). Copias archivadas en `_ARCHIVO_FRIOSEGURO_copia_vieja` y `_ARCHIVO_AgenteBahia`.
