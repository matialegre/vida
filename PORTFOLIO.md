# PORTFOLIO — Cuartel General de Matías Alegre

> **Documento maestro.** El agente `@director` lo lee PRIMERO en cada sesión y lo actualiza al cerrar.
> Regla de oro: lo que no está acá (o en un doc linkeado) no existe. Cold-start test siempre.
> Creado 2026-07-07 por Claude (Fable) a partir del análisis completo de todos los repos.

## Quién es Matías (contexto mínimo)
Estudiante de último año de Ing. Electrónica (UTN Bahía Blanca, legajo 19074) + dev en Mundo Outdoor (autor de un ERP en producción, 23 usuarios) + investigador GIMAP (telemetría de galgas para Louis Dreyfus). En vacaciones de la uni hasta agosto 2026. Email dev: alegrematiasdev1@gmail.com.

## 👑 Jerarquía oficial de prioridades (Matías, 2026-07-07)
**1º PLATA y UNIVERSIDAD — empatados en la cima.** Plata = abonos FrioSeguro + ERP hacia autonomía (PLATA.md). Universidad = TPs, finales, cursada, proyecto final (recibirse).
**2º Octubre** (datalogger + galgas para la parada Dreyfus) — es también reputación/plata futura (servicios industriales).
**3º Todo lo demás.**
Regla de desempate diaria: si un día no alcanza para todo, sobrevive lo que tenga deadline más cercano de las categorías 1 y 2. La uni tiene fechas de cátedra (no se mueven); la plata tiene momentum (una semana sin acción comercial = pipeline muerto); el banco tiene octubre. En ese orden se sacrifica: 3º → banco (si octubre lo permite) → JAMÁS uni ni acción comercial de la semana.

## ⏰ Deadlines duros (ordenan TODO)

| Fecha | Qué | Proyecto |
|---|---|---|
| **2026-08-01** | Cierre de cuadernillos de TP de Sistemas de Control Industrial | UTN |
| **2026-08-18** | Inicio de clases 2° cuatrimestre — **LAS ÚLTIMAS 5 MATERIAS**: Sistemas de Control, Electrónica de Potencia, Economía, Inglés 2, Diseño y Manufactura de Circuitos Impresos. Finales: llamados 3-7 y 10-14 ago (fechas UTN: ver `CALENDARIO_UTN_2026.md`, única fuente) | UTN |
| Sin fecha de cátedra (los agenda Matías) | **Finales POR PROYECTO** de materias ya cursadas: TC2, Medidas Electrónicas 2, Tecnología, Sist. de Control Industrial, Electrónica Industrial | UTN |
| **Fin de año 2026** | **Proyecto de LABORATORIO de Sistemas de Control** (materia que cursa 2° cuatri) | UTN |
| 2026-2° cuatri | **Elegir el PROYECTO FINAL de carrera** (aún indefinido — decisión estratégica: alinearlo con datalogger GIMAP / harvesting / FrioSeguro) | UTN |
| **2026-10 (fecha exacta TBD)** | **PARADA DE PLANTA Louis Dreyfus** — el sistema de galgas tiene que estar validado e instalable | galgas-supabase |
| 2027 | Proyecto Final de carrera | UTN |

## 🗂️ Los proyectos (estado al 2026-07-07)

### P0 — galgas-supabase (LA PRIORIDAD TÉCNICA)
`C:\Proyectos\galgas` (repo: github.com/matialegre/galgas · pendientes: `QUE_FALTA.md`)
Reescritura cloud-first del sistema de galgas para Dreyfus (REDLER RPRB3). Fusión: física/DSP del GIMAP clásico + cliente Supabase de FrioSeguro. **Validado E2E en banco**: readings con deep sleep, provisioning, comandos con ack, OTA cloud (0.1.2→0.1.3), self-trigger alerta. Fuente de verdad del repo: `act.md` (las docs raíz están desactualizadas).
**Pendiente para octubre:** RX completo (hoy heartbeat-only; falta Realtime subscriber + LCD + buzzer + gateway HTTP del PLAN v5) · test con galga FÍSICA real (hoy `DEV_SIMULATE_ADC`) · test con LiPo real (hoy `DEV_BENCH_NO_BATTERY`) · re-flashear B · OTA que distinga A/B · bucket firmware con URL firmada · brownout USB · integrar mockup SCADA `redler/` al dashboard.
TLS: cert **GTS Root R4** + HTTPClient simple (lección aprendida, no repetir el debug).

### P0 — UTN: cierre de TPs integradores (deadline 1-ago)
`C:\Users\Pandemonium\Documents\UNIVERSIDAD UTN`
- **SCI**: TP Integrador 2026 desarrollado (WPLSoft, LOGO!, TSX Micro, S7-200, SLC500, CODESYS). **Cierra 1-ago.** Verificar completitud contra el cuadernillo, pasar a plantilla `UTxx.SCI Formato para los TP.docx`, entregar. Ojo: hay "temas de final" aparte (foro).
- **Electrónica Industrial**: TP Integrador A–F resuelto completo (mod. 6-jul). Falta verificación final + formato de entrega.
- **TC2 / Medidas Electrónicas 2 / Tecnología**: Matías debe un proyecto por materia. **NO HAY MATERIAL EN DISCO** — primera acción: bajar consignas del aula virtual.

### P1 — FrioSeguro (LA PALANCA DE PLATA)
`C:\Proyectos\frioseguro` (megaprompt en `docs\` · repo: github.com/matialegre/frioseguro · pendientes: `QUE_FALTA.md`)
Monitoreo de frío/puertas. Origen: Panamerican Cerro Moro (7 reefers). Destino comercial: SaaS para comercios de Bahía. **Inventario listo para vender: 5 PCBs, 20 sondas DS18B20, 10 reed switches, 5 ESP32, relés, IP fija propia.**
**Pendiente:** validar SIM800/OTA/NTP en hardware · flashear módulos nuevos · migración SQL columnas nuevas · credenciales de producción · circuito físico SIM800 · retención de datos. Dashboard Netlify y APK v17 ya operativos.

### P0 — RuView / datalogger — **TERMINARLO PRIMERO, antes del trabajo Dreyfus (orden de Matías 2026-07-07, "por las dudas")**
`C:\Proyectos\datalogger` (repo: github.com/matialegre/datalogger · pendientes: `QUE_FALTA.md` · desanidado, la copia vieja quedó en `Desktop\_ARCHIVO_RASPBERRY_copias_viejas`)
Red de vibración: Pico 2 W (MPU6050 50Hz→SD+FFT) → LoRa 433 mesh "RV1" → ESP32-S3 gateway → Vercel → Supabase. Visión: **datalogger fino alineado con las galgas**, perfil de energía "el mejor del planeta".
**Pendiente:** ECO-LoRa (sleep clase A) NO implementado — no mandar `eco on` a P1/P2 · driver INA219 para consumo real · prueba de alcance + salto repetidor · calibrar RSSI-distancia · decisión MicroPython vs C/C++ en nodos para low-power/muestreo determinista.

### P2 — Cosechador de energía (investigación)
`C:\Proyectos\cosechador` (repo: github.com/matialegre/cosechador · pendientes: `QUE_FALTA.md`)
Replicación del paper MEAS-D-25-07766 (harvesting piezo → LTC3588 → supercaps → detector de incendios sin batería). ~10%: BOM completo (~$154.500), nada comprado, sin firmware. 6 fases planificadas. Se cruza con `recolector/` de RuView (mismo LTC3588+supercaps).

### P0 permanente — ERP / Modulia (5hs diarias garantizadas — NUNCA olvidarlo)
`C:\Users\Pandemonium\Documents\BACKUP MATI ERP\codigo` (original en `D:\ERP MUNDO OUTDOOR`)
En producción (Mundo Outdoor, Montagne, TallerEuro, Cassano) + SaaS multi-rubro en curso. Se gobierna con SU `.claude` (37 agents, @empresario como CEO) — el Director no lo microgestiona, pero lo tiene SIEMPRE en el mapa porque:
- **Matías le dedica mínimo 5hs/día** (el bloque 13:00-17:30 ES el ERP + horas remotas) — es su frente diario más grande, no un proyecto lateral.
- **Objetivo declarado (2026-07-07): que quede ANDANDO SOLO** — soporte cubierto por un agente de IA (o pagado), onboarding y cobros automatizados. Cada sesión de ERP debería acercar eso: menos dependencia de Matías, no más features.
- **Es el mejor activo de venta**: "cadena de 13 locales lo usa hace 2 años" — se vende solo con referencia.
- **Es infraestructura para todo lo demás**: servidor físico + IP fija + dominios de Mundo Outdoor pueden hostear servicios del resto del portfolio. Regla Ponytail: lo que ya anda en Supabase/Netlify/Vercel NO se migra; el server propio es capacidad libre para lo NUEVO/pesado (video, MQTT, workers, backups pesados).

### P3 — Otros
- CV/búsqueda laboral: `C:\Users\Pandemonium\Desktop\CV` (CV v6 ES/EN + `buscar_empleos.py` corriendo).
- Kiosco Ofiuco: solo README (sin código en disco). Congelado salvo que aparezca el repo.
- Carpetas aún NO analizadas en Documents: `GALGAS POST DREY (2)`, `GIMAP`, `Datalogger Acelerometro4`, `Refineria`, `CHUBON`, `TALLER EURO`, `Proyecto Android SO` — pedirle a Matías cuáles importan.

## ⚠️ DECISIÓN URGENTE DE MARCA (detectada 2026-07-07 por @comercial)
**"FrioSeguro" ya existe como empresa** (*Frío Seguro Monitoreo del Frío S.R.L.*, CUIT 30-71874481-0, frioseguro.com, app en Play Store) con el MISMO modelo en CABA/Zona Norte. Valida el negocio, pero es riesgo marcario: **Matías debe elegir nombre nuevo para el producto comercial ANTES del primer folleto/contrato** (chequear INPI). El código/repos pueden seguir llamándose frioseguro internamente. Detalle en `comercial\PRECIOS_FRIOSEGURO.md`.

## 💪 Recursos (NO son cuello de botella)
- **Sistemas (vía Mundo Outdoor):** servidor físico disponible, dominios, PCs, compras de infra — lo que haga falta.
- **Hardware (vía GIMAP):** stock enorme ya en mano (mucho más que lo listado en FrioSeguro) + capacidad de compra de lo que falte.
- **Infra propia:** IP fija + router → self-hosting posible.
- **Mano de obra:** decisión de Matías — el equipo son SUS AGENTES DE IA (este sistema), no gente designada. El cuello de botella real es el TIEMPO de Matías y su foco → por eso WIP=1 y este portfolio.
- **Capital:** ~$300.000 ARS/mes propios para invertir.
- **Red humana de refuerzo:** compañeros de la uni con ganas, técnicos/ingenieros accesibles, profesores y contactos. Se usan para lo que un agente no puede hacer: instalar en campo, mediciones presenciales, avales académicos, cerrar ventas.

## 🧭 Jugada estratégica: LA CONVERGENCIA
Matías tiene **5 finales que puede rendir POR PROYECTO** (TC2, Medidas 2, Tecnología, SCI, Electrónica Industrial) + el **Proyecto Final 2027** (sin definir) + los proyectos reales (datalogger GIMAP, galgas, harvesting, FrioSeguro). La jugada correcta NO es hacer 6+ proyectos independientes: es **hacer que cada final-por-proyecto y el Proyecto Final SEAN piezas de los proyectos reales**. Un solo esfuerzo, crédito doble o triple: la uni convalida lo que Dreyfus/GIMAP/FrioSeguro ya necesitan. Ejemplos de mapeo candidato (a validar con cada profesor — Matías tiene los contactos):
- **Medidas Electrónicas 2** → caracterización y calibración del datalogger de vibración/galgas (incertidumbre, trazabilidad, fs real, ruido).
- **Teoría de Circuitos 2** → análisis del front-end analógico (INA333, puente de Wheatstone, filtros) o del harvesting piezo (LTC3588, supercaps, transferencia).
- **Tecnología** → la PCB del datalogger o de FrioSeguro: materiales, procesos, DFM.
- **SCI** → integración SCADA/alertas del sistema de galgas (ya casi lo tiene con el dashboard redler).
- **Electrónica Industrial** → instrumentación del REDLER: lazo 4-20mA/HART equivalente, P&ID de la instalación.
- **Proyecto Final 2027** → candidatos naturales: el datalogger inalámbrico de vibración/deformación completo (con aval GIMAP y cliente real Dreyfus) o el sistema de harvesting piezo aplicado.
El `@utn` consigue los requisitos de cada final-por-proyecto con los profesores y el Director asigna cada pieza al especialista que corresponda.

## 🎯 Plan Julio → Octubre (v1, 2026-07-07)

**Julio (vacaciones = ventana de oro):**
1. Semana 1-2: cerrar y ENTREGAR los 2 TPs integradores (SCI antes del 1-ago con margen). En paralelo, banco de pruebas galgas: galga física + LiPo real.
2. Semana 3-4: RX completo de galgas-supabase + OTA A/B. FrioSeguro: validar SIM800/OTA en hardware con el stock que ya hay.
3. Continuo: conseguir consignas de TC2/Medidas2/Tecnología ANTES de que arranque el cuatri.

**Agosto:** arranca cursado → régimen mixto. Galgas: pruebas de robustez (WiFi industrial, reconexión, NVS tras cortes). FrioSeguro: primera instalación comercial en un comercio de Bahía (carnicería/farmacia) con las 5 placas.

**Septiembre:** pre-parada: ensayo general del sistema completo de galgas + SCADA redler integrado + checklist de instalación en campo. RuView/datalogger si suma a la parada, sino post-octubre.

**Octubre:** PARADA DE PLANTA. Todo lo demás se congela esa semana.

**Reglas del plan:** WIP=1 por dominio · nada se declara "hecho" sin evidencia verificable · cada sesión deja bitácora actualizada · el Director re-prioriza cada semana, no cada hora.

## 🕳️ Huecos conocidos del sistema (auditoría 2026-07-07 — el Director los persigue hasta cerrarlos)
1. **GIT + BACKUP OFFSITE — el riesgo más grande de todos.** Cosechador sin git, repos con copias triple-anidadas y ZIPs como "versionado", todo en UN disco local. Si el disco muere, se pierden años. Fix barato: GitHub privado (gratis) para galgas-supabase, RuView, FrioSeguro, cosechador y MATI-HQ + `git push` como hábito de cierre de sesión. UNA mañana lo deja resuelto.
2. **El sistema de agentes está SIN PROBAR (cold-start).** Nadie verificó aún que una sesión nueva en otra carpeta cargue el modo Director y encuentre todo. Test: abrir Claude en el Escritorio y decir "¿qué hacemos hoy?". Si algo falla, arreglar el harness.
3. **Fecha y logística de la parada de OCTUBRE.** No hay fecha exacta, ni lugar confirmado (¿General Lagos como en febrero? = viaje a Santa Fe), ni definición de quién va de GIMAP. Sin esto, "octubre" no es un plan, es una intención. Preguntar YA en GIMAP.
4. **Consignas reales pendientes.** El plan académico se apoya en supuestos hasta que los profesores respondan: requisitos de los 5 finales-por-proyecto, consigna del labo de Control, y el REGLAMENTO del Proyecto Final (alcance, tutores, si acepta trabajo con empresa/GIMAP).
5. **Lado legal de cobrar:** monotributo/facturación para los abonos, y un **contrato simple de FrioSeguro con límite de responsabilidad** ("el servicio avisa, no garantiza la mercadería") — SIN eso, un freezer perdido puede convertirse en un reclamo contra Matías. Redactar antes del primer abono cobrado.
6. **Seguridad antes de vender:** credenciales hardcodeadas en repos (Supabase anon en RuView, `CREDENCIALES.txt`, defaults de FrioSeguro), bucket firmware público. Barrida de higiene antes del primer cliente pago.
7. **Decisión CV/trabajo:** `buscar_empleos.py` sigue corriendo — ¿el objetivo 2026 es conseguir laburo remoto o construir ingreso propio? Son estrategias distintas que compiten por las mismas mañanas. Matías debe elegir el default (el otro pasa a oportunista).
8. **Carpetas sin analizar** (posible material valioso): `GALGAS POST DREY (2)`, `GIMAP`, `Datalogger Acelerometro4`, `Refineria`, `CHUBON`, `TALLER EURO`, `Proyecto Android SO`.

## 📚 Documentos del cuartel (leer según necesidad)
- `MAPA_PROYECTOS.md` — TODAS las rutas del disco: cada repo, sus archivos clave, sus reglas y trampas. **El próximo agente empieza por acá si no conoce el terreno.**
- `PLAN_MES.md` — plan día por día 7-jul → 18-ago (v2 con calendario UTN real).
- `CALENDARIO_UTN_2026.md` — fechas oficiales UTN (llamados, feriados, inicio clases 18-ago).
- `PLATA.md` — plan de monetización: FrioSeguro como abono (línea 1), Modulia, servicios industriales post-octubre. Métrica única: abonos activos.
- `dominios\*.md` — bitácoras de los 6 especialistas.

## 📓 Bitácora del Director
Formato: `fecha — qué pasó / qué se decidió / próximo paso`

- 2026-07-07 — Sistema creado. Análisis completo de 8 repos hecho por Claude Fable (detalle en dominios/*.md). Deadline crítico detectado: SCI 1-ago. Próximo paso sugerido: sesión de cierre de TP SCI + pedir a Matías las consignas de TC2/Medidas2/Tecnología y la fecha exacta de la parada de octubre.
- 2026-07-13 — **SESIÓN GRANDE: FrioSeguro renació en nube nueva y las 2 placas quedaron E2E online.** (1) Mergeados 2/3 branches nocturnos (secret-scan #6, linter #4) tras veredicto @verificador; resumen-mensual #11 quedó MERGE-CON-FIXES (falta RLS + mensaje cobertura 0%). (2) El Supabase viejo fue ELIMINADO por Matías → se bootstrapeó proyecto nuevo **MATIAS (cjdluhemschrynijzvap)** vía Management API con SETUP_COMPLETO + OTA + 3 fixes de schema descubiertos con hardware real (`migration_fixes_2026-07-13.sql`). (3) Dashboard deployado en **frioseguro-dashboard.vercel.app** (admin+cliente; user admin = alegrematiasdev1@gmail.com). (4) **Placa 1 flasheada por USB, Placa 2 flasheada POR AIRE** (portal cautivo + POST /update — sin cable, tenía el USB bloqueado por el SIM800); ambas registradas y online con lecturas cada 10 s. Arquitectura completa en `frioseguro/docs/ARQUITECTURA_2026-07-13.md`. **Deudas nuevas**: rotar bot Telegram, password al /update, firmware sube wifi_password (sacar), flash al 98%, sondas sin soldar, decidir si desoldar SIM800 de Placa 2. La decisión de marca (nombre ≠ FrioSeguro) sigue pendiente.
- 2026-07-13 (tarde) — **RECORDATORIO HOY MISMO: mandar el mensaje de la RÉPLICA DE LA PLACA DE LA FUENTE** (pedido de Matías en vivo — es PARA HOY, no dejar pasar).
- 2026-07-13 (tarde) — **Hardware Placa 1 (sin SIM), anotado por Matías**: (a) le FALTA LA TIERRA en el borne **TB A6** — necesaria para conectar relé o sensor de puerta → soldarla antes de usar esas funciones (dueño: Matías/@hardware); (b) para 3 sondas de temperatura YA ESTÁ lista. No mucho más pendiente en esa placa.
- 2026-07-13 — **PENDIENTES HUMANOS de Matías (anotados en vivo, no los hace ningún agente):**
  1. **Paco**: pasarle una forma de **reactivar él mismo la Supabase** (free tier se pausa por inactividad → opciones: acceso al dashboard con su cuenta como member, o instructivo de 2 pasos "entrar → Restore project". El Director puede preparar el instructivo; el contacto es de Matías).
  2. **Lucas (GIMAP)**: mandarle mensaje — después le lleva el **ESP en modo AP para el sensor que él tenía**; preguntarle si quiere **SSID y contraseña específicos** o le da igual cualquiera.
  3. **Taller Cassano**: hablarle — coordinar **cuándo le queda cómodo que Matías pase** y confirmar **la dirección/dónde hay que ir**.
- 2026-08-10 (nocturno-local) — **El registro de la SD del datalogger —el entregable del DoD y el futuro "estudio de vibraciones con informe"— está ALIASEADO: la misma máquina que la ráfaga ve en 66 Hz, el canal continuo la reporta en 16 Hz.** Por rotación tocaba datalogger (P0, "terminarlo primero"). Los 🔴 sin branch son banco o hardware y los 16 branches previos auditan *timing* (jitter), *contabilidad* (muestras perdidas) y *forma* (campos, columnas): **ninguno auditaba SIGNIFICADO**, y el datalogger entrega **una frecuencia** — el chip del dashboard dice "Frec. dominante 66 Hz" con cero decimales. Esa cadena tiene cuatro tramos y **tres implementaciones de la FFT** (el nodo mide la tasa, el dashboard hace una en JS, el laboratorio otra con numpy) y nadie las había comparado. `tools/check_vibration_chain.py` (stdlib, read-only, no compila ni toca hardware) modela el sensor desde el register map, extrae las tasas del SCHEMA **y de los tres `config.P*.json` flasheados**, y cruza el reensamblado del gateway con las dos FFTs; trae **tres oráculos numéricos** (DFT en Python puro) que *demuestran* los hallazgos en vez de afirmarlos. **Corrida real: 5 error / 4 warn / 1 info.** **Los dos hechos de los que cae todo:** `mpu6050.py` escribe `PWR_MGMT_1` y `ACCEL_CONFIG` **y nada más** — nunca `CONFIG` (0x1A, el DLPF) ni `SMPLRT_DIV` — así que por default el ancho de banda del acelerómetro es **260 Hz** y su ODR es fija en **1 kHz**. **V1 (el más caro):** el nodo clásico (`mision=off`, que es lo que corren P1/P2/P3) muestrea a `mpu_hz` = **50 Hz** con `sd_interval_s=0` (una fila de SD por muestra) ⇒ **todo entre 25 y 260 Hz se pliega dentro de 0-25 Hz**, y los defectos de rodamiento (3-10× rpm) caen **siempre** adentro; los armónicos pares caen en DC (`alias(50,50)=0`) y dos componentes distintos pueden plegar al mismo lugar. **No hay síntoma:** un CSV aliaseado tiene largo correcto, `seq` contiguo y cero gaps — **pasa los controles de integridad de las tres noches que los escribieron** (integridad ≠ validez). Y el plegado ocurre en el ADC del sensor: no lo deshace ningún reprocesamiento **ni ningún runtime** — anotado en el bloqueante #1, porque gane MicroPython o gane C el canal sigue aliaseado. **Hallazgo que apareció escribiendo el informe y obligó a rehacer el control:** las misiones de la taxonomía **no usan `mpu6050.py`** — cada una tiene su copia del setup y **sí** escribe el DLPF (`lab` 184 Hz, `baja`/`media`/`dreyfus` 94 Hz). Generalizado a las **cinco rutas de muestreo** con el criterio correcto (**Nyquist** vs ancho de banda, no fs vs ancho de banda): `lab` (1000/184) y `media` (333/94) están **BIEN filtradas** —los valores correctos ya están escritos en el repo, no hay que investigarlos, hay que centralizarlos— pero **`mision_baja` deriva su fs de `cfg["mpu_hz"]`**: filtra bien a sus 200 Hz nominales y **mal a los 50 que le dejan los nodos reales**. El filtro y la tasa se eligen en archivos distintos y nada los ata: **bajar `mpu_hz` para ahorrar energía rompe el anti-alias sin tocar una línea de la misión.** **V10:** el DLPF vive en **cuatro copias privadas** y el driver compartido en ninguna, y el registro es **del chip** ⇒ volver a `mision=off` sin cortar la alimentación deja puesto el filtro de la misión anterior: **el mismo código mide distinto según lo que corrió antes** y nada lo registra (ni el dato, ni el header del CSV, ni el frame). **V4:** el gateway **sí** calcula si la ráfaga está completa (`vibMask`/`vibReady`) y **`vibReady` se escribe 3 veces y se lee 0** — bandera muerta; el JSON la publica con `vibN>0` y el dashboard **cachea por id de ráfaga**, así que la única versión que se dibuja puede ser la incompleta, para siempre (push cada 1000 ms vs ráfaga de ~1-3 s, con `fft_auto` prendido en los tres nodos → cada 15-20 s, solo). Mismo patrón que los otros repos —**el sistema informa por un camino que no verificó**— con el agravante de que acá la evidencia **existe, se calcula y se tira**. **V5:** los chunks perdidos se rellenan mutando el array in situ ⇒ una **rampa inventada**; medido: la **frecuencia** del pico sobrevive pero la **amplitud cae 12 %** con UN chunk de 8 ⇒ la métrica de "vibra más que ayer" **se degrada con la calidad del enlace LoRa** y una tendencia de semanas mezcla vibración con RSSI. Nadie cuenta chunks perdidos. **V2/V3:** `burst_hz` acepta 4000 Hz con ODR de 1 kHz (cada muestra leída 4 veces; el default 1000 es *exactamente* la ODR) y `real_hz` mide el **lazo de lectura**, no la tasa de datos nuevos · `read()` divide por el literal 16384 ignorando `self.accel_lsb` y `capture_burst` restaura el rango en un `except: pass` ⇒ camino a que el canal quede a ¼ de escala con la gravedad marcando 0,25 g. **V6/V7/V8:** `fftMag` recorta a la potencia de 2 (hasta -50 % de muestras) y el gateway topea en 512 marcando lo descartado como recibido · **sin ventana**: el mismo tono se reporta con **36 % menos de amplitud** según dónde caiga en el bin de 3,9 Hz, y las dos FFTs **no son comparables** (numpy normaliza al pico ⇒ adimensional) — que es exactamente lo que pide **Medidas Electrónicas 2** (incertidumbre, resolución, trazabilidad) · `sim_data` default **True**: una placa recién flasheada **inventa senos y los archiva en la SD** con header idéntico al real (los tres nodos lo apagan por config, no por diseño → un nodo nuevo como el de Santa Cruz nacería inventando). **Fijado por test lo que está BIEN:** el chunk del nodo y el del gateway coinciden · el nodo **mide** la tasa real y el dashboard la usa para el eje (si el lazo no llega a 1 kHz, el eje se corrige solo) · `fdom = di·hz/N` es la fórmula correcta y el eje se rotula hasta Nyquist · la ráfaga transmite el LSB real del driver · **la ráfaga a 1000 Hz NO aliasea** (Nyquist 500 > 260) — vale la inversión en voz alta: **el espectro lindo del dashboard es el honesto; el registro largo de la SD es el aliaseado** · `analysis.py` resamplea a grilla uniforme antes de la FFT. **Verificado:** `python -m unittest tools.test_check_vibration_chain` **82/82 OK** (7 capas + control negativo que exige que cada defecto encienda **uno y sólo un** código + los 3 oráculos con números fijados); `tools.test_misiones` 20/20 intacto; `TestRepoReal` fija los 10 hallazgos **y la tabla completa de rutas**; **7 mutaciones** hacen fallar la suite. **Método que vale anotar:** una de esas mutaciones (juzgar el aliasing por `fs` en vez de por `fs/2`) **no rompía ningún test** porque con los valores reales del repo los dos criterios dan el mismo veredicto — las mutaciones no sólo validan los tests, **encuentran los que faltan**; agregado el caso que los separa (`fs=150` con `BW=94` pliega aunque `fs > BW`) y el del borde exacto. **Branch `nocturno/local-2026-08-10-cadena-vibracion`** (datalogger, 2 commits: `42b8781` + `c82efed`) + ítem **#15** y notas en el bloqueante **#1** y en el **#5**. **Falta (banco, SIN instrumental — el Pico y el Serial):** contar muestras consecutivas idénticas en una ráfaga (confirma y **mide** V2), excitar el sensor a ~66 Hz y comparar ráfaga vs CSV (V1 pasa de deducción a medición), y mirar cuántos de diez espectros salen con un tramo raro (V4). **Revisado a mano:** las otras tres copias del driver (`pico2-lora-com10/`, `pico2w-wifi-com13/`, `pico/`) tampoco escriben `0x1A` —ni `ACCEL_CONFIG`— ⇒ V1 vale para las cuatro. Detalle: `diario/nocturno-local-2026-08-10.md`.
- 2026-08-10 (nocturno-local-b, 2do turno) — **La alerta de FrioSeguro se decide sobre el PROMEDIO de las dos sondas: con una a −20 °C, la otra puede estar entre −10 y 0 °C sin que suene nada. Y `sensor_probes` —la tabla que le daría identidad a cada sonda— está en la base y en el dashboard, y el firmware no la escribe nunca.** El 1er turno de hoy fue a datalogger; la jerarquía manda PLATA, así que este fue a FrioSeguro. Todo lo software-puro del `QUE_FALTA` ya tenía branch (los 🔴 restantes son hardware, plata o nube), así que fui al tramo que ninguna auditoría había tocado: las cuatro noches previas de este repo —decisión de alertar (07-18), entrega del aviso (08-02), forma de los datos (08-03-b), aislamiento entre clientes (08-09)— **empiezan cuando el número ya existe**. Falta el de arriba, y en FrioSeguro **el número ES el servicio**: ¿de qué sonda física salió, y sigue siendo cierto? `tools/check_temperature_chain.py` (stdlib, read-only, sin nube ni hardware) modela los cuatro tramos —`sensors.h` → `alerts.h` → las 29 columnas de `readings` → el dashboard— leyendo el código real, con **tres oráculos numéricos** que demuestran los hallazgos en vez de afirmarlos. **Corrida real: 5 error / 6 warn / 2 info.** **T1 (el más caro):** `checkAlerts` compara `sensorData.tempAvg` contra **un solo** `config.tempCritical`, y la sonda 2 viene habilitada de fábrica ⇒ la ruta promediada es la de default. **Zona ciega medida de 10 °C**, y su ancho es exactamente `crítico − sonda_sana`: **cuanto mejor anda un freezer, más se puede descongelar el otro** — que es el escenario que se cobra (un equipo desenchufado mientras el otro sigue andando). Es **decisión de producto, no bug**; la salida barata es alertar sobre el **máximo** de las sondas válidas (una línea). **T2:** `sensor_probes` existe en `SETUP_COMPLETO.sql` con `rom_id`, `name`, `slot_index`, `last_temp_c` y **umbrales por sonda**, y el dashboard la lee y la edita — pero en todo `firmware_modular/` la única mención es **un comentario** (`supabase.h:11`) ⇒ los umbrales por sonda que el cliente configura **no llegan a la placa**, `last_temp_c` queda NULL para siempre (y `GUIA_INSTALACION.md:365` lo pide como paso del checklist de instalación: **un ítem que no puede pasar nunca**), y no hay auto-descubrimiento. Tercer caso del repo de *UI escrita contra algo que no existe* (los otros dos: las tablas faltantes del 08-03-b) — con el agravante de que acá **la tabla sí existe y la UI está hecha y pagada**: falta el productor. **T5:** se lee con `getTempCByIndex(0/1)` y **nunca con `getAddress`** ⇒ el índice es la posición en el ROM ordenado del bus, un **ranking entre las presentes**, no una identidad: cambiar una sonda quemada puede **intercambiar `temp1`/`temp2`** —demostrado con ROM reales: la única sonda que NO se tocó es la que cambia de columna— y que pase o no **depende del ROM que venga en la bolsa del repuesto: es azar, no configuración**. **T3/T4:** `checkAlerts` arranca con `if (!sensorData.valid) return;` ⇒ con la sonda caída **deja de evaluarse la temperatura**, la alerta activa **se congela** (el `return` saltea el `clearAlert()`: relé y buzzer encendidos, `alert_active=true` publicado indefinidamente) y **ninguna de las 29 columnas lleva `valid`/`sensor_count`** ⇒ desde la nube no se distingue "sin datos" de "todo bien" y la card queda **en verde** — la señal existe y no viaja (el JSON local sí la publica). Antes de eso, **6,0 s** (3 fallas × 2000 ms) publicando el último valor bueno como fresco: 60 % de chance de que un post caiga adentro, y en el historial se ve una **meseta perfecta**, idéntica a "todo bien". **T6/T7/T8/T9/T10:** `initSensors` **no llama al clasificador** que el propio archivo define y acepta **85,0 °C** —el centinela de power-on-reset, la lectura más probable al energizar— como temperatura válida · `sensorCount` se cuenta **sólo en el boot**: una sonda enchufada después no se lee nunca, y si al arrancar no había ninguna **el lazo entero se saltea** (sin `sensor_fault`, reportando 0.0 °C para siempre) · el dashboard deduce "2 sondas" de `temp2 > -55` y `temp2` sin inicializar vale **0.0** ⇒ dibuja una serie plana en 0 °C en el gráfico de un freezer · la falla de sonda **no llama a `sendTelegramAlert`** (sí lo hacen temperatura, puerta y corte de luz): **el modo de falla más silencioso es el único que no suena en el teléfono**, y la push dice siempre "Sonda 1" porque el firmware no manda `metadata` · el modo simulación es invisible desde la nube (default `false` —al revés que el datalogger— pero **se persiste en NVS**). **Fijado por test lo que está BIEN:** `ds18b20Classify` distingue los tres modos de falla del DS18B20 · la falla se declara recién tras 3 lecturas malas SEGUIDAS · **`readings` publica temp1/temp2/temp_avg por separado** ⇒ el promedio de T1 es auditable después desde la nube · cuando falla la sonda 2 el promedio cae a temp1 en vez de envenenarse · descongelamiento y cooldown suspenden las alertas. **Verificado:** `python -m unittest tools.test_check_temperature_chain` **80/80 OK** (7 capas + control negativo que exige que cada defecto encienda uno y sólo un código + los 3 oráculos con números fijados); `TestRepoReal` fija los 13 hallazgos y las 29+4 columnas; **7 mutaciones** hacen fallar la suite. **Segunda noche seguida que una mutación encuentra un test que faltaba:** `sum → max` en el cálculo del bloqueo del lazo pasó limpia porque `readSensors` tiene un solo `delay()` — agregado el caso que los separa. **Branch `nocturno/local-2026-08-10-b-cadena-temperatura`** (frioseguro, `6099803`) + ítems **#19, #20 y #21** nuevos. **Solapamiento que conviene aprovechar: T2/T5 (identidad de sonda) y el ítem #4 (credencial única por dispositivo) se resuelven en el MISMO reflasheo** que ya está pendiente por la rotación de claves — tres razones para tocar la placa una sola vez. **Falta (banco, 10 min con el Serial, sin instrumental):** desenchufar una sonda con alerta activa y ver si el relé se apaga (T3), mirar la primera línea de `TEMPERATURA INICIAL` tras un power-cycle (T6), arrancar sin sondas y ver si sale un `sensor_fault` (T7). Detalle: `diario/nocturno-local-2026-08-10-b.md`.
- 2026-08-09 (nocturno-local) — **Con la anon key pública de FrioSeguro —la que está en el bundle de Netlify y en el flash de cada ESP32— se le puede cambiar el firmware a toda la flota, y cualquier cliente logueado ve y BORRA el historial de los otros comercios.** Por rotación tocaba frioseguro y la jerarquía manda PLATA; todo lo software-puro del `QUE_FALTA` ya tenía branch, así que fui a la pregunta que el documento **da por respondida sin haberla verificado**: la definición de "vendible" promete *"credenciales únicas por cliente"* y el ítem #4 la lista pendiente, pero **nadie midió qué cuesta no tenerla**. FrioSeguro se vende como abono a varios comercios **contra UNA base**: lo único que separa al comercio A del B es el RLS. `tools/check_tenant_isolation.py` (stdlib, read-only, sin nube ni hardware) replaya el SQL canónico en el orden del RUNBOOK de bootstrap, arma el modelo efectivo de **policies Y grants** —una policy decide qué FILAS, un grant decide qué COLUMNAS— y lo cruza con lo que el firmware y el dashboard **realmente ejercen**. **Corrida real: 16 tablas, 16 con RLS, 37 policies → 18 error / 6 warn / 7 info.** **H2 (el más caro):** `anon_update_ota` es `FOR UPDATE USING (true)` **sin `WITH CHECK`** (Postgres reusa el `USING` para la fila nueva) → un PATCH con la key pública reescribe `ota_updates.file_url`, y el firmware baja de ahí **sin host allowlist, sin firma y sin SHA** (`supabase.h:597`): **ejecución de código arbitrario sobre el equipo del cliente en ≤30 s** — el mismo hallazgo que el H1 del REVIVAL pero por el otro lado (allá se entra por la LAN del sitio, acá por internet). **H3:** el mismo UPDATE libre alcanza `temp_max`/`temp_critical`/`alert_delay_sec` —**el servicio deja de avisar, que es lo que se cobra**, y el dashboard muestra la config nueva como si siempre hubiera sido esa— y `subscription_status` (el paywall se auto-renueva). **H4:** seis tablas de historial con `FOR ALL USING (true)` → incluye **DELETE**, que nadie ejerce, y sin filtro de tenant: **es la fuga que sobrevive aunque el firmware deje de usar la anon key**. **H5:** dos operaciones que informan éxito y afectan **0 filas** (PostgREST no da error por eso): el `DELETE` del device fantasma imprime `[SUPABASE] Fantasma eliminado` sin borrar nada, y el `PATCH readings` del botón SILENCE no tiene policy de UPDATE — mismo patrón que el H2 del REVIVAL. **Fijado por test lo que está BIEN:** las 16 tablas tienen RLS, `user_profiles`/`user_device_access`/`payments` no tienen **ninguna** rama anon (cuentas y plata bien aisladas), el dashboard manda el JWT del usuario y no la anon key, y ningún `.sql` viejo del repo aflojaría el RLS si se corriera por error (control positivo). **Hallazgo de proceso:** `migration_fixes_2026-07-13.sql` dice *"YA APLICADOS"* pero **la checklist copy-paste del RUNBOOK de bootstrap no lo nombra** → un bootstrap nuevo al pie de la letra repite el `42501` del auto-registro que ese archivo existe para evitar (fix: una línea). Escrita `supabase/migration_2026-08-09_tenant_isolation.sql` (idempotente, 5 verificaciones), **NO aplicada** — baja el checker de 18 a 14 errores; **lo que queda es el ítem #4**: mientras el firmware se autentique con la anon key, el RLS no tiene con qué distinguir la placa del comercio A de la del B (salidas: JWT por dispositivo —**coordinable con la rotación de claves ya pendiente, es el mismo reflasheo**— o Edge Function de ingesta). **Verificado:** `python -m unittest tools.test_check_tenant_isolation` **71/71 OK**; `TestRepoReal` fija los 31 hallazgos; 4 mutaciones hacen fallar la suite. **Branch `nocturno/local-2026-08-09-aislamiento-tenant`** (frioseguro, `3fb5e33`) + ítem **#20** nuevo y nota en el **#4**. **Falta (día, nube):** la consulta a `pg_policies` de la base VIVA (el checker mira el **repo**), confirmar H2/H5 con curl contra una placa de banco, y aplicar la migración. Detalle: `diario/nocturno-local-2026-08-09.md`.
- 2026-08-09 (nocturno-local-b, 2do turno) — **El `v_pp` de galgas —el número que dispara la alerta y el que Dreyfus mira en pantalla— puede estar reportando la DERIVA TÉRMICA en vez de la vibración, y el `in_alert` del firmware publicado no mira la señal.** El 1er turno de hoy fue a frioseguro; por rotación tocaba galgas (P0 octubre, sin tocar desde el 08-07-b). Los 🔴 sin branch son hardware puro y los 🟡 ya los auditó el contrato de schema. Los 17 branches previos auditan contratos de **forma** (qué columnas, qué comandos, qué binario); falta el de **significado**: del ADC crudo al número que pinta una card de rojo, ¿los cuatro tramos están de acuerdo sobre qué significa? Esa cadena está escrita **cuatro veces en tres lenguajes** —familia monolítica (`ota_wm_pp`, la publicada en `bins_ota/`), familia modular, dashboard y migraciones— y **nadie las había comparado**. `tools/check_measurement_chain.py` (stdlib, read-only, sin nube ni hardware, no entra a `field_captures`) modela la transformada de cada familia, sus centinelas de min/max, de dónde sale `in_alert`, dónde vive la calibración en NVS y qué umbrales están hardcodeados. **Corrida real: 2 transformadas / 7 umbrales / 6 comandos cal / 3 fuentes de in_alert → 4 error / 3 warn.** **H4 (el más caro, porque `v_pp` ES la alerta):** los centinelas de `sampleBurst()` son `vmin=3.3f`/`vmax=0.0f` —el rango del ADC **crudo**— y la transformada `(v_raw-offset)*k` produce negativos **por diseño** (centrar en cero es para lo que existe `autoffset`). Si un burst entero queda bajo el offset —deriva térmica horas después de calibrar— `vmax` se queda en 0 y **`v_pp` pasa a valer la deriva**: con 50 mV de deriva y 14 mV de vibración real (la mediana medida en las capturas de campo) **se reporta 57 mV → ALERTA espuria** contra el umbral de 40. **En banco no se ve** (recién autoffseteada la señal cruza cero): aparece horas después, en planta, o sea durante la parada. Y **`v_min`/`v_max` se calculan y NO se postean** → desde la nube no hay con qué distinguirlo de una alerta real. La familia modular no tiene el bug (usa ±1e9): es exclusivo del firmware publicado. Demostrado con un **oráculo numérico** (`--demo-sentinel`), no afirmado. **H3:** `in_alert` en el firmware publicado es `(PROFILE_NAME == "ALERTA")`, una **constante de compilación** → el binario `*-palerta` (existe, `3.6.3-palerta`) reporta `true` en **todas** las lecturas (card roja permanente) y los `*-pnorm`/`*-palways` reportan `false` **siempre**, dejando como única detección la comparación que hace el **navegador** mientras alguien tiene la pestaña abierta. La familia modular sí lo deriva de `v_pp`. **Misma columna, dos significados.** **H1:** la calibración de la familia modular es un **dead store** — `nvsSaveCalibration` tiene call sites, `nvsLoadCalK`/`nvsLoadCalOffsetV` **ninguno**, y el sampler usa `DEFAULT_K`/`DEFAULT_OFFSET` de compilación: el comando ackea éxito y no cambia un volt (mismo patrón que el H2 del REVIVAL y el H5 de anoche). **H2/H5/H6/H7:** las dos familias aplican fórmulas distintas al mismo `{offset_v,k}` (coinciden sólo si `k=1`; `OFFSET_SIGN=-1` en B es una tercera variante) · **`cal_with_load` cambia las UNIDADES de `v_pp`** (`k = applied_load/v_corr`) y los **7 umbrales siguen en volts** — seis de los siete son el mismo número escrito seis veces — lo que es **precondición del ítem 🔴 #2**: `field_captures` es ground truth en volts crudos, así que calibrar con carga **antes** de caracterizar invalida la caracterización · los 6 botones de calibración no existen en la familia modular y caen al catch-all que ackea verde (08-04) · cada familia guarda la cal en un namespace de NVS distinto → un OTA que cruce de familia la pierde en silencio. **Fijado por test lo que está BIEN:** la monolítica **recarga la cal en cada boot** (sobrevive deep sleep y power-cycle — **media respuesta al ítem #10**) y `factory_reset` la borra como promete · `sampleMeanRaw()` muestrea crudo (no hay doble corrección) · los 6 comandos existen en la monolítica y el CHECK los acepta · `cal_set` valida `k`. **Verificado:** `python -m unittest tools.test_check_measurement_chain` **69/69 OK** (7 capas + control negativo + oráculo numérico); `TestRepoReal` fija los 7 hallazgos; 4 mutaciones hacen fallar la suite. **Branch `nocturno/local-2026-08-09-b-cadena-medicion`** (galgas, `926b623`) + ítem **#17** y notas en el **#2** y el **#10**. **Falta (banco, SIN galga ni LiPo — 10 min con el Serial abierto):** H4 con un `cal_set` de offset alto (ver `max=0.0000` en el `[BURST]`), H3 con `select in_alert … limit 20` sobre una placa `*-palerta`, H1 con `set_calibration {k:2.0}` y ver que `v_mean` no se duplica. Detalle: `diario/nocturno-local-2026-08-09-b.md`.
- 2026-08-04 (nocturno-local-b, 2do turno) — **Tres de las cuatro misiones del datalogger emiten un frame LoRa que el gateway DESCARTA EN SILENCIO — y en el campo eso se lee como "problema de alcance".** El 1er turno de hoy fue a galgas; de los otros repos, FrioSeguro sólo tiene 🔴 de hardware/compras/decisiones de plata y cosechador está bloqueado por la compra, así que fui a datalogger (P0, "terminarlo primero", y el repo con el commit de día más reciente). Como anoche, el hueco real no era un ítem del `QUE_FALTA`: era **el contrato del frame RV1**, lo único que cruza los **tres lenguajes** del sistema — lo arman **12 lugares** y lo desarman **3 parsers**, y nadie los comparaba. Lo elegí porque **los tres modos de falla no son equivalentes y el peor es mudo**: un campo de más deja basura visible, pero un campo de MENOS hace que `nodo.py:386` (`if len(parts) < 8: return`) y el `parsePayload` del gateway (`if (idx < 0) return false`) tiren el frame **sin contador, sin log y sin error**, mientras el TX de LoRa devuelve OK y `tx_ok` sube. `tools/check_rv1_contract.py` (stdlib, read-only, sin radio ni hardware) cruza emisores (Python con `%` mapeando placeholder→arg posicionalmente, C++ concatenado normalizado a `{expr}`), parsers y specs de doc — siguiendo **docstrings triples**, porque ahí vive la mitad de las specs — contra el canónico `RV1|src|dst|pid|ttl|tipo|via|payload`. **Corrida real: 12 emisores (6 conformes) / 3 parsers / 12 specs → 26 hallazgos.** **H1** `mision_baja`/`mision_media`/`mision_dreyfus` emiten **5 campos con src/dst invertidos** → el commit `da574c3` entero ("los 4 dataloggers de la taxonomía GIMAP en un firmware") con **3 de 4 misiones sin canal en vivo** (`mision_lab` se salva: va por UDP). La SD sigue siendo fuente de verdad, así que el dato no se pierde: se pierde poder mirar el nodo **sin ir a buscar la tarjeta**, que es lo que hace falta en campaña. **H2** el gateway manda los comandos con **7 campos** (`:391`, le falta el `via` vacío; el orden está bien) → el nodo los descarta: **segunda causa, más simple y anterior, del "`eco on` deja el nodo inalcanzable" del QUE_FALTA #3**, que estaba atribuido al sleep — lo delata que el nodo **sí** ackea con 8 campos. **H3** el relay corre *después* del parseo → **el QUE_FALTA #6 (salto de repetidor) no puede pasar con misiones**: es falla de contrato, no de alcance. **H5 la causa raíz es la doc:** `nodo.py:265` y `.ino:195` documentan 7 campos (omiten `via`) y el `.ino` **se contradice seis líneas más abajo** (`:201` sí dice 8), y `MODOS_MISION.md:39` documenta el dialecto de 5 **afirmando compatibilidad** — las misiones se programaron contra el docstring, no contra el parser. La spec correcta estaba en `QUE_HACER.md:213` todo el tiempo. **H6** `test_misiones.py:170,201` **FIJA el dialecto roto**: la suite verde 20/20 protege el defecto. **Fijado por test lo que está BIEN:** `nodo.py` es canónico en sus 5 emisores y los dos parsers principales están exactamente de acuerdo — el núcleo de la malla es sólido; lo roto es la capa de misiones montada encima. El checker **no marca todo**: `QUE_HACER.md` y `QUE_FALTA.md` salen limpios (control positivo). **Advertencia clave del doc:** pasar las misiones al frame canónico hace que **llegue, no que se entienda** — el gateway no tiene handler para los cuerpos `BAJA,…`/`DREY,…`; el fix completo son **dos** pasos. **NO corregido** (generator≠evaluator): 5 fixes candidatos en `docs/rv1-contract.md`. **Verificado:** `python -m unittest tools.test_check_rv1_contract` **56/56 OK** + `tools/test_misiones.py` 20/20 (regresión intacta); `TestRepoReal` fija los 26 hallazgos. **Branch `nocturno/local-2026-08-04-b-contrato-rv1`** (datalogger, `3c7523b`) + ítem **#14** nuevo y notas en #3 y #6. **Falta (banco):** la demo de 10 minutos que lo prueba en placa — nodo en misión `baja`, la línea `[lora rx] …RV1|GW|P1|3|BAJA,…` **sí** aparece en el Serial del gateway (`:1258` imprime antes del parseo) y el nodo **no** aparece en el dashboard. Detalle: `diario/nocturno-local-2026-08-04-b.md`.
- 2026-08-04 (nocturno-local) — **En el dashboard de galgas hay 4 botones que la placa no implementa, y cuando se aprietan la UI los pinta VERDE.** Los dos turnos de ayer fueron a datalogger y FrioSeguro; galgas (P0 octubre) no se tocaba desde el 07-31 y sus ítems sin branch son todos hardware, nube o decisiones de Matías. El hueco de software puro era algo que el `QUE_FALTA` **no nombraba**: el **contrato de comandos**. Un comando remoto atraviesa **cuatro listas escritas a mano en tres lenguajes** — `CMD_META`+`defaultPayload` (`App.jsx`), `commands_cmd_check` (migraciones) y el dispatch `strcmp` de `ota_wm_pp.ino` — y nadie las comparaba. Lo elegí porque **los tres modos de falla no son equivalentes y el peor es mudo**: si el CHECK rechaza el cmd el INSERT explota con `23514` (ruidoso, el operario avisa), pero si el **firmware** no lo conoce cae en el catch-all (`ota_wm_pp.ino:1556`) y hace `ackCommand(…,"ack","no implementado")` → la fila queda `status='ack'` y el dashboard la muestra en verde. `tools/check_command_contract.py` (stdlib, read-only, sin nube ni hardware) cruza las cuatro fuentes: las migraciones **en orden** respetando `drop`+`add` (el CHECK se **reescribe entero** cada vez y ya pasó **5 veces**), el dashboard con un scanner consciente de strings (las descripciones tienen llaves adentro: `'Payload: {offset_v, k}'`), las ramas del firmware con las claves `pl["k"]` que cada una lee (los `strcmp` de la misma línea = una rama: `force_ota_check || ota`), y si el RX consulta la cola. **Corrida real: 26 en el CHECK / 26 ofrecidos (15 activos) / 14 implementados → 7 hallazgos.** **H1** los 4 activos fantasma: `locate`, `set_log_level`, `pause_reports`, `resume_reports` — el caro es `pause_reports`, el botón para callar un nodo durante una intervención en el REDLER: la UI dirá "pausado" y el nodo seguirá posteando. **H2** `locate`/`identify` **invertidos**: el firmware implementa `identify` y lee `duration_ms`; el dashboard ofrece `locate` mandando `seconds` — nombre inexistente **y** clave distinta. **H3** el form de `ota` deja elegir versión y la rama nunca lee `pl["version"]`: siempre baja la última. **H4** el RX **no consulta `/rest/v1/commands`** → todo comando al RX queda `pending` para siempre, y el dashboard le ofrece el mismo panel que a A/B (evidencia directa para el ítem #1, Task 08). **Nota:** `set_config` se perdió en una redefinición del CHECK y sigue vivo en `protocol.h` — el patrón "copiar la lista entera de nuevo" pierde entradas en silencio. **Fijado por test lo que está BIEN:** ningún botón es rechazado por el CHECK (cero `23514` al apretar), ningún cmd de la placa es inalcanzable, y los 6 cmds de calibración + `set_profile` + `set_burst_samples` cierran de punta a punta — el camino que se usa en banco. **Fix candidato más barato (no aplicado, generator≠evaluator):** que el catch-all ackee `status='error'` en vez de `'ack'` — **una palabra**, y los 4 hallazgos silenciosos se vuelven ruidosos para siempre. **Verificado:** `python -m unittest tools.test_check_command_contract` **57/57 OK**; los tests de `TestRepoReal` fijan los 7 hallazgos (si alguien arregla uno, el test avisa). **Branch `nocturno/local-2026-08-04-contrato-comandos`** (galgas, `a617960`) + ítem **#16** nuevo en su QUE_FALTA + nota en el #1. **Falta (día):** contrastar el CHECK contra la base VIVA (la query está en el doc — el checker mira el **repo**) y la demo de 2 minutos en placa (mandar `pause_reports` a A y ver la fila en `ack` mientras el nodo sigue posteando). Detalle: `diario/nocturno-local-2026-08-04.md`.
- 2026-08-03 (nocturno-local-b, 2do turno) — **Tres features de FrioSeguro (WiFi remoto, panel SIM800, terminal AT) están escritas, compiladas y con UI… contra tablas que NO EXISTEN en ninguna parte.** El 1er turno de hoy fue a datalogger (P0 octubre); la jerarquía manda PLATA, así que este fue a FrioSeguro y esta vez a un **🔴 bloqueante del primer abono**: el ítem **#3, migración SQL**. Es el único 🔴 sin branch que es software puro, y el ítem donde el repo **ya se quemó**: PostgREST **no ignora** una columna desconocida — rechaza el request entero y **pierde la fila** (`migration_fixes_2026-07-13.sql`, FIX 1: *"el firmware manda columnas que SETUP_COMPLETO no creaba → CERO lecturas guardadas"*). La única defensa contra eso era **acordarse**; eso es lo que arreglé. `tools/check_schema_drift.py` (stdlib, sin nube ni hardware) compara lo que el firmware **escribe y lee** (`doc["col"]=`, bodies JSON armados a mano, `select=`) contra los `.sql` **canónicos del RUNBOOK**, atribuyendo cada acceso a la tabla del endpoint más cercano **dentro de la misma función**. **Corrida real: 3 tablas + 5 columnas faltantes.** **H1** `wifi_commands`, `sim800_status` y `at_commands` **no están en NINGÚN `.sql` del repo** y las usan firmware **y** dashboard → el cambio de WiFi remoto (la placa consulta cada 10 s), el panel SIM800 (sube 19 campos cada 30 s) y la terminal AT están **muertos de los dos lados, con la UI ya escrita** — el costo ya se pagó, faltan tres `CREATE TABLE`; la línea minera (Cerro Moro) depende de dos. **H2** `ota_updates` tiene `progress` pero el firmware escribe `progress_pct`/`progress_bytes`/`updated_at` → la barra de progreso del OTA nunca avanza (el OTA **sí** funciona: `status` usa columnas que existen) — es justo el dato que se mira cuando un update parece colgado. **H3** `devices` no tiene `wifi_scan_results`/`wifi_scan_at`. **Fijado por test lo que está BIEN:** `readings` **intacta** (29 columnas; el incidente del 07-13 realmente cerrado) y las 7 columnas de config que el firmware lee. **Ojo con el falso amigo:** `migration_resilience_v1.sql` apunta al proyecto viejo borrado y sus columnas ya viven en `SETUP_COMPLETO` — no cubre nada. Escrita `supabase/migration_2026-08-03_schema_drift.sql` (append-only, idempotente, RLS, con el `UNIQUE(device_id)` que el upsert del SIM800 necesita), **NO aplicada** — es cambio de nube. **Verificado:** `python -m unittest tools.test_check_schema_drift` **37/37 OK**; el checker con la migración en la lista da **exit 0** (cierra el drift completo, ni de menos ni de más); suites previas intactas (13 + 35). **Branch `nocturno/local-2026-08-03-b-schema-drift`** (frioseguro, `9c14b5f`) + ítem #3 anotado. **Falta (día, nube+placa):** contrastar contra la base VIVA (la query está en el doc — el checker mira el **repo**, no la nube), aplicar la migración + sus 4 verificaciones, y confirmar SIM800/WiFi remoto E2E. **Deuda anotada, no cerrada:** `wifi_commands.password` guardaría la clave WiFi del cliente en claro (misma deuda que `readings.wifi_password`, hueco #6) y `door_sensors` tampoco existe — no la creé porque el firmware no la toca y su forma no se puede derivar. Detalle: `diario/nocturno-local-2026-08-03-b.md`.
- 2026-08-03 (nocturno-local) — **El nodo del datalogger puede tener la SD muerta y reportar `gaps=0 cola=0 desc=0` — o sea "todo bien" — mientras no escribe nada.** Datalogger (P0, "terminarlo primero") no se tocaba desde el 07-26 y es el único repo con **código nuevo de día**: `e611bc5`, la SD desacoplada en core 1 (`RegistroSD`, cola RAM). Cae justo en el DoD del proyecto (*"loguear a SD SIN GAPS durante N horas"*) y su **contabilidad** no estaba verificada. Ojo con el ítem #5: ya hay **3 branches** de `sd_integrity` (07-09/07-15/07-25), pero los tres analizan el **CSV post-mortem** con la tarjeta en la PC — **ninguno mira el camino EN LA PLACA que lo produce**. Ese es el hueco que tomé. `tools/test_registro_sd.py`: **18 tests** (stdlib, sin hardware) sobre el **código real** (no un espejo — corre el `_hilo()` verdadero de forma determinista), midiendo la ley de conservación `pusheadas == en_archivo + descartes + en_cola + INVISIBLES`. Una muestra se pierde en **3** lugares y el nodo ve **2**. **8 hallazgos con test, NO corregidos (generator≠evaluator):** **H1** `SDLogger.write_line` devuelve `False` (no monta / no abre / write revienta) y `_hilo` ya sacó la línea de la cola ⇒ desaparece sin contador; si la tarjeta se sale *después* del arranque (el `⚠️ SIN SD` se imprime una sola vez) el nodo se ve sano para siempre. **H2** el `except` envuelve al `for` completo: una excepción se traga el resto del lote y saltea el `flush`. **H3** `cerrar()` mira `self.cola`, que ya está vacía mientras el escritor tiene el lote en la mano ⇒ vuelve OK y el `lanzar()` que sigue termina en `machine.reset()`: se pierden hasta 1,8 s al cambiar de misión. **H5** el frame LoRa —único canal en vivo— lleva `gaps=` (atrasos del lazo) y **no** las pérdidas de SD; en fase campaña LoRa se apaga y no queda ninguno. **H6** el colchón real es **1,80 s** (fs_real=333 Hz, no 300), no los "2-3 s" del docstring — un stall de 2 s ya descarta. **H8** atraso y descarte dejan rastros **opuestos** en el CSV (`seq` contiguo + salto de `t_ms` vs. hueco de `seq`) → las tools de `sd_integrity` deben cruzar **las dos** columnas. **Verificado:** `python -m unittest tools.test_registro_sd` **18/18 OK** + `tools/test_misiones.py` 20/20 (regresión intacta). **Branch `nocturno/local-2026-08-03-registro-sd-contabilidad`** (datalogger, `e31820d`) + ítem #5 anotado. **Falta (banco, @firmware+@muestreador):** el hilo real en core 1 (H3 es una carrera: el test prueba que la ventana existe, no cuán seguido se gana), la latencia real de la FAT, la RAM real de la cola (41 KB de caracteres medidos, pero ~65-75 KB reales por overhead de `str` en MicroPython — medir con `gc.mem_free()` antes de subir `MAX_COLA`) y las N horas. Detalle: `diario/nocturno-local-2026-08-03.md`.
- 2026-08-02 (nocturno-local) — **El CAMINO DE AVISO de FrioSeguro (P1 PLATA) tenía 4 formas de tragarse una alerta y ninguna estaba testeada.** Tras 6 noches seguidas en galgas/cuartel, la jerarquía manda PLATA: de los ítems de FrioSeguro sin branch, el único de software puro verificable offline es también el más caro — el producto se cobra por *"el servicio avisa"*, la **decisión** de alertar ya tenía oráculo (`alert_model`, branch 07-18) pero la **entrega** no tenía ninguno. Las **4** fuentes de alerta (temperatura `alerts.h`, puerta `door_sensors.h`, corte de luz `power_monitor.h`, sobrecorriente `current_sensor.h`) desembocan en **un solo gate global de 300 s** (`telegram.h:24`). Escrito `tools/telegram_gate_model.py` (oráculo espejo 1:1, stdlib, sin red) que distingue **entregado** (POST 2xx) de **marcado** ("ya avisé" en la máquina de estados) + **18 tests** + `docs/telegram-gate-model.md`. **4 hallazgos con test, NO corregidos (generator≠evaluator):** **H1** `state` es global → `lastTelegramAlert=0` en cada boot ⇒ **ventana muda de 5 min tras CADA arranque** — justo después de un corte de luz, un watchdog o un OTA: el aviso de corte (~3 s) y el de puerta (120 s) se descartan y quedan marcados como enviados; la alerta de temperatura zafa por el borde, por casualidad. **H2** el gate es único y el descarte **no se difiere, se pierde**: una puerta abierta silencia 5 min a la crítica; en la tormenta realista (corte → puerta → temperatura) de 4 eventos llega **1**. **H3** el timestamp se actualiza sin mirar el código HTTP ⇒ un 429/500/TLS caído **cuenta como enviado**. **H4** los call-sites marcan `alertSent=true` fuera del `if` de internet/token ⇒ placa sin provisionar cree que avisó. **Verificado:** `python -m unittest tools.test_telegram_gate_model` **18/18 OK**. **Branch `nocturno/local-2026-08-02-telegram-gate-model`** (frioseguro) + ítem **#19** nuevo en su QUE_FALTA. **Falta (banco, @firmware):** confirmar H1/H3 en placa (rebootear con puerta abierta; forzar POST fallido) y aplicar los 3 fixes candidatos del doc. Detalle: `diario/nocturno-local-2026-08-02.md`.
- 2026-07-31 (nocturno-local) — **Red de seguridad de la lógica de versionado OTA del dashboard de galgas (P0 octubre) — lógica crítica que estaba SIN un solo test.** Con el backlog de merge ya con tooling de drenaje, la jugada de mayor palanca era software real verificable, no otro modelo. `compareSemver`/`isVersionNewer`/`getProfileSuffix`/`getMaxSilenceSec` decidían si una placa en campo baja o no una OTA (fallo = loop de reflasheo o placa pegada en fw viejo = veneno para la parada) y vivían **inline en `web/src/App.jsx` (1700 líneas) sin evidencia ninguna**. Las **extraje** a `web/src/lib/versioning.js` (fuente única, importada de vuelta — quirúrgico, cero cambio de comportamiento) + **18 tests** `node --test` (cero dependencias). **Hallazgo con test (no corregido, generator≠evaluator):** el desempate por string de `compareSemver` hace que `"3.2.0-B-pmax"` parezca "newer" que `"3.2.0-A-pmax"` solo por `'B'>'A'` → el aislamiento A/B real depende del filtrado por device en el call-site, no del comparador (evidencia directa para **QUE_FALTA #5**). **Verificado:** `node --test src/lib/versioning.test.js` 18/18 + `npm run build` OK. **Branch `nocturno/local-2026-07-31-ota-versioning-tests`** (galgas) + nota en QUE_FALTA #5. Detalle: `diario/nocturno-local-2026-07-31.md`.
- 2026-07-30 (nocturno-local-b, 2do turno) — **Auditoría (evaluador) de las auto-resoluciones del 1er turno: 3 de 8 duplicaban un bullet — arreglado en el harness.** Pase adversarial sobre la salida del generador de las 07-30 (generator≠evaluator). El reporte del 1er turno verificó "0 marcadores de conflicto" pero **no 0 duplicados** — hueco clásico del `merge --union`. Hallado: en 3 resoluciones (galgas `07-29`, frioseguro `07-11-b` y `07-13`) el bullet `EN BRANCH \`X\`` quedaba **repetido**, porque `main` ya documentaba ese branch (con otra redacción, vía sync posterior) y la union sumaba ambas anotaciones — sin marcador, invisible al chequeo de anoche; un humano drenando a ciegas commiteaba la bitácora con el bullet doble (P0 galgas / P1 frioseguro). **Fix en `tools/resolve_doc_conflicts.py`** (harness engineering, no parche a mano): funciones puras `en_branch_refs` + `dedup_en_branch` que descartan la anotación repetida y **conservan la de main**; cableadas a `union_merge_file`/`build_plan`/reporte. **Verificado (git real):** 8 resoluciones regeneradas → **0 duplicados** + **0 marcadores**; las 3 subsumidas quedan **idénticas a main** (`diff` 07-29 = vacío) — su cambio de docs ya vivía ahí, el valor real del branch (código/tests) entra igual merge-a-limpio aparte; 4 repos intactos (read-only). Tests: `test_resolve_doc_conflicts` **13→20 OK**, `test_merge_queue_status` 23/23 (reuse intacto). Reporte `COLA_MERGE_RESOLUCIONES.md` regenerado con notas 🧹; banner 07-30-b en `COLA_MERGE_NOCTURNOS.md`. Sin branch (tooling del cuartel). Detalle: `diario/nocturno-local-2026-07-30-b.md`.
- 2026-07-30 (nocturno-local) — **Los 8 conflictos-docs de la cola YA vienen pre-resueltos: el drenaje de día quedó mecánico de punta a punta (ataca el cuello #1, sin sumar branch).** Paso siguiente al *clasificar* (07-29-b): *resolver* de antemano. Escrito `tools/resolve_doc_conflicts.py` (MATI-HQ main, stdlib, reutiliza los helpers de `merge_queue_status.py`): para cada branch `nocturno/*` que da **CONFLICTO 100% en docs** (los 8 de la cola: 6 galgas + 2 frioseguro, todos por `QUE_FALTA.md`), reconstruye ours/base/theirs y los funde con **`git merge-file --union`** (toma ambos lados, sin marcadores) → deja el `.md` fusionado en `COLA_MERGE_RESOLUCIONES/<repo>/<branch>/` + los comandos exactos de drenaje. Si un branch chocara en código lo **SALTEA** (jamás auto-resuelve código; hoy 0 salteados). **Verificado (git real):** 8 resoluciones, **0 marcadores de conflicto**, cada una conserva **tanto** el avance de main (bullets 07-28-b/07-29) **como** la línea nueva del branch → no revierte nada; los 4 repos intactos (read-only, no mergea). Tests: `python -m unittest tools.test_resolve_doc_conflicts` **13/13 OK** + `test_merge_queue_status` 23/23 (el reuse no rompió nada). Con esto los 8 "conflictos" pasan de merge-a-mano a **revisar-y-copiar**; sumado a 9 LIMPIO-ADITIVO + 10 stale-docs, el único trabajo humano real que queda son los ~3 branches que tocan código. Reporte: `COLA_MERGE_RESOLUCIONES.md` + banner 07-30 en `COLA_MERGE_NOCTURNOS.md`. Sin branch (tooling del cuartel). Detalle: `diario/nocturno-local-2026-07-30.md`.
- 2026-07-29 (nocturno-local-b, 2do turno) — **Cola de merge hecha DRENABLE: el tool ahora dice qué conflicto es trivial y cuál es real (ataca el cuello #1 sin sumar branch).** Con los 4 repos saturados de análisis offline (34 branches nocturnos sin mergear; galgas 6 noches seguidas; PLATA comercial completo), la jugada de mayor palanca no era un branch #35 sino **drenar**. Corregí+extendí `tools/merge_queue_status.py` (MATI-HQ main, como el 07-27-b): (1) **bug de conteo** — reportaba "3 archivos en conflicto" donde había 1 (metía las líneas informativas `Auto-merging`/`CONFLICT` de git como archivos); ahora corta en la línea en blanco; (2) **clasificación doc-vs-código** (`is_doc`/`collision_kind`, puras+testeadas) → cada conflicto/colisión etiquetada `[SOLO docs]`/`[codigo]`/`[doc+codigo]`; (3) rollup accionable + salida ASCII-safe. **Hallazgo que des-asusta la cola (git real):** de 34 branches, **8 CONFLICTO y los 8 son SOLO `QUE_FALTA.md`** (0 tocan firmware); 14 REVISAR-STALE, 10 solo-docs; 9 LIMPIO-ADITIVO. El atasco de galgas es **bitácora, no código** → se resuelve tomando ambos lados del `.md`. Orden de drenaje escrito en `COLA_MERGE_NOCTURNOS.md` (banner 07-29). Verificado: `python -m unittest tools.test_merge_queue_status` **23/23 OK** (+11 nuevos) + corrida en vivo. Reporte fresco: `COLA_MERGE_STATUS_2026-07-29.md`. Sin branch (es tooling del cuartel). Detalle: `diario/nocturno-local-2026-07-29-b.md`.
- 2026-07-29 (nocturno-local) — **Umbrales de v_pp de galgas revalidados contra la señal REAL de campo (P0-octubre, #2).** Hasta hoy el v_pp se calibraba contra un comentario del simulador (`config.h:108`), no contra el terreno. Escrito `tools/analyze_vpp_field.py` (galgas, stdlib, **read-only** sobre las capturas Dreyfus sagradas) que reconstruye la ventana EXACTA del firmware (500 muestras = 1 s, `BURST_SAMPLES_NORMAL`) y mide el v_pp real; + 9 tests + `docs/vpp-field-characterization.md`. **Dos hallazgos:** (1) el v_pp real es **≤14 mV mediana en toda condición normal** → el ALERT de 40 mV tiene margen enorme y el fix MONITOR=35 mV del 07-28-b queda **validado, hasta conservador** (el "30 mV" era pesimista); (2) **cry-wolf latente en el firmware v3 [@firmware]** — el self-trigger (`v_pp>40mV`, 1 burst, **sin hold**) convierte los dropouts de 0.0 V (1.2 % de las muestras con motor ON) en **ALERTAS espurias** (3.0 % de los segundos; 0 % con un guard de rango). El legacy tenía `HOLD_SEC=1.5s`, el informe §9.2 pide 2–3 s. **No aplicado de noche** (generator≠evaluator): fix candidato documentado (guard de outliers y/o restaurar hold) para confirmar en banco. **Branch `nocturno/local-2026-07-29-vpp-field-characterization`** (galgas, commit `877390b`) + puntero en `main` (commit `b717127`). Detalle en `diario/nocturno-local-2026-07-29.md`.
- 2026-07-28 (nocturno-local-b, 2do turno) — **Cry-wolf del SCADA de galgas corregido (P0 octubre, "la cara que Dreyfus ve").** La vista Planta (`PlantaView.jsx`) clasificaba las cards con `TH_VPP_MONITOR=20mV`, por DEBAJO del vpp de reposo documentado (~30mV, `config.h:108`) → **ámbar "MONITOREO" permanente en operación normal** = desensibiliza al operario. Corregido a 35mV (30mV reposo + 1σ ruido 5mV, 5mV bajo el ALERT de 40mV que NO se toca) → imposible causar alerta perdida. De paso cerré el **drift del item #9** de `QUE_FALTA.md` (decía "integrar SCADA — pendiente" cuando ya vive integrado en PlantaView). Verificado offline: `web/ npm run build` OK. **Branch `nocturno/local-2026-07-28-b-scada-monitor-threshold`** (galgas) + puntero en `main` (commit `bbe095f`). Falta (campo): validar el borde exacto contra `field_captures` con señal real [@muestreador+@diseno]. Detalle en `diario/nocturno-local-2026-07-28-b.md`.
- 2026-07-27 (nocturno-local-b, 2do turno) — **Tool que regenera la cola de merge en vivo + hallazgo que corrige la COLA_MERGE del 07-24.** Escrito `tools/merge_queue_status.py` (MATI-HQ, stdlib, solo lectura): mide cada branch `nocturno/*` con la **métrica correcta de merge** (3-way real `merge-tree --write-tree` → diff vs main), no el `git diff main..branch` (two-dot) que engaña. Corriéndolo se desmintió la alarma central del doc a mano: los 8 branches STALE de datalogger **NO borran `misiones/`** — **0 de 32 branches borra un solo archivo** (verificado con git). Estado real: 19 LIMPIO-ADITIVO, 8 REVISAR-STALE, 2 CONFLICTO (frioseguro resumen-mensual), 1 BINARIOS (galgas 07-09), 1 SUBSUMIDO, 1 YA-EN-MAIN (galgas 07-21-b = rama huérfana en el tip de main). 12 tests de `classify()` OK. Reporte: `COLA_MERGE_STATUS_2026-07-27.md`; banner de corrección en `COLA_MERGE_NOCTURNOS.md`. **Ataca el cuello #1 (drenaje humano): 27/32 mergeables sin drama → una sesión con @verificador baja la cola fuerte.** Detalle en `diario/nocturno-local-2026-07-27-b.md`.
- 2026-07-27 (nocturno-local) — **Drift del README de galgas corregido (item #14, P0 octubre).** Con datalogger 100% triado y el provisioning de FrioSeguro completo, el valor nocturno de producir modelos/tests está agotado (el cuello es merge humano). Tomé el fallback documentado: el `README.md` de galgas mentía ("scaffolding esperando contexto", ruta muerta `GALGAS CON SUPABASE`, pointer roto `PLAN_v2_DEFINITIVO.md`) cuando el sistema está validado E2E en banco → reescrito reflejando el estado real (A 0.1.3/B 0.1.1/RX heartbeat), designando `act.md` fuente de verdad; 14/14 pointers verificados. **Branch `nocturno/local-2026-07-27-readme-drift`** (galgas, solo docs, el más barato de drenar). Detalle en `diario/nocturno-local-2026-07-27.md`.
- 2026-07-24 (nocturno-c) — **COLA DE MERGE creada: `COLA_MERGE_NOCTURNOS.md`.** Hay **26 branches nocturnos sin mergear** (galgas 10, datalogger 8, frioseguro 7, cosechador 1), casi ninguno en main — el cuello de botella que los informes venían marcando, ahora cuantificado con git real. Hallazgos: datalogger 🔴 STALE (8 branches 6-8 commits atrás, nunca rebaseados → merge naïve revierte firmware); 2 pares redundantes (frioseguro 07-11-b⊂07-13; datalogger 07-09≈07-15 sd-integrity); galgas 07-09 arrastra binarios de build. galgas+cosechador sanos (drenaje mecánico). **Próximo paso: sesión de drenaje con @verificador siguiendo el orden del doc (PLATA/octubre primero).** Detalle en `diario/nocturno-local-2026-07-24-c.md`.
- 2026-07-07 (cierre de sesión Fable) — Sistema COMPLETO entregado: modo Director global (`~/.claude/CLAUDE.md`), 7 agentes, MATI-HQ con PORTFOLIO/PLAN_MES v2/CALENDARIO/MAPA/PLATA/6 dominios. Calendario UTN real incorporado (feriados 9-10 jul; receso 20-31 jul; finales 3-7 y 10-14 ago; clases 18-ago). Inventario completo registrado (26 MCUs, 10 relés, analizador lógico). Matías sigue con Opus 4.6. **DECISIONES PENDIENTES DE MATÍAS: (1) qué final rinde en agosto, (2) precios FrioSeguro, (3) fecha exacta de la parada de octubre, (4) qué hay en las carpetas no analizadas (GALGAS POST DREY (2), GIMAP, Datalogger Acelerometro4, Refineria, CHUBON).** Primera acción del plan: esta noche checklist TP SCI + inscripción a las 5 materias.
- 2026-08-21 — **SESIÓN GRANDE multi-frente.** (1) **Nodo GIMAP**: diagnosticado el cuelgue en batería (VSYS/ADC29 comparte pin con el WiFi en Pico 2 W → lee ~0.011 V y el corte por batería baja frenaba el boot ANTES del OTA — irrecuperable sin cable). Fix v1.0.3 listo + `flashear_nodo.py` (autodetecta COM, flashea directo, verifica hash) + `probar_bateria.py` (evidencia de que anda SIN cable). **PENDIENTE: enchufar USB, flashear y correr la prueba en batería — NO declarado hecho.** (2) **Nace @bibliotecario (agente 22)** + repo `C:\Proyectos\biblioteca` con primera cosecha (OTA, batería, red, led, visor UDP, protocolo broadcast — cada uno con ficha ORIGEN/PROBADO/GOTCHAS). Regla permanente en CLAUDE.md global: biblioteca antes de código nuevo. (3) **@pcb**: guía de armado en perfboard del acondicionador de galgas Dreyfus (SIN INA333 — v3 usa PGA del ADS1220) en `galgas/hardware/perfboard/` + HTML. Bloqueantes: confirmar llegada del ADS1220 (P1) y comprar R de precisión/C0G. (4) **@firmware**: OTA anti-ladrillo para el Stitch (ESP32-S3, 3 redes: atómico+rollback+modo rescate; compila, cadena PC probada E2E; primer flasheo va por USB). (5) **Pandemonium**: modo conversación arreglado (purga de eco + `claude --continue`); falta prueba de voz real. (6) **STM32 full-bridge** localizado en `C:\FPGA\MotorPortable\` (terminado 19/6, retomar con INICIAR.bat); no existe PDF propio — la doc es motor_ui.html. Próximo paso global: prueba en batería del nodo GIMAP.
- 2026-08-27 (noche) — **EMSICA PASÓ DE PROSPECTO A CLIENTE (PLATA, línea 2).** Brief recibido (Descargas + .txt): rehacer sitio, campaña mail 1 (Sealweld), generador de plantillas HTML, automatización de envío/lectura de casilla. Sesión dedicada full a EMSICA (lo de Eduardo/Paradise va en otra sesión). Ejecutado en paralelo: **@frontend** (emsica-web: submenú Servicios, 27 páginas de marca con SEO "marca + Bahía Blanca", docs/SEO.md para el cliente, build verde — detectó 4 marcas faltantes vía scrape, una mal nombrada en el brief: es Kistler-Morse, no Kane May); **@comercial** (campaña Sealweld completa: investigación, copy por bloques, segmento de 341/596 contactos deduplicados, 15 prospectos nuevos — ángulo: recuperar válvula con pérdida sin sacarla de línea; Sealweld sin distribuidor listado en Sudamérica); **@diseno** (plantilla mail Outlook-safe VML + generador autocontenido para que el cliente escriba sus textos, verificado byte a byte + Playwright); **@backend** (informe factibilidad: mail vive en M365, hay cuenta Brevo A MEDIO VERIFICAR en el DNS — preguntar de quién es —, DKIM sin activar = riesgo spam YA, camino recomendado Brevo + Graph API para leer casilla). Base Access del cliente exportada (596 contactos/106 empresas) con driver ACE instalado. emsica-comercial ahora es repo git; credenciales FTP fuera de git. **Pendiente de Matías/Jorge:** acceso repo GitHub EMSICA/web-emsica, dueño de la cuenta Brevo, acceso DNS, validar segmento, OK logo Sealweld, lista contractual de marcas "representante oficial", deploy Vercel + Search Console + Business Profile. Próximo paso: integración copy→plantilla (en curso) + @verificador + presentar paquete a Jorge.
